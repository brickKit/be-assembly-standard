# 阶段四 · 做外壳验拆回

> **前置：** 先读 [`00-master-guide.md`](../standards/00-master-guide.md)（§2.4 `be-ops` 的 11 个产出，尤其产出 4/7/8——本阶段
> 唯二的新产出；§3.5.1 的仓库清单，本阶段要建 `be-shell-go`/`be-shell-python` 两个仓库）、
> 《BrickEnterprise 设计书.md》**第 13 章全章**（§13.1 核心哲学、§13.2 五外壳分组方案、§13.3 七条铁律、
> §13.4 演进路线、§13.5 代价、§13.6 与 brickKit 的对接、§13.7 拆回门禁、§13.8 合并态环境变量与三份 compose）、
> `shells/README.md`、[`docs/retrospectives/03-阶段三-业务闭环-复盘.md`](../retrospectives/03-阶段三-业务闭环-复盘.md)
> （尤其 §5：阶段收官后先写复盘再展开新线程这条规矩，本阶段同样适用）。
>
> ⚠️ **本阶段与前三阶段的性质不同：不新建业务组件，只做工程基建。** 第 13 章已经把"为什么这样做、这样做
> 对不对"论证得很完整——本文档不重复那部分推理，只负责把它拆成任务、定顺序、定验收标准。**读完第 13 章
> 再往下读**，本文档假设你已经知道"三份 compose 是什么"、"外壳七条铁律是什么"，不再重新解释。

**这一阶段做什么：** 对应设计书 §9.6.1 的**档 3**。把档 2 已经建好的 14 个组件，按语言合并成外壳：

| 语言 | 组件（11 个 Go） | 落点 |
|---|---|---|
| Go | `mdm-customer`、`mdm-product`、`erp-inventory`、`erp-finance`、`erp-sales`、`infra-authz`、`infra-iam-casdoor`、`infra-workflow`、`infra-notification`、`integration-im-dingtalk`、`crm-opportunity` | 合成 **1 个 Go 外壳**（`be-shell-go`） |
| Python | `infra-print` | 单独成一个 **Python 外壳**（`be-shell-python`）——只装 1 个模块，验证的是"这套机制对 Python 语言同样成立"，不是"Python 这边也要装很多模块"（那要等阶段五/六才有第二个 Python 组件） |
| TS / 静态资源 | `infra-bff-mobile`、`frontend-standard` | **保持独立容器，不合并**（设计书 §13.5：跨语言合不进去，Nginx 也合不进去） |

**出档条件**（设计书 §9.6.1 档 3 原文）：合并态业务闭环全绿；§13.7 的拆回门禁全绿；铁律六的 import 扫描全绿；
§13.8 那三份 compose 一条命令启停。

⚠️ **档 3 必须在档 4 之前，理由是成本，不是顺序偏好。** 外壳的坑是结构性的：11 个组件时踩到，改的是
`be-shell-go` 一个仓库；62 个组件时才踩到，要改 61 份 `component.yaml` 的端口，还要拆开一堆已经互相
`import` 了的代码（设计书 §9.6.1 原文）。**本阶段找到的每一个外壳工程问题，都比往后再找便宜得多。**

---

## 本阶段的 12 个任务

| # | 任务 | 产出 |
|---|---|---|
| **第 0 部分 · 调研与外壳骨架** | | |
| 1 | SOP-R 调研：brickKit《组件合并部署.md》全文 + 设计书第 13 章反查 + Go/Python 生态里"一个进程内跑多个独立服务"的现成参考模式 | `docs/design/_调研记录/04-阶段四.md` |
| 2 | `be-sdk-go` 补外壳级构件 + `be-shell-go` 建仓库 → **CP-18** → 骨架 | 外壳启动器：多路 `Listen`、共享连接池、按拓扑序跑迁移、按模块分发 env map |
| 3 | `be-shell-python` 建仓库 → **CP-19** → 骨架 | 同上的 Python 版，先证明装 1 个模块也能跑通整套机制 |
| **第 1 部分 · `be-ops` 扩展** | | |
| 4 | `be-ops` 补产出 4（合并清单+端口分配+迁移顺序）、产出 7（每外壳环境变量表）、产出 8（shell-compose 的 `depends_on`） | 三份新产出，从 `assembly.yaml`+`component.yaml` 生成 |
| **第 2 部分 · 把 11 个 Go 组件迁进外壳** | | |
| 5 | 先迁两个零依赖组件（`mdm-customer`、`mdm-product`）验证机制本身 | 合并态下这两个组件的 `batchGet` 真实互通 |
| 6 | 迁完剩余 9 个 Go 组件，沿用它们各自被建出来时验证过的依赖顺序 | 11 个模块全部装进 `be-shell-go` |
| **第 3 部分 · Python 外壳** | | |
| 7 | 把 `infra-print` 迁进 `be-shell-python` | 单模块外壳，验证机制对 Python 同样成立 |
| **第 4 部分 · 编排与验收** | | |
| 8 | 三份 compose 编排 + 启停脚本 | `make up`/`make down` 各自包住三条命令 |
| 9 | 合并态业务闭环真机验证 | 附录 E 全链路在合并态下重新跑一遍 |
| 10 | §13.7 拆回门禁 + 铁律六 import 扫描的外壳专属检查 + 每周定时任务 | `make gates` 新增外壳专属检查；定时拆回验证 |
| 11 | 新增合并态断言，落进 `be-acceptance` | `make tier2`（合并态专属，`tier0`/`tier1` 判据的延伸） |
| **第 5 部分 · 出档** | 12 | 复盘 → 改设计书 → 写阶段五计划 |

**两个人工检查点**（比阶段三少得多——本阶段不建业务组件，只建两个工具型仓库）：

| 检查点 | 在 | 要建的仓库 |
|---|---|---|
| **CP-18** | Task 2 | `be-shell-go`（`shells/go/`） |
| **CP-19** | Task 3 | `be-shell-python`（`shells/python/`） |

---

## 顺序原则，和为什么

1. **骨架先于迁移**：外壳启动器要先能"装 0 个模块也启动成功"，再往里加真实模块——这是阶段一"先立最小可运行骨架，
   再填内容"这条节奏在外壳工程上的应用，不是新规则。
2. **`be-ops` 的产出 4/7/8 要等外壳骨架先长出真实形状**：产出 4（合并清单）要读外壳启动器实际认得的配置格式，
   产出 7（环境变量表）要对齐外壳启动器实际读 env 的方式——**先有骨架，`be-ops` 才有真实的生成目标**，反过来
   做（先臆想一份格式，`be-ops` 先生成，外壳骨架再去适配）大概率走两遍。
3. **迁移顺序沿用组件各自当初被建出来时验证过的依赖顺序**：`mdm-customer`/`mdm-product` 零依赖，最先迁；
   `erp-inventory`/`erp-finance` 次之；`erp-sales` 要等前两个都迁完（它是唯一同时调三个强依赖的组件）；
   `infra-authz`/`infra-iam-casdoor` 再之后（`erp-sales`/`crm-opportunity` 等组件的权限判定要靠它们）；
   `crm-opportunity` 最后（触发全链路的起点）。**不是重新论证一遍依赖图，是直接照抄阶段二/三已经验证过的
   那张图**——合并顺序和当初的建造顺序没有必然关系，但这次刚好一致，省得重新排一遍。
4. **Python 外壳（Task 7）可以和 Go 外壳的迁移（Task 5–6）并行**：两者除了都要复用 Task 2/3 的骨架经验之外
   没有依赖关系，`infra-print` 不依赖任何 Go 组件。
5. **验收（Task 9–11）必须等全部迁移完成之后**：合并态业务闭环用的是附录 E 全链路，链路上的每一环都要真的在
   外壳里——迁移过程中的中间态没有验收意义，只有调试意义。

---

## 第 0 部分 · 调研与外壳骨架（Task 1–3）

#### Task 1：SOP-R 调研 ✅ 已完成

按 [`02-reference-implementation-standard.md`](../standards/02-reference-implementation-standard.md) §1 的三步法：
先想清楚自己的方案，再去看别人怎么做，最后自己决定怎么写——**这次"别人"不是某个开源 ERP，是 brickKit 平台自己
关于合并部署的第一手材料**：

- [x] 通读 brickKit 仓库 `组件合并部署.md` 全文——与设计书 §13 逐条对照，确认是忠实转译，没有矛盾；补出
      几处转译时被简化掉的细节（网络别名为什么走不通的完整推理链、迁移状态表主键冲突条件）
- [x] Go 侧：确认 `errgroup.Group` 是"一个进程内多个 `Listen`、任一失败即整体退出"的标准做法，且
      `be-sdk-go` 现有 `serveHTTP`/`serveExtraPort` 已经是可以直接复用的独立函数（只是当前私有，导出即可用）
- [x] Python 侧：确认 `uvicorn.Server` 手动实例化 + `asyncio.TaskGroup`（Python ≥ 3.11）是等价模式，语义上
      与 Go 的 `errgroup` 保持一致
- [x] 核对 `module.New(ctx, rt) (*besdk.Module, error)` 的公开性：**11 个 Go 组件逐一核实，全部确认落在
      `backend/module/` 公开包，无一例外**——`mdm-customer` 的包注释原话就是"外壳启动器与 be-ops 产出 4
      都按它生成"，证明这条设计从阶段一起就是刻意的
- [x] 判断 `be-shell-go` 引入 11 个组件模块的具体方式：确认全部 11 个组件的 `go.mod` module path 统一是
      `github.com/brickKit/<组件仓库名>`，当普通 Go 依赖装（`go.mod` require 锁 tag 版本），不用 `go.work`
      做最终构建——`go.work` 仅用于本地联调，不进任何仓库的版本控制

**⭐ 调研过程中发现一个值得诚实记录的张力**：brickKit 自己的文档明确说"以 Go/Rust 为主的项目，别做"（纯
内存收益角度）——本阶段合并的 11 个组件全部是 Go，按这份文档自己的标准这次合并几乎不划算。已核实设计书
§13.0 的理由本来就不是内存收益，是容器数量本身的运维开销 + "档 3 验的是合并机制本身，不是省内存"——理由
站得住，不需要因此调整范围，但值得记在案，见 `_调研记录/04-阶段四.md` §2。

**🔍 另一个更具体的发现，已回填进 Task 4**：产出 7（跨外壳环境变量表）的"跨外壳"改写分支，在本阶段真实
的 11+1 个组件依赖图里大概率一次都不会被触发（`infra-print` 零依赖、也没有任何 Go 组件正式声明依赖它）——
Task 4 的验证方式已相应调整为"必须用一个故意构造的假 fixture 单独验证跨外壳分支"，完整推演见
`_调研记录/04-阶段四.md` §4。

**验证：** 调研记录写完后，不看设计书、只看调研记录，能完整讲清楚"外壳启动器大致要做哪几件事"——
✅ 已达成，见 [`docs/design/_调研记录/04-阶段四.md`](../design/_调研记录/04-阶段四.md)。

#### Task 2：`be-sdk-go` 补外壳级构件 + `be-shell-go` 骨架 ✅ 代码已完成，等待 CP-18 建仓库

⚠️ **本任务的核心判断：外壳启动器不能重新发明一遍 `RunStandalone` 已经做对的事。** `be-sdk-go` 的
`RunStandalone` 目前是"一个进程只装一个模块"的形状（`standalone.go`）：一次 `Bootstrap`（OTel）、一个连接池、
一次 `newModule` 调用、`Listen`。外壳要做的是"一个进程装 N 个模块"——**如果 `be-shell-go` 自己从零重新实现
读环境变量/建连接池/起 gRPC server 这些逻辑，等于把 A4g 那次真实教训（"测试用的构造路径和生产用的构造路径
不是同一条路径"）在 11 倍的爆炸半径上重演一遍**。正确做法：`be-sdk-go` 把 `RunStandalone` 内部已经用得很稳的
几块拆成可独立调用的导出函数，`be-shell-go` 直接复用这几块，只是循环调用 N 次、每次传不同的 `rt`：

- [x] `be-sdk-go@v0.2.6/v0.2.7` 新增导出函数：`NewShellRuntime`（构造单个模块的 `Runtime`，连接池由调用方
      传入不再自己开）、`InitShellAuthz`（整个外壳只调一次，不是每模块各调——发现 `authzRuntime` 是包级
      全局单例，照搬 `RunStandalone` 会导致后面的调用悄悄覆盖前面的、且 N-1 个 bundle 轮询 goroutine 永久
      泄漏，这条不是从设计书推演出来的，是读 `authz.go` 源码时发现的真实缺口）、`ServeHTTP`/`ServeExtraPort`
      （`serveHTTP`/`serveExtraPort` 的导出别名）、`BuildPGDSN`/`BuildNATSURL`（外壳级共享连接串复用，不是
      每模块各一份）。`Bootstrap` 本来就是导出的，外壳直接复用，只调一次。4 条新测试全绿，`make test`
      （-race）全量回归无破坏
- [x] `be-shell-go` 骨架（`shells/go/`，尚未 `git init`，等 CP-18）：
      - `internal/shell.Run`：`Bootstrap` 一次 → 开一个共享 `*sql.DB`/`*nats.Conn` → `InitShellAuthz` 一次 →
        按 `cfg.Modules` 顺序逐个调 `New` → 按同一顺序逐个跑迁移 → `errgroup.Group` 一起 `Listen` → 任一失败
        或 `ctx` 取消，全部优雅退出
      - 迁移执行：`golang-migrate/v4/source/iofs` 直接吃 `*besdk.Module.Migrations`（各组件 `migrations` 包
        用 `//go:embed *.sql` 导出的 `fs.FS`），DSN 用外壳共享串 + 按模块 `schema` 追加
        `search_path`/`x-migrations-table`——与全拆态迁移容器共用同一份 `.sql` 文件，不新建一份
      - 共享连接池：`besdk.WithTx(ctx, db, role, schema, fn)` 本来就是参数化的，外壳传共享 `db` +
        每个模块自己的 role/schema 直接复用，不需要单独抽一层（`_调研记录/04-阶段四.md` 待核实清单第 3 条
        已确认）
      - **外壳自己的健康检查（`Config.HealthPort`）**：计划原文没有覆盖到，写骨架时发现"11 个模块各有
        各的 `/healthz`，谁的代表整个容器"这个问题平台不会替外壳回答——加了一个独立于任何模块的最小
        `/healthz`（不查任何模块、不查任何依赖，同既有判据），供 Task 4/8 的 `shell-compose.yml` 健康检查
        探测
      - 空转验证：3 条测试全部用假模块，不依赖任何真实组件仓库/真实 Postgres（`sql.Open` 是懒的，`nats.Connect`
        需要真实可达的 NATS）——两个假模块共享 DB/NATS 但各自字段独立、`ctx` 取消后优雅关闭且 `Stop`
        都被调用、`HealthPort` 独立于任何模块响应
      - **单模块 `Start()` 里 panic 不崩溃整个进程**：写测试时才发现的真实缺口——`RunStandalone` 的等价
        调用不需要这层 `recover`（单模块进程整个崩溃退出正是正确行为，Docker 重启策略负责），外壳把 N 个
        模块塞进一个进程后，同一个未捕获 panic 会把其余模块一起带崩且无法被 `errgroup` 拦住——已加
        `recover` 并转成清楚指出 `componentID` 的 error，测试验证过 recover 真的生效
      - `go.mod` 遇到一次生态地板上移（`golang.org/x/sync@v0.23.0` 要求 `go >= 1.26`，第三次同类问题，
        见 D0/C16）——核实只是 `errgroup` 这一个小包的要求，改用 `v0.22.0`（同样支持 `errgroup.Group`，
        `go.mod` 地板仍是 `1.25.11`）原地解决，不牵动全项目的 Go 版本，真机 `docker build`（`golang:1.25-alpine`）
        构建通过

**验证：** ✅ 已完成——用假模块起 `internal/shell.Run`（不依赖真实组件仓库），确认能起、能优雅关闭
（`ctx` 取消后 `Stop` 都被调用）；故意让假模块的 `Start()` panic，确认 `recover` 住、返回的错误里带着
出问题的 `componentID`、测试进程本身没有崩溃；`go build`/`go vet`/`gofmt -l`/`docker build` 全部通过。

#### Task 3：`be-shell-python` 建仓库 → CP-19 → 骨架

同 Task 2 的判据，Python 版。**本阶段只准备装 1 个模块**（`infra-print`）——骨架仍然要按"能装 N 个"的设计写
（不能因为现在只有 1 个就写死成单模块专用），但验收范围只到"装 1 个模块能跑通"，不需要在没有第二个 Python
组件之前假想多模块场景硬造一个测试。

- [ ] `be-sdk-python` 是否需要同 Task 2 一样拆出可复用构件——**大概率需要**，判断方式一致：`run_standalone`
      现在是"一个进程一个模块"的形状，外壳需要的是"每个模块自己的 `Runtime`，池共享"这个模式
- [ ] ASGI 应用的合并方式：`Starlette`/`FastAPI` 支持用 `Mount` 把多个子应用挂在同一个 `uvicorn.Server`
      下共享一个端口，但设计书 §13.3 铁律三要求"每个组件的 `deployment.port` 必须独立"——**不能用 `Mount`
      这种"同端口不同路径"的方案**，必须是"同进程、不同端口，每个模块各自的 ASGI app 各自绑一个
      `uvicorn.Server` 实例，用 `asyncio.gather` 一起跑"

**验证：** 同 Task 2，用假模块验证骨架本身（能起、能优雅关闭、单模块异常不拖垮进程），这次额外验证
"1 个模块"这个退化场景本身没有因为"只有一个"而抄了近路（比如误把外壳级的池/配置逻辑跳过，直接照抄
`run_standalone` 的单模块逻辑）——**外壳骨架必须走"N 模块"的那条代码路径，即使 N=1**，否则阶段五/六真的
装第二个 Python 模块时，骨架代码本身还要重写一遍。

---

## 第 1 部分 · `be-ops` 扩展（Task 4）

#### Task 4：产出 4/7/8

- [ ] **产出 4（合并清单）**：读全部已装配组件的 `component.yaml`/`brickkit.yaml`，产出"哪些组件进哪个外壳、
      每个模块的端口分配、迁移执行顺序"——**生成的对象是 Task 2/3 定下的骨架实际认得的格式**，不是先臆想一份
      格式。端口分配直接读每个组件自己 `component.yaml` 声明的 `deployment.port`/`extraPorts`
      （设计书 §13.8.1："外壳启动器要从每个模块自己的 `component.yaml` 读端口，不许在外壳里另写一份端口表"）
- [ ] **产出 7（每外壳环境变量表）**：按设计书 §13.8.2 的规则——同外壳依赖用
      `http://127.0.0.1:<对方端口>`，跨外壳/独立容器依赖用 `http://<宿主机地址>:<对方端口>`。**必须读
      `brickkit up --dry-run` 生成的 `local-debug.*.env` 作为输入，只重写依赖地址那几行，其余原样**（§13.8.2
      原话："自己另算的那份，早晚和平台的算法分叉"）——这意味着 Task 4 依赖每个即将合并的组件先在
      `brickkit.yaml` 里被标成 `local: true`（否则平台不会为它生成 `local-debug.*.env`），这一步的具体时机
      在 Task 5/6 迁移时同步做，不是 Task 4 自己单独一步
      ⚠️ **调研发现（`_调研记录/04-阶段四.md` §4）：本阶段的真实依赖图里，"跨外壳"这条改写分支大概率一次
      都不会被触发**——11 个 Go 组件互相之间的依赖天然是"同外壳"（brickKit 自己生成的 `local-debug` 对
      这种情况本来就是对的，不需要改写）；`infra-print` 零依赖，也没有任何 Go 组件正式声明依赖它（打印是
      `frontend-standard` 直接调用，不经过任何后端依赖边）。**这不代表可以跳过跨外壳分支的实现**（阶段
      五/六一旦出现第二个 Go 外壳或 Python 外壳装了多个互相依赖的模块，这个分支立刻会是刚需）——只是验证
      方式要相应调整：真实的 11+1 个组件只能验证"同外壳不需要改写也是对的"这一半，**跨外壳分支必须用一个
      故意构造的假 fixture（两个组件分属不同 local 进程）单独验证**，不能指望真实依赖图里天然长出一个
      跨外壳案例
- [ ] **产出 8（shell-compose 的 `depends_on`）**：外壳一必须最先起（主数据在里面），其余外壳依赖它做
      `batchGet` 校准对账——本阶段只有 1 个 Go 外壳 + 1 个 Python 外壳，`depends_on` 关系比设计书 §13.8.3
      描述的"五外壳"简单得多（Python 外壳的 `infra-print` 零依赖，不需要等 Go 外壳启动完成）

**验证：** 用 Task 5 迁移的头两个组件（`mdm-customer`/`mdm-product`）跑一遍产出 4/7/8 的生成，人工核对生成的
合并清单/环境变量表/`depends_on` 与手工推算的结果一致；刻意在其中一个组件的 `component.yaml` 改错一个端口，
确认生成过程能报错而不是静默生成一份错误清单。

---

## 第 2 部分 · 把 11 个 Go 组件迁进外壳（Task 5–6）

#### Task 5：先迁两个零依赖组件

- [ ] `mdm-customer`/`mdm-product` 从 `brickkit.yaml` 标成 `local: true`（配合 Task 4 产出 7 的输入需要）,
      模块从 `be-shell-go` 里真实装载
- [ ] 真机验证：合并态下两者的健康检查、REST 接口、跨模块 `batchGet`（若测试场景需要）都真实工作；
      **`brickkit up --dry-run` 确认这两个组件不再生成独立 service**（§13.1 `local: true` 的既有行为，本阶段
      第一次在"外壳"语境下被真正用到，不是新行为）

**验证：** 只有这两个模块在外壳里跑，其余 9 个组件仍按阶段三的独立容器形态运行；确认这种"部分合并"的混合
形态本身不会让 `brickkit up`/`make check` 报错——这也是在为设计书 §13.4"阶段二操作细节"（客户后来把
`erp-sales` 单独拆出外壳）预先验证反向操作的可行性。

#### Task 6：迁完剩余 9 个 Go 组件

按 §"顺序原则"第 3 条的依赖序，逐个把 `erp-inventory`、`erp-finance`、`erp-sales`、`infra-authz`、
`infra-iam-casdoor`、`infra-workflow`、`infra-notification`、`integration-im-dingtalk`、`crm-opportunity`
标成 `local: true` 并装进 `be-shell-go`。

- [ ] 每迁完一个，重跑一次该组件自己的 `make tier0`-等价真机检查（健康检查、REST/gRPC 连通）——**不要等 11
      个全部迁完才第一次验证**，出问题时范围收窄到"最后迁的那一个"
- [ ] 全部迁完后：`docker ps` 确认只剩 1 个 Go 外壳容器（不再是 11 个独立容器）+ `infra-print` 独立容器
      （Task 7 之前）+ `infra-bff-mobile`/`frontend-standard` 独立容器 + 5 个基础资源

**验证：** 11 个模块全部装进 `be-shell-go` 后，容器数从"11 + 3（Python/TS×2）"变成"1 + 1（Python）+ 2（TS/
静态）"；`docker stats` 确认内存占用明显低于 11 个独立容器之和（设计书 §13 的合并收益要在这里第一次被真实
量出来，不是只停留在论证层面）。

---

## 第 3 部分 · Python 外壳（Task 7）

#### Task 7：`infra-print` 迁进 `be-shell-python`

- [ ] `infra-print` 标成 `local: true`，装进 `be-shell-python`
- [ ] 真机验证：渲染接口真实工作（复用阶段三 Task 10 已经验证过的"真实渲染一张 A4 送货单 PDF"这条判据，
      这次在合并态下重跑一遍）

**验证：** 与 Task 5 一样，先确认"1 个模块的外壳"这个退化场景本身没有抄近路——`be-shell-python` 骨架代码
路径与将来装第二个 Python 组件时会走的代码路径必须是同一条。

---

## 第 4 部分 · 编排与验收（Task 8–11）

#### Task 8：三份 compose 编排 + 启停脚本

- [ ] `docker-compose.infra.yml`（已存在，不用改）+ `.brickkit/` 下 `brickkit up` 生成的 compose（阶段一
      这一档只剩 `infra-bff-mobile`/`frontend-standard` 两个 service）+ `shell-compose.yml`（`be-ops` 新
      产出，Go 外壳 + Python 外壳两个容器）
- [ ] 三份必须共享同一个 external network `be-net`（设计书 §13.8.3 最后一条：`docker-compose.infra.yml`/
      `shell-compose.yml` 都显式声明 `external: true` 接进 `be-net`；平台生成的那份做不到，靠 `expose: true`
      + `exposePort` + Traefik file provider 补）
- [ ] 根 `Makefile` 新增 `make shell-up`/`make shell-down`（委派到 `be-ops` 生成的 `shell-compose.yml`），
      `make up`/`make down` 各自扩展成包住全部三条命令的一键脚本（设计书 §13.8.3 原话：**"必须给出成套的
      启停脚本，否则现场一定会出现'以为关干净了，其实外壳还在跑着占着端口'"**）

**验证：** `make up` 一条命令，5 基础资源 + 2 外壳 + 2 独立容器全部按正确顺序（①基础资源健康→②建库脚本→
③外壳→④`brickkit up`）起来且全部 `healthy`；`make down` 一条命令全部干净停掉，`docker ps` 确认零残留。

#### Task 9：合并态业务闭环真机验证

- [ ] 附录 E 全链路（CRM 赢单 → 建单 → 锁库存 → 生成应收 → 审批 → 钉钉通知 → 打印送货单 PDF）在合并态下
      完整重跑一遍——**这是阶段三 Task 14 同一条链路，本次只是换了部署形态，业务断言不变**
- [ ] Saga 补偿、超时查询、DLQ 进出，同样在合并态下各走一遍（阶段三已经验证过独立容器形态，这次验证的是
      "换了进程边界，这些机制是否还成立"）

**验证：** 与阶段三 Task 14 完全相同的验收清单，跑在合并态部署上；任何一条在独立容器形态下成立、合并态下
不成立的行为，都是本阶段要抓的问题（大概率命中铁律六或铁律二，见 Task 10）。

#### Task 10：拆回门禁 + 铁律六扫描 + 每周定时任务

- [ ] **实现设计书 §13.7 的拆回门禁**：把全部 `local: true`/`localPort` 去掉，`brickkit up` 一次，确认
      11 个 Go 组件 + `infra-print` 各自单独起一个容器、全部 `healthy`，`be-acceptance` 的业务闭环用例全绿——
      **这条门禁的失败不代表"独立部署有 bug"，代表"合并那一刻磨掉了组件性"**（§13.7 原文），失败时优先怀疑
      铁律六（有人 `import` 了别人）或铁律二（有人跨 schema 查了）
- [ ] **铁律六 import 扫描扩展到外壳仓库**：`make gates` 现有的 import-scan 只扫组件仓库之间是否互相
      `import`；`be-shell-go`/`be-shell-python` 引入 `go.work`（本地开发用）之后，跨组件 `import` 在编辑器
      里会变得"看起来更顺手"——扫描器需要确认：① 外壳仓库自己可以 `import` 每个组件的 `backend/module`
      （这是允许的）；② 但组件仓库之间任何一条 `import` 边依然是 0（这条判据不变，只是现在要在"存在
      `go.work`"这个新环境下重新确认扫描器没有被 `go.work` 的模块解析规则绕过）
- [ ] **每周定时拆回验证**：加进 `be-acceptance` 的定时任务（设计书 §13.7："平时每周一次"）——具体机制
      （cron/GitHub Actions scheduled workflow/别的）留给实现时按现有基础设施决定，本任务只要求"确实有一个
      每周自动跑一次拆回门禁的机制，而不是只在改动合并组关系时手动跑"

**验证：** 真的跑一次完整拆回（不是纸面推演）；故意在 `erp-sales` 里临时加一行 `import` 某个其他组件的代码，
确认 `make gates` 能抓到（哪怕这行代码从未被外壳实际引用过）；拆回完成后再 `brickkit up` 重新合并回外壳，
确认两个方向都能干净切换，不需要手工清理残留状态。

#### Task 11：新增合并态断言，落进 `be-acceptance`

- [ ] 新增 `make tier2`（合并态专属断言，`tier0`/`tier1` 判据的延伸，命名沿用既有分层习惯）：至少覆盖
      "单个模块 panic 不拖垮外壳其余模块"（Task 2 已经验证过骨架层面，这里补一条用真实模块触发一次真实
      panic 的回归测试）、"共享连接池下 `SET LOCAL` 越权测试"（故意让一个模块的查询漏加 schema 限定，确认
      读到的是自己 schema 的数据而不是隔壁模块的——铁律二"最难查的一个坑"，值得专门开一条常驻回归断言，
      不能只在拆回门禁失败时才被动发现）
- [ ] `tools/be-acceptance/platform/README.md` 与 `docs/plans/03-...` 的既有清单同一套维护纪律：新增用例
      编号紧接在阶段三的 23 条之后

**验证：** 与阶段二/三新增平台断言的既有判据一致——真的故意改坏代码，确认新断言会红，再改回来确认转绿。

---

## 第 5 部分 · 出档（Task 12）

**🏁 出档检查：**

```bash
make check
make registry-check
make gates
make tier0                          # 十四个组件各自档 0 六项验收（合并态下逐个模块的健康检查/连通性不受影响）
make tier1                          # 平台断言（阶段二 20 条 + 阶段三 3 条）
make tier2                          # 本阶段新增的合并态断言
```

加人工检查：`make arsenal-check` 绿；`git status` 干净；§13.7 拆回门禁手动跑一次确认绿；三份 compose 一条
`make up`/`make down` 确认能干净启停。

### 出档之后要做的事（按阶段三复盘 §5 定下的规矩，不许直接开阶段五）

1. **写阶段四复盘**：`docs/retrospectives/04-阶段四-做外壳验拆回-复盘.md`——同前三份复盘的结构。⚠️
   **本阶段第一次真正验证"合并不磨掉组件性"这条设计书核心承诺**，预期会有比前三个阶段更多"纸面设计和真机
   实现之间的落差"，尤其是共享连接池、按模块 env map 分发、拆回门禁这三处此前完全没有真机验证过的机制。
2. **改设计书回填**：本阶段延续阶段三"边做边回填"的方式（阶段三复盘 §7 第 1 条确认这条方式比阶段二"最后
   统一回填"更不容易遗漏），不再单独留一轮回填任务。
3. **写阶段五计划**：把 `infra-storage`、`infra-attachment`、`infra-dlq-monitor`、`mdm-supplier`、`mdm-org`
   五个 `default` 角色组件补齐（设计书 §9.6.1 档 4a），凑成最小可交付形态。

⚠️ **阶段四预期会验不到的**（写在这里，免得阶段五又忘）：

| 验不到 | 为什么 | 什么时候能验 |
|---|---|---|
| 五外壳的完整分组方案（设计书 §13.2 的最终形态） | 本阶段只有 11 个 Go 组件，够不成"外壳一/二/三"三个 Go 外壳的分组理由——那需要 `erp-purchase`/`crm-*`/`hrm-*` 等阶段五/六才建的组件 | 阶段六铺完军火库之后 |
| 客户成长期"把一个组件从外壳里单独拆出来"（设计书 §13.4 阶段二操作） | 本阶段验证的是"合并"，不是"从合并态里单独摘出一个"——虽然 Task 5 的"部分合并混合形态"间接验证了它的可行性，但没有走一遍设计书 §13.4 列的五个具体操作步骤 | 阶段五或客户真实提出扩容需求时 |
| K8s 全拆下的外壳（本来就不存在这个概念） | 铁律：合并部署只用于 Docker 单机交付，K8s 就是全拆——外壳这个概念在 K8s 目标下根本不出现 | 阶段七 |
