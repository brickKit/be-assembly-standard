# 阶段四附加 · 部署矩阵组合测试记录

> 这份文档只记录"用的是什么组合、用了哪些组件、怎么测的、结果是什么"——**要测什么、为什么这么测**留在 `docs/plans/04b-部署矩阵验证.md`（计划文档），本文档不重复那边的动机说明，只留过程和结果，跟本项目"文档要分文件管理，任务计划只留改法，过程另开专门文档"的既有习惯一致。
>
> 每条记录固定用下面这个模板，缺哪一项都要写清楚"为什么没有"（比如"预期失败，没有代表性沟通测试"是合法的，但要写出来，不能悄悄空着）：
>
> ```
> ## Task N：<名称>
>
> **坐标**：环境=<本地/Docker/Docker+本地混合/K8s> · 拓扑=<纯组件/纯外壳/混搭> · 版本=<单版本/多版本-哪个子场景>
>
> **组件参与清单**：<明确列出用了哪些组件，排除了哪些、为什么排除>
>
> **测试方法**：<真实跑的命令，不是转述>
>
> **代表性沟通测试**：<具体是哪条调用、期望结果、实际结果——如果这条任务预期失败，写清楚"不适用，本任务测的是失败本身">
>
> **结果**：<真机输出摘要、关键断言、耗时>
>
> **结论**：<通过 / 预期失败且验证了失败原因（附报错文案）/ 发现新 bug（附 bug 描述与后续处理）>
> ```

---

（Task 1 开始后，每做完一个任务立刻在这里补一条记录，不攒到最后一起写。）
