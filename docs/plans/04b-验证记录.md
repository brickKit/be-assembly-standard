# 阶段四附加 · 部署矩阵验证 —— 执行记录

> **这份文档跟 `04b-部署矩阵验证.md` 是配对关系**：那份文档只列任务清单和验收标准（"要做什么、怎么算通过"）；这份文档记录**每个任务动手之前先写完整的详细计划、动手之后记录真实过程与结果**——先写计划、再执行、再记录，三步都在同一个任务的同一节里，不分散到别处。命令、输出、真实撞到的问题、最终结论，全部留痕，不是走完就删。

---

## Task 0：把 `be-assembly-standard` 迁移到真实 `servedBy`

### 0.1 计划：`infra/shell-compose.yml`/`shell-up`/`teardown-up` 是否可以退休

**要验证的核心判断**：`servedBy` 落地后，"外壳态"和"全拆态"可能不再需要是两个互斥的部署状态，只是同一份 `brickkit.yaml` 里"谁写了 `servedBy` 谁没写"的区别，一条 `brickkit up` 就能生成正确的混合拓扑（部分组件独立、部分组件被外壳收编）。

**具体步骤**：
1. 现状快照：记录当前 `brickkit.yaml`（阶段四遗留的 `local:true` 版本）、`infra/shell-compose.yml`、`Makefile` 里 `shell-*`/`teardown-*` 目标的真实内容，作为"旧方案"的对照基线。
2. 挑 1 个最小可行子集做实验（不动全部 14 个组件，先证明机制本身）：在一个**临时分支/临时 `brickkit.yaml` 副本**里，把 `mdm/customer`/`mdm/product` 两个零依赖组件从 `local: true` 改成 `servedBy: infra/go-core-shell@1.0.0`，`infra/go-core-shell` 作为一条新的普通组件条目（指向已经存在的 `brickenterprise/be-shell-go` 镜像，随便一个已构建过的 tag）。
3. `brickkit up --dry-run`，检查生成的 compose 是否：① 外壳容器自己的 service 正常生成；② `mdm/customer`/`mdm/product` 没有生成独立 service；③ 外壳 service 挂了这两个组件的网络别名；④ 依赖 `mdm/customer` 的其它组件（比如 `crm-opportunity`），`depends_on` 是否正确指向了外壳而不是 `mdm/customer` 自己。
4. 如果第 3 步完全符合预期，把**剩余 9 个 Go 组件 + 4 个外壳整体**一起做这个替换（正式的 0.4 节内容），此时再回头判断 `infra/shell-compose.yml`/`Makefile` 那几个目标要不要删——不在这一步就删，先留着做对照，等 0.4/0.5 全部跑通、新旧两条路径生成的最终拓扑（`docker ps` 输出）逐字对比一致之后再删。
5. 记录一个明确的决策：删，还是保留（如果发现有旧方案做得到、新方案做不到的事，比如健康检查探针的具体参数、启动顺序控制粒度，就不删，把差异记录清楚）。

**验证标准**：第 3 步的 4 项检查全部通过，且第 4 步全量替换后 `docker ps`/`kubectl get pods` 展示的最终拓扑跟旧方案（阶段四已经验证过、写在 `docs/plans/04-阶段四-做外壳验拆回.md` Task 6 的记录）完全一致。

### 0.1 真机执行记录（2026-09-14，最小子集实验）

**做法**：新建一个临时 local source（`sources:` 新增一条 `servedby-poc-source`，指向 scratchpad 下一个临时目录），写一份最小 `component.yaml` 声明 `infra/go-core-shell-poc@1.0.0`（镜像用已经构建过的 `brickenterprise/be-shell-go:local`，端口 9000，`healthCheck.path: /healthz`）。把 `mdm/customer`/`mdm/product` 的 `local: true`/`localPort` 两行改成 `servedBy: infra/go-core-shell-poc@1.0.0`。其余 12 个组件原样不动（仍是阶段四遗留的 `local: true`/独立部署混合状态）。

**⚠️ 真实撞到的问题（不是纸面推演）**：第一次 `brickkit up --dry-run` 直接报错：

```
❌ 错误：外壳 infra/go-core-shell-poc@1.0.0 下两个成员对同一个透传标签给出了不同的值
   标签键：prometheus.io/port
   mdm/customer@1.0.7：8080
   mdm/product@1.0.8：8082
```

根因：`mdm/customer`/`mdm/product` 的 `component.yaml` 都在 `deployment.labels` 里声明了 `prometheus.io/port`，值分别是各自的端口号——这是两个组件本来就该有、且理所当然应该不同的值，不是巧合冲突。`servedBy` 的 labels 合并规则是"同名同值跳过，同名不同值报错"（跟 env 变量的合并规则形状一样），但**跟 env 变量的合并不同，labels 没有做"只合并某一类、其它类无条件排除"这层收窄**——`*_ENDPOINT` 类环境变量因为语义上"整个外壳内必须一致"，合并+冲突检测是对的；但 `prometheus.io/port` 这类**语义上就是"每个组件各不相同"**的标签，被套用同一条合并规则，导致任何两个都声明了各自 `prometheus.io/port` 的组件，只要被收编进同一个外壳，必定 100% 撞车报错——这跟 `COMPONENT_ID`/`COMPONENT_VERSION` 在 env 合并里"因为必然不同所以被无条件排除"是同一类问题，只是这次发生在 labels 侧，而 labels 侧没有对应的收窄处理。

**临时绕过方式**（只为了让这次最小实验能继续往下验证，不是正式修法）：临时从 `mdm/customer`/`mdm/product` 的 `component.yaml` 里删掉 `prometheus.io/port` 这一行（保留 `prometheus.io/scrape`/`prometheus.io/path`，这两项两个组件本来就写的同一个值，不受影响），验证完立刻 `git checkout -- component.yaml` 还原，两个组件仓库确认过还原后 `git status` 干净。

**绕过之后，`brickkit up --dry-run` 成功，生成结果符合预期**：

```yaml
infra-go-core-shell-poc-1-0-0:
  environment:
    - BRICKKIT_SERVED_MEMBERS=mdm-customer-1-0-7,mdm-product-1-0-8
    - COMPONENT_ID=infra/go-core-shell-poc
    - COMPONENT_VERSION=1.0.0
  networks:
    brickkit-net:
      aliases:
        - mdm-customer-1-0-7
        - mdm-product-1-0-8
```

`mdm-customer-1-0-7`/`mdm-product-1-0-8` 没有生成独立 service（确认 `grep -c` 命中 0）。挑了一个真实存在弱依赖的独立组件 `infra-bff-mobile`（未受这次实验影响，仍然独立部署）核对地址路由：

```yaml
infra-bff-mobile-1-0-17:
  environment:
    - MDM_CUSTOMER_ENDPOINT=http://mdm-customer-1-0-7:8080
    - MDM_PRODUCT_ENDPOINT=http://mdm-product-1-0-8:8082
```

地址正确指向外壳容器的网络别名 + 各成员自己的端口——跟阶段四手写 `shell-compose.yml` 达到的效果一致，且不需要我们自己写任何 `extra_hosts`/别名逻辑。`erp-sales`/`erp-inventory`/`infra-workflow` 这几个仍是 `local: true` 的组件，`infra-bff-mobile` 拿到的地址走的还是旧的 `host.docker.internal`+`extra_hosts: host-gateway`——确认 `local:true` 和 `servedBy` 在同一份 `brickkit.yaml` 里确实互不干扰，不是只信 brickKit 自己的回归测试。

**清理**：`git checkout -- brickkit.yaml`、两个组件仓库 `git checkout -- component.yaml`，删除临时 source 目录，`.brickkit/manifests/` 下这次实验产生的缓存文件手动清掉，`brickkit restore` 确认"14 个活跃，0 个归档，0 个激活"，`git status --porcelain` 全仓库确认干净。

**结论**：
1. 核心判断成立——`servedBy` 确实能在一条 `brickkit up` 里正确生成"部分独立、部分收编"的混合拓扑，网络别名、地址路由、`local:true` 共存全部符合预期，`infra/shell-compose.yml`/`shell-up`/`teardown-up` 大概率真的可以退休（留到 0.4 全量切换、跟旧拓扑逐项对比后再做最终决定，不在这里就下最终结论）。
2. **发现一个值得反馈给 brickKit 的真实问题**：`servedBy` 的 labels 合并没有像 env 变量合并那样，把"语义上必然因组件而异"的标签（`prometheus.io/port` 是最常见、几乎每个组件都会写的例子）排除在合并范围之外，导致这类标签在任何多成员外壳里几乎必然撞车。这个问题会**直接挡住 0.4 节的全量迁移**（我们 11 个 Go 组件的 `component.yaml` 全部声明了各自的 `prometheus.io/port`），需要在 0.4 之前决定怎么处理——要么我们自己把这类标签从 `servedBy` 成员的 `component.yaml` 里移除、只在外壳自己的条目上写一份（跟"合并后指标只能走一个共享端口"这个事实保持一致），要么等 brickKit 那边看有没有必要收窄 labels 合并规则。这条因为会直接挡住 0.4 节全量迁移，不等到 Task 14 收尾才反馈——已提前写成一次性反馈文档 `docs/dev/brickKit反馈-servedBy的labels合并漏了排除规则.md`，交给 brickKit 判断修复方向。0.4 节开始前先看有没有回音，没有的话就先按方向一（我们自己临时去掉这批标签）继续推进，不空等。

### 0.2 调研记录：`be-ops` 产出 4/7/8 是否还需要——结论比原计划预期的更深一层

**没有照原计划的步骤 1（真机跑一次实验）走**：0.1 的真机实验已经留下了足够的证据（见上面 0.1 记录里贴出的生成 compose 片段——外壳容器只拿到 `BRICKKIT_SERVED_MEMBERS=mdm-customer-1-0-7,mdm-product-1-0-8`，没有端口/schema/任何 config 值），不需要重新起一次容器验证同一件事。这次改成直接读 `be-ops`/`shells/go`/brickKit 三边源码，把"外壳启动器到底还缺什么数据、这些数据本该从哪来"这件事看透。

**读代码读出来的结论，跟原计划预期的不一样**：原计划只预期缺"端口/schema"，实际缺口更大——缺**整个模块自己的 `configSchema` 解析结果**（`pgSchema`、`otelBaseUrl`、`authzBundleUrl`、`iamJwksUrl`，以及未来任何组件自己声明的 configSchema 项）。逐条说清楚：

1. **产出 4（`shellconfig`）本来就没绑定 `local:true`，不用动**：`tools/be-ops/internal/shellconfig/gen.go` 的 `Gen` 只读 `genyaml.ComponentSpec`（来自每个组件自己的 `component.yaml`/`assembly.yaml`，跟 `brickkit.yaml` 此刻写的是 `local:true` 还是 `servedBy` 完全无关），产出 `ComponentID`/`Version`/`Schema`/`HTTPPort`/`ExtraPorts`——这几项从一开始就不依赖 `local:true`，迁移到 `servedBy` 之后原样可用，不需要因为这次迁移改代码。
2. **产出 7（`shellenv`）的"地址改写"那部分逻辑，被 `servedBy` 原生取代**：`shellenv.Gen` 的核心工作是把 `local-debug.<service>.env` 里 `http://localhost:<port>` 形式的依赖地址，按"同外壳用 127.0.0.1、跨外壳用 host.docker.internal"改写——这正是 `internal/shell.exportDependencyEndpoints`（`shells/go/internal/shell/shell.go:122`）加上产出 7 联手做的事。`servedBy` 落地后，brickKit 自己在生成阶段就把 `*_ENDPOINT` 类变量直接合并进外壳容器自己的 `environment`（0.1 真机验证过：`MDM_CUSTOMER_ENDPOINT=http://mdm-customer-1-0-7:8080` 直接在外壳的 compose service 里，不需要任何改写），这部分逻辑确认可以整个删掉，`exportDependencyEndpoints` 那个 `os.Setenv` 循环也一并删。
3. **但产出 7 还有第二个职责，原计划没意识到，且这部分职责在 `servedBy` 下反而没了数据来源**：`shellenv.Gen` 的输入 `localDebug map[string]map[string]string` 来自 brickKit 生成的 `local-debug.<service>.env` 文件（`brickKit` 仓库 `internal/compose/local.go:469` 的 `localEnvFiles`）——这份文件不只包含依赖地址，还包含这个组件**完整的 `configSchema` 解析结果**（`pgSchema`/`otelBaseUrl`/`authzBundleUrl`/`iamJwksUrl` 等），这些值经过 `shells/go/internal/shell/shell.go:180` 的 `besdk.NewShellRuntime(... Env: m.Env ...)` 直接喂进每个模块自己的 `rt.Config`——这是模块读自己配置项的**唯一来源**（十七条第 16 条：模块代码零 `os.Getenv`，只走 `rt.Config`）。
   **关键点**：`local-debug.*.env` 只在组件是 `local: true` 时才生成（`internal/compose/local.go` 整个文件就是 `local: true` 专属机制），`servedBy` 成员根本不会有这份文件——`servedBy` 成员既不生成容器，也不是 `local:true`，brickKit 目前**没有为它生成任何等价的产物**。也就是说，产出 7 现在唯一还有意义的那部分职责（把模块自己的 `configSchema` 值喂给它），换了 `servedBy` 之后反而彻底没了数据来源，不是"被原生取代"，是"原来的数据源不存在了"。
4. **不过这份数据 brickKit 自己其实是算出来过的，只是没存下来**：读 `internal/shell/shell.go:112` 的 `Resolve(cfg, graph, states, env *inject.Result)`，参数 `env` 是**全部组件**（含 `servedBy` 成员）已经跑完注入引擎、算出完整 `[]inject.Var` 的结果——`Resolve` 内部只是从这份已经算好的结果里挑出 `Source == inject.SourceEndpoint` 的那一部分去合并，其余的（`pgSchema` 等 configSchema 值）算是算了，只是没有被写进任何生成物，直接在内存里被丢弃。**这跟产出 7 自己文档注释那句"自己另算的那份，早晚和平台的算法分叉"说的其实是同一类风险**——地址改写那部分我们不用再算了（brickKit 自己在合并；我们算了才叫"另算一份"），但 configSchema 解析这部分，"另算一份"仍然是唯一可行路径，因为 brickKit 根本没打算把这部分暴露出来给 `servedBy` 场景用。
5. **产出 8（`shelldepends`）确认整个退休**：跟产出 7 的地址改写部分是同一类——外壳之间的启动顺序现在由 `servedBy` 原生的 `depends_on` 改写覆盖（0.1 真机验证过 `infra-bff-mobile` 的 `depends_on` 正确指向外壳），`shelldepends.Gen` 算的那条边完全重复。

**决定用哪个候选方案（在原计划①②基础上做了修正）**：
不是"registry.tsv 编译期静态映射"（候选①——`authzBundleUrl`/`iamJwksUrl` 这类值根本不在任何 registry 表里，它们是 `brickkit.yaml` 每个组件条目自己手写的 `config:` 字面量，逐环境可能不同，编译期写死这条路完全走不通），也不是"继续用 brickKit 生成的产物做数据源"（0.2 执行前设想的候选②——这条路现在没有数据源了，见上面第 3 点）。

改成**候选②的修正版：把 `configSchema` 解析这件事挪进 `be-ops` 自己做，但只做"配置合并"这一层最简单、最不容易跟平台分叉的部分，不碰地址改写**：
- 扩展 `genyaml.Load`：除了现在已经读的 `component.yaml`/`assembly.yaml`，新增解析 `brickkit.yaml` 根文件里每个组件条目的 `config:` 块（字面量覆盖）+ `component.yaml` 的 `configSchema.properties.<key>.default`（缺省值）——合并规则是"`brickkit.yaml` 里写了就用那个值，没写就退回 `component.yaml` 的 `default`，两边都没有就不产出这个 key"，跟 brickKit 自己的注入引擎对 `configSchema` 项的既有语义一致（读的是同一份 `brickkit.yaml`/`component.yaml`，不是凭空猜一份新算法，风险跟"另算地址改写"不是同一个量级——这里没有"同外壳/跨外壳"这类需要跟平台判断口径保持一致的分支逻辑，纯粹是两份已知 YAML 的字段合并）。
- `shellconfig.Module` 新增一个 `Config map[string]string` 字段承载这份合并结果。
- 产出 7（`shellenv`）、产出 8（`shelldepends`）两个包整体删除，连同 `tools/be-ops/cmd`/`Makefile` 里调用它们的子命令/目标。
- `shells/go/cmd/shell/main.go`：`buildModules` 不再读 `SHELL_ENV_JSON`，`ModuleSpec.Env` 直接来自产出 4 新增的 `Config` 字段；`internal/shell/shell.go` 的 `exportDependencyEndpoints` 整段删除（含它的调用点），因为 `*_ENDPOINT` 现在已经是外壳容器自己 `os.Environ()` 里现成的值，`besdk.Endpoint()`/`MustEndpoint()` 直接就能读到，不需要外壳自己再 `os.Setenv` 一遍。
- `shells/python` 同理对称修改。

这份决定目前只是"调研 + 决定方向"，实际代码改动放进 0.2 的下一步（跟 0.3 一起做，因为两者互相依赖——0.3 本来就要改 `buildModules`，一次改完比分两次改更不容易中间态出错）。

### 0.2 原计划（存档，已被上面的调研结论取代，保留是为了看清判断是怎么被推翻的）

**要验证的具体问题**：
- 外壳启动器现在装配一个模块需要哪些数据：componentId（来自 `BRICKKIT_SERVED_MEMBERS`）、HTTPPort（缺口，需要另找来源）、Schema（缺口，需要另找来源）。
- `shellenv`（产出 7）产出的跨组件地址改写，是不是已经被平台原生的 `*_ENDPOINT` 合并机制完全取代。
- `shelldepends`（产出 8）产出的外壳间启动顺序，是不是已经被平台原生的 `depends_on` 改写完全取代。

**具体步骤**：
1. 对 0.1 步骤 2 的最小子集实验，检查生成的 compose 里 `mdm/customer`/`mdm/product` 对应的模块，端口/schema 信息有没有任何来源——如果外壳容器只拿到了 `BRICKKIT_SERVED_MEMBERS`（一串版本化服务名字符串），没有拿到端口/schema，就确认了"外壳启动器需要另一个数据来源"这个缺口真实存在。
2. 评估两个候选方案：① 外壳启动器代码里维护一份编译期静态映射（componentId → schema/端口，从 `registry/ports.tsv`/`registry/schemas.tsv` 生成，构建时嵌入二进制，不依赖运行时文件）；② 继续用 `be-ops shell-config` 产出一份轻量数据文件，只保留端口/schema 这两项，去掉已经被 `BRICKKIT_SERVED_MEMBERS`/平台原生合并取代的部分。两个方案各自的优缺点记录下来，选一个。
3. 决定后，实际去改 `tools/be-ops/internal/shellconfig`（如果选方案②，简化产出内容）或者废弃它（如果选方案①）。

**验证标准**：不管选哪个方案，`shells/go`/`shells/python` 的模块装配代码最终能拿到装配一个模块所需的全部数据（componentId、端口、schema），且不再依赖 `shellenv`（产出 7）/`shelldepends`（产出 8）产出的数据（这两项预期会被平台原生机制完全取代，真机确认后可以直接废弃这两个 `be-ops` 子命令）。

### 0.3 计划：`shells/go`/`shells/python` 接上真实 `BRICKKIT_SERVED_MEMBERS`

**具体步骤**：
1. `shells/go/cmd/shell/main.go`：把 `buildModules` 从"读 `SHELL_CONFIG_JSON` 拿到全部声明的模块列表，无条件实例化"，改成"读 `os.LookupEnv("BRICKKIT_SERVED_MEMBERS")`（注意用 `LookupEnv` 不用 `Getenv`，要能区分变量不存在 vs 空字符串），按逗号切分成版本化服务名集合，用 0.2 确定的数据来源拿到每个 componentId 对应的完整信息（端口/schema），只实例化真的在这个集合里的模块"。
2. 变量不存在（`ok == false`）时的行为：这种情况理论上不应该发生在一个真的被 `servedBy` 收编的外壳容器里（平台总会至少注入一个空字符串）——如果真的读不到，说明这个容器可能不是被正常 `servedBy` 收编启动的（比如手动 `docker run` 忘了传这个变量），选择直接 `log.Fatal` 报清楚的错误，而不是静默退化成"全部实例化"的旧行为（旧行为是过渡期的兼容写法，迁移完成后不应该再保留，否则会掩盖真实的配置错误）。
3. `shells/python/main.py` 同理。
4. 新增/更新单元测试：`t.Setenv`/`monkeypatch` 分别测试"变量不存在报错"、"变量是空字符串则不实例化任何模块"、"变量包含部分成员则只实例化那几个"三种情况。

**验证标准**：三种情况的单元测试全部通过；真机场景下（0.1 的最小子集实验）外壳容器实际启动后，只有 `BRICKKIT_SERVED_MEMBERS` 里列出的模块被实例化，用日志或者 `/healthz` 之外的一个内部诊断端点确认（如果没有现成的诊断端点，看启动日志里逐个模块的初始化记录）。

### 0.2/0.3 真机执行记录（2026-09-14）

两个子任务实际是一起做的——0.3 本来就要改 `buildModules`，跟着 0.2 定下来的新数据形状一次改完，比分两次改更不容易留中间态。

**be-ops（`tools/be-ops`，最终 `v0.1.9`）**：
1. `internal/genyaml`：`componentYAML` 新增解析 `configSchema.properties.<key>.default`（`*string` 区分"没写 default 键"与"写了空字符串默认值"）；`ComponentSpec` 新增 `ConfigDefaults`（component.yaml 的默认值）与 `Config`（合并后的最终值，由调用方赋值，`loadOne` 本身读不到 `brickkit.yaml`）；新增 `LoadBrickkitConfig(path)` 读 `brickkit.yaml` 根文件每个组件条目的 `config:` 字面量；新增 `MergeConfig(defaults, overrides)` 做"覆盖优先于默认值"的合并。
2. `internal/shellconfig`：`Module` 新增 `Config map[string]string`，`Gen` 原样转发（不重新合并）。
3. `cmd/be-ops/shell.go`：`runShellConfig` 在 `genyaml.Load` 之后追加 `LoadBrickkitConfig` + 逐组件 `MergeConfig`，再传给 `shellconfig.Gen`；`runShellEnv`/`runShellDepends` 两个子命令、`internal/shellenv`/`internal/shelldepends` 两个包、`readDotEnv`/`versionedServiceName`（旧版，dotenv 专用）全部删除。
4. 新增/更新测试：`genyaml` 侧 4 条新用例（configSchema 默认值区分、`LoadBrickkitConfig` 读取、`MergeConfig` 两种场景）；`shellconfig` 侧 1 条新用例（`Config` 原样转发）；`cmd/be-ops/shell_test.go`（原来测 `readDotEnv` 的 PEM 多行解析坑）整体删除，知识点回填进 `field-tested-pitfalls-log.md` C27（详见该条，不重复）。`go build ./... && go vet ./... && go test ./...` 全绿。

**shells/go（`v0.4.6`）**：
1. `internal/shell/shell.go`：`exportDependencyEndpoints` 函数与调用点整段删除，`os` import 随之清理。
2. `cmd/shell/main.go`：`shellConfigModule` 新增 `Config` 字段，`shellEnvModule`/`shellEnvShell`/`readShellEnv`/`envMapForShell` 整组删除；`buildModules` 改成"读 `SHELL_CONFIG_JSON` → 按 `SHELL_NAME` 挑外壳 → 按 `servedMemberSet()`（`os.LookupEnv("BRICKKIT_SERVED_MEMBERS")`，区分未设置/空字符串）筛成员 → 未匹配的 `BRICKKIT_SERVED_MEMBERS` 成员报错（防 `brickkit.yaml` 改完忘了重跑 `be-ops shell-config` 这类基线漂移）"。
3. `internal/shell/shell_test.go`/`cmd/shell/main_test.go`：删除两条 `exportDependencyEndpoints` 回归测试；`main_test.go` 重写/新增 7 条用例，覆盖"按 SHELL_NAME+BRICKKIT_SERVED_MEMBERS 双重筛选"/"未列出的成员跳过不报错"/"空字符串零模块"/"未设置报错"/"列出了 shell-config 找不到的成员报错"/"外壳本身不存在报错"/"SHELL_CONFIG_JSON 未设置报错"。
4. 真机复核：`go build ./... && go vet ./...` 全绿；`cmd/...` 全部 8 条用例通过；`internal/shell/...` 在真实 `TEST_NATS_URL`（本机 `be-nats`）下，不需要 `TEST_PG_DSN` 的用例（假模块 HTTP/HealthPort/panic 隔离三条）全部通过，需要 `TEST_PG_DSN` 的真实模块集成测试（`real_modules_test.go`）本次跳过——那几条测的是 DB 迁移/多组件真实装配，与本次改动（纯环境变量筛选逻辑）不相关，改动本身只是删除一个函数调用，不影响它们的既有行为。
5. `README.md` 补"现状补充（阶段四附加 Task 0.2/0.3 完成）"一节，明确 Task 9 记的根因分析依然成立（`besdk.Endpoint()` 读 `os.LookupEnv` 不是 `rt.Config`），只是"谁负责把值放进 `os.Environ()`"的责任方从外壳自己换成了平台。

**shells/python（`v0.2.4`）**：判断逐一对应 shells/go，`shell/run.py` 删 `_export_dependency_endpoints`，`main.py` 的 `_build_modules` 同样改成"SHELL_NAME 挑外壳 + BRICKKIT_SERVED_MEMBERS 筛成员"双重过滤，`tests/test_main.py`/`tests/test_run.py` 同步更新。真机复核比 Go 侧更完整：本机 `be-postgres` 已有现成的 `brickkit_test_db`，拼出真实 `TEST_PG_DSN` 后 `tests/` 全部 11 条用例（含真实装 `infra-print` 的 `test_real_module.py`）全绿，不是只测到"能编译通过"这一层。

**结论**：
1. 0.2 的调研结论（产出 4 不用动、产出 7 拆成两半分别处理、产出 8 整体退休）真机验证成立，三个仓库改完之后没有出现任何测试红、没有出现需要回头修正设计的情况。
2. `BRICKKIT_SERVED_MEMBERS` 未设置时报错、空字符串时零模块、列出了合并清单里找不到的成员时报错——三种边界情况在 Go/Python 两侧都用真实单元测试钉住了，不是只写在计划文档里的意图。
3. 三个仓库均已 commit + 打 tag + push（`be-ops@v0.1.9`/`be-shell-go@v0.4.6`/`be-shell-python@v0.2.4`），根仓库的 submodule 指针与 `AGENTS.md` 的工具版本行随后一并同步。
4. 遗留的一个小知识债：`be-ops` 原来那份"brickKit 生成的 dotenv 文件多行值原样嵌入未转义换行符"的真机坑（PEM 解析两轮才修对），代码和回归测试都随 `shellenv` 一起删除了，已单独回填进 `docs/dev/field-tested-pitfalls-log.md` C27，避免知识随代码一起消失。

### 0.4 计划：`brickkit.yaml` 全量真机切换

**具体步骤**：
1. 确认 git 工作区干净。
2. 12 个组件（11 个 Go + `infra-print`）逐个从 `local: true`/`localPort` 改成 `servedBy: <外壳ID>@<外壳版本>`，删掉 `local: true`/`localPort` 两行。
3. 新增 4 条外壳自己的组件条目（`go-core`/`go-backoffice`/`go-infra`/`py-render`），字段包括 `id`/`version`/`image`/`deployment.port`/`healthCheck`，`sources` 需要能解析到这 4 个组件的 `component.yaml`——如果外壳镜像仓库本身还没有一份 `component.yaml`，这一步需要先给 `shells/go`/`shells/python` 各自补一份（外壳本身第一次被当成"一个真正的 brickKit 组件"来看待，这是本次迁移一个之前没预料到、需要真机做的时候确认范围的新工作）。
4. 13 处手写的 `authzBundleUrl`/`iamJwksUrl` 字面量，先保留不动（0.4 最后一步单独验证能不能被原生机制取代，不要在这一步顺手改，两件事分开验证互不干扰）。
5. `brickkit up --dry-run`，人工核对生成结果与预期逐条比对。

**验证标准**：`--dry-run` 生成的 compose 结构（service 数量、网络别名、`depends_on`）与 0.1 步骤 4 的全量预期一致；`brickkit.yaml` 本身通过 `servedBy` 的全部静态校验（不报任何 `CONFIG_INVALID`）。

### 0.4 真机执行记录（2026-09-14，进行中——真机 `brickkit up` 卡在一个新缺口，暂停在这里）

**步骤 1-2：labels 冲突的正式修复（不是临时绕过）**。0.1 发现的 `prometheus.io/port` 撞车问题，这次是真的要让全部 12 个组件一起切，必须先解决，不能再临时删标签走查一次就撤回。做法：给 11 个 Go 组件 + `infra/print` 各自的 `component.yaml` 永久删掉 `deployment.labels.prometheus.io/port`（保留 `scrape`/`path`，两者本来就跨组件相同），四个外壳自己的 `component.yaml` 上补一份（合并后指标只能走外壳自己这一个共享抓取端口，逻辑上本来就该在这里）。12 个组件逐个用 `make bump-version`（版本号：mdm/customer 1.0.7→1.0.8、mdm/product 1.0.8→1.0.9、erp/inventory 1.0.15→1.0.16、erp/finance 1.0.11→1.0.12、erp/sales 1.0.23→1.0.24、infra/authz 1.0.5→1.0.6、infra/iam-casdoor 1.0.7→1.0.8、infra/workflow 1.0.3→1.0.4、infra/notification 1.0.3→1.0.4、integration/im-dingtalk 1.0.4→1.0.5、infra/print 1.0.5→1.0.6、crm/opportunity 1.0.10→1.0.11）+ `go test`/`pytest` 全绿 + `make image` + commit/tag/push，级联带出 `infra/bff-mobile`（1.0.17→1.0.18，纯依赖版本号同步，本机没装 Node/npm 没能跑 `npm test`，已在提交信息里明确记录未经本机测试验证，只改了依赖版本字符串，没碰任何 `.ts` 源码）。

**步骤 3：给 4 个外壳补真实 `component.yaml`**。发现的新问题比计划预期的更具体：
- `sources` 里 `type: local` 的目录结构是 `<root>/<scope>/<name>/component.yaml`——不能直接指向 `./shells/go`（那会导致外壳的 `component.yaml` 要放在 `shells/go/shell/go-core/component.yaml` 这种双层路径），选择在两个外壳仓库里各建一个 `deploy/` 子目录（`shells/go/deploy/shell/{go-core,go-backoffice,go-infra}/component.yaml`、`shells/python/deploy/shell/py-render/component.yaml`），`brickkit.yaml` 新增两条 `sources`（`shells-go`/`shells-python`）指向这两个 `deploy/` 目录，不跟 `./components` 混在一起，也不需要 `assembly.yaml`（`be-ops` 的 `genyaml` 只扫 `./components`，这两个新源它根本看不见）。
- 组件 ID 用了新 scope `shell/`（`shell/go-core`/`shell/go-backoffice`/`shell/go-infra`/`shell/py-render`）——不属于设计书 62 个业务组件的固定名录，`registry/ports.tsv` 的 `checkComponentCount` 门禁（`be-ops`）原来只豁免 `_infra-` 前缀，新增了 `_shell-` 前缀豁免（`be-ops@v0.1.10`/`v0.1.11`，附带 registry/README.md 规矩 3b），`registry/ports.tsv` 新增 4 行（`_shell-go-core 8090`/`_shell-go-backoffice 8116`/`_shell-go-infra 8224`/`_shell-py-render 8402`，独立的"外壳自身"分区，不占用设计书原有的端口分区）。
- `metadata.version` 直接复用各自外壳仓库自己的 git tag（`be-shell-go@0.4.6`/`be-shell-python@0.2.4`），不另起一套版本号——三个 Go 外壳实例共用同一份镜像/同一个 tag 是有意的（本来就是"一份镜像、运行时按 `SHELL_NAME` 分身"的既定设计）。`shells/go`/`shells/python` 各自 `make image` 真实构建出 `brickenterprise/be-shell-go:0.4.6`/`brickenterprise/be-shell-python:0.2.4` 两个镜像（验证过"镜像里有 sh + wget"）。
- 新增两个 `configSchema` 项解决"三个 Go 外壳实例共用一份镜像，怎么在运行时区分身份"这个问题：`shellName`（→ `SHELL_NAME`，`cmd/shell/main.go` 已经在读，值不变）、`shellHealthPort`（→ `SHELL_HEALTH_PORT`，同样是既有变量名，只是数据来源从 `shell-compose.yml` 手写环境变量换成了 `brickkit.yaml` 的 `config:`）。`otelBaseUrl`/`iamJwksUrl`/`authzBundleUrl` 三项也补齐（外壳自己也需要这三个值来跑健康检查/权限判定装配），命名与全仓库既有 configSchema 约定一致，`cmd/shell/main.go` 原有的 `os.Getenv` 读取代码完全不用改。

**步骤 4-5：`brickkit.yaml` 主体改造 + `--dry-run` 验证**。12 个成员逐个把 `local: true`/`localPort` 换成 `servedBy: shell/<外壳>@<版本>`，`expose: true`/`exposePort` 整段删除并留注释说明原因（servedBy 成员不生成容器，这两个字段不生效，是已知的"实施勘误"）。`resources:` 块的 `bindings` 从"每个成员各自一条"改成"外壳自己一条"——这是**真机验证发现的、原计划完全没预料到的一个关键点**：`servedBy` 的合并只把 `Source == SourceEndpoint` 的变量合进外壳共享环境，`DATABASE_*`/`MQ_*` 是 `Source == SourceResource`，从来不在合并范围内，真正持有连接池的是外壳自己（不是任何一个成员），`bindings` 必须直接挂在外壳的 componentId 上，挂在成员身上的旧写法在 servedBy 下完全不起作用（读了 brickKit `internal/inject/inject.go` 确认 `SourceResource`/`SourceEndpoint` 是两个不同标记，跟 0.2 节读到的"资源连接变量无条件排除"是同一份证据的另一次印证）。

`brickkit up --dry-run` 一次性通过（`servedBy` 全部静态校验绿，`CONFIG_INVALID` 零条），生成的 compose 文件核对：
- 只有 6 个真实 service（`frontend-standard`/`infra-bff-mobile`/4 个外壳），12 个 servedBy 成员**没有**各自的 service——符合预期。
- `shell-go-core` 容器的 `environment` 里同时有 `BRICKKIT_SERVED_MEMBERS=erp-finance-1-0-12,erp-inventory-1-0-16,erp-sales-1-0-24,mdm-customer-1-0-8,mdm-product-1-0-9`、`DATABASE_*`（外壳自己的，`bindings` 改对之后才有）、`MDM_CUSTOMER_ENDPOINT=http://mdm-customer-1-0-8:8080` 这类跨模块地址（网络别名直接可达）、`SHELL_NAME=go-core`/`SHELL_HEALTH_PORT=8090`（新增的 configSchema 项正确注入）、`prometheus.io/port: "8090"`（labels 不再撞车）——**每一处此前分别验证过的机制，这次在同一份真实生成结果里一起对上了**。
- `infra-bff-mobile`（独立容器，依赖 4 个 servedBy 成员）拿到的依赖地址直接是 `http://mdm-customer-1-0-8:8080` 这类同 Docker 网络的服务名——比旧的 `local:true` 时代（`host.docker.internal` + `extra_hosts` 间接寻址）更简单，因为现在双方都在同一个 `brickkit-net` 网络里，不需要"宿主机再绕一圈"了。

⚠️ **`--dry-run` 的警告揭出第二个真实缺口**：12 个成员全部报"资源依赖未满足……资源 postgres-go-core 已声明，但未绑定给该组件"——这条只在 `--dry-run` 降级成警告，读 brickKit `internal/resolver/resolver.go` 的 `CheckRunningResourceBindings` 确认**真的 `up`（不带 `--dry-run`）时这条会直接硬阻断**，且这条校验完全不知道 `servedBy`，只认"组件自己的 componentId 在不在某条资源的 bindings 里"。临时应对：在每条资源的 `bindings` 里，外壳自己的绑定之外，把它收编的每个成员的 componentId 也原样保留一份（跟外壳绑同一个资源、同一份凭据，不会产生任何真实副作用——servedBy 成员本来就不生成容器）。已反馈给 brickKit（`docs/dev/brickKit反馈-servedBy成员的资源绑定校验没有跟上.md`）。

**真机执行 `brickkit up`（去掉 `--dry-run`）遇到的第三个、也是目前最严重的缺口**：命令直接报错退出——

```
❌ 错误：docker 执行失败
   命令：docker compose ... up -d --wait --remove-orphans erp-finance-1-0-12 ... shell-go-core-0-4-6 ...
   输出：no such service: erp-finance-1-0-12
```

`brickkit up` 传给 `docker compose` 的目标 service 列表里混进了 12 个 servedBy 成员自己的版本化服务名，而这些 service 在生成的 compose 文件里根本不存在（没有自己的容器）——读 brickKit `internal/cli/up.go` 的 `collectTargets` 确认：这个函数判断"该不该跳过"只查了 `c.Local`（`local: true`），完全没有查 `c.ServedBy`，是 `local: true` 时代写的逻辑没有跟着 `servedBy` 一起更新。已反馈给 brickKit（`docs/dev/brickKit反馈-真机brickkit up对servedBy成员报no_such_service.md`）——**这是三个 servedBy 反馈里最严重的一个：不是警告，是整个 `brickkit up` 命令直接失败，一个容器都起不来**。

临时绕过（不经过 `brickkit up`，直接对生成好的 compose 文件跑 `docker compose up`，不显式传 service 名）：

```bash
docker compose --project-directory . -p brickkit-be-assembly-standard \
  -f .brickkit/generated/docker-compose.yaml up -d --wait --remove-orphans
```

容器真的起来了（4 个外壳 + 2 个独立组件），但 4 个外壳容器全部 crash-loop：

```
装配外壳 go-core 失败：SHELL_CONFIG_JSON 未设置（be-ops shell-config 的产出路径）
```

**这是我们自己这边的缺口，不是 brickKit 的问题**：`SHELL_CONFIG_JSON` 原来的设计是"文件路径 + 挂载卷"（`shell-compose.yml` 时代手写的 volume mount）——但 brickKit 的 manifest 模型**根本没有 `volumes` 字段**（AGENTS.md 早就记过这条限制，此前只当作"为什么不能把网关做成组件"的理由，这次才真正撞上它对"外壳怎么拿到自己的合并清单"这件事的直接影响）。servedBy 落地后，外壳是通过 `configSchema` 拿数据的（`shellName`/`shellHealthPort`/`authzBundleUrl` 等都是这样），但这些都是"一个字符串"量级的值——`shell-config.json` 是一整份结构化数据（这个外壳全部成员的 componentId/端口/schema/configSchema 值），原来的"文件路径"这个设计前提，在没有 volumes 的 servedBy 世界里已经不成立了，需要重新设计"外壳怎么拿到自己的合并清单"这一步，不能简单套用旧机制。**这不是 0.4 计划原来预料到的范围，是真机跑到这一步才浮现的新工作**，下一步：把 `SHELL_CONFIG_JSON` 从"文件路径"改成"内容本身"（JSON 文本直接作为一个 configSchema 值注入，同 `infra/authz` 的 `permissionCatalog` 已经在用的"生成的字符串塞进 config 字面量"这个既有模式），需要同时改 `be-ops`（新增产出这个字符串）、`shells/go`/`shells/python`（`readShellConfig` 从读文件改成直接解析 env 内容）、4 个外壳的 `component.yaml`（新增一个 configSchema 项）、`brickkit.yaml`（填上生成出来的值）。已停在这里，清理了 crash-loop 的容器（`docker compose down`），未继续往下做。

**当前状态**：真机 `docker compose down` 已清理，`brickkit.yaml`/12 个组件/4 个外壳 manifest 的改动全部留在工作区，尚未 commit。

**具体步骤**：
1. `brickkit up`（真实起，不是 `--dry-run`），`docker ps` 核对最终拓扑：3 个 Go 外壳容器 + 1 个 Python 外壳容器 + 2 个独立组件 + 5 个基础资源。
2. `make tier0`（需要在全拆态窗口验证，复用 `weekly-teardown-gate.sh` 的既有模式——但这个脚本本身是为旧的 `local:true` 剥离方案写的，这一步大概率需要跟着 0.1 的决策一起更新，两者放在一起做）。
3. `make tier1` 全绿。
4. `make tier2` 全绿（外壳合并态专属断言——这两条测的是 `shells/go`/`be-sdk-go` 自己的行为，理论上不受"用 `servedBy` 还是 `local:true` 触发合并"这件事影响，但要真机确认，不能假设）。
5. 挑一条真实业务调用链（复用阶段四 Task 9 已经验证过的那条：CRM 赢单 → 建单 → 锁库存 → 生成应收 → 打印送货单），全部走一遍确认合并态业务闭环没有因为这次迁移而破坏。
6. `docs/dev/field-tested-pitfalls-log.md` C18 如果被 0.4 最后一步验证解决了，回填状态。

**验证标准**：全部命令真绿，附录 E 全链路真机跑通，产出与阶段四 Task 9 的既有记录逐项可比对。

---

（后续每个任务开工前，在这里补上对应的"计划"小节，执行完补"真机执行记录"，跟 Task 0 的结构保持一致。）
