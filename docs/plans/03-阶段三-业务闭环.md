# 阶段三 · 业务闭环

> **前置：** 先读 [`00-总纲.md`](./00-总纲.md)（全局约束 A–K、SOP-W/B/D/L/R/P）、
> [`01-阶段一`](./01-阶段一-地基与第一块砖.md) 与 [`02-阶段二`](./02-阶段二-验平台.md) 的出档结论、
> [`docs/retrospectives/02-阶段二-验平台-复盘.md`](../retrospectives/02-阶段二-验平台-复盘.md)（尤其 §4：跨组件调用的
> vendored-contract 模式、版本+tag 纪律、真故障注入测试的判据，本阶段全部要用到）。
>
> **本文档写在九份组件设计计划之前**（总纲 §5.1 的流程：出档 → 复盘 → 改设计书 → **写阶段三计划**（本文档）
> → 写该阶段涉及组件的设计计划 → 开工）。九份设计计划随开工逐个补齐，不是写完本文档就已经有了。

**这一阶段做什么：** 对应设计书 §9.6.1 的**档 2**。加 9 个组件（`infra-iam-casdoor`、`infra-authz`、`infra-workflow`、
`infra-notification`、`integration-im-dingtalk`、`infra-print`、`infra-bff-mobile`、`frontend-standard`、`crm-opportunity`），
凑成设计书 §5.13「切片」列（= 附录 H 打勾）的 **14 个**。同时引入两种新语言，`be-sdk-python`、`be-sdk-ts` 要先建起来。

**这一档目标不只是"业务链能跑通"，还有一件同等重要的事：权限判定要从阶段二的 fail-closed stub 换成真实的
`infra-authz`。** 阶段二五个组件的接口全标 `Public` 是刻意的临时状态（阶段二计划 Task 1 原文），本阶段要回去换成
真实权限键——否则"业务闭环"跑通的其实是一条权限全放行的闭环，验不到设计书第 14 章的任何承诺。

**要跑通的链路**（设计书附录 E，本阶段在其基础上延伸）：

```
CRM 赢单 → 建单 → 锁库存 → 生成应收 → 审批 → 钉钉通知 → 打印送货单 PDF
```

⚠️ **`infra-print` 选 Python、`infra-bff-mobile`/`frontend-standard` 选 TS，理由不是业务，是阶段四要靠它们验
Python 外壳与"跨语言合不进外壳"这两件事**（设计书 §9.6.1）。业务功能仍然只做到"能把这条链压出来"为止，
不追求完整——不做多级审批、不做通知模板管理界面、不做打印模板设计器。

**出档条件：** 附录 E 延伸链路真机跑通一遍 + Saga 补偿与超时查询各走一遍 + DLQ 进出各一遍 + 阶段二五个组件的
权限判定从 stub 切到真实 bundle、真机验证过 403/200 两条路径都对。

---

## 本阶段的 15 个任务

| # | 任务 | 产出 |
|---|---|---|
| **第 0 部分 · 两个新 SDK 与门禁扩展** | | |
| 1 | `be-sdk-python` 建仓库 → **CP-7** → 实现 | 与 `be-sdk-go` 公开 API 一一对应的 Python 版 |
| 2 | `be-sdk-ts` 建仓库 → **CP-8** → 实现 | 只有一半能力（无 `withTx`、无冷热路由，多 GraphQL 深度限制） |
| 3 | `make gates` 扩展到 Python/TS；两张表从"空表+校验"变"真实产出" | `registry/permissions.tsv`、`registry/data-scopes.tsv` 由 `be-ops` 聚合全部已发布组件的真实声明 |
| **第 1 部分 · 权限判定真正上线** | | |
| 4 | `infra-authz` 建仓库 → **CP-9** → 契约 + 实现 | `GET /authz/bundle`（ETag/304）+ 角色/分配 + `stale_since` 有界列表 |
| 5 | 三个 `be-sdk-*` 补 bundle 轮询 + 内存 map + `RequirePermission` 真实判定 | `besdk.RequirePermission` 从 stub 换成进程内 map 查找；`ScopeFilter` 中间件真的下场 |
| 6 | 阶段二五个组件回填真实权限键 + 数据权限 `.sql` 谓词真正启用 | `Public` → 真实权限键；`erp-sales`/`erp-inventory`/`erp-finance` 的 `data_scopes` 从声明变成真正生效的过滤 |
| **第 2 部分 · infra 基建四块** | | |
| 7 | `infra-iam-casdoor` 建仓库 → **CP-10** → 实现 | Casdoor 官方镜像（带外容器）+ 薄适配层；`/api/tenant/features` |
| 8 | `infra-workflow` 建仓库 → **CP-11** → 实现 | 审批待办登记/分发、异常任务，零业务规则、零直查业务库 |
| 9 | `infra-notification` + `integration-im-dingtalk` 建仓库 → **CP-12/CP-13** → 实现 | 通知意图路由 + 第一个真实 channel adapter |
| 10 | `infra-print` 建仓库 → **CP-14** → 实现 | 第一个 Python 组件；模板渲染、PDF/ZPL |
| **第 3 部分 · TS 与前端** | | |
| 11 | `infra-bff-mobile` 建仓库 → **CP-15** → 实现 | 第一个 TS 组件；GraphQL BFF，DataLoader 防 N+1 |
| 12 | `frontend-standard` 建仓库 → **CP-16** → 实现 | Vue3 + Uni-app，只做本阶段这几个模块的页面 |
| **第 4 部分 · 链上一环与全链路** | | |
| 13 | `crm-opportunity` 建仓库 → **CP-17** → 契约 + 实现 | 商机跟进/漏斗，依赖 `mdm-customer`/`mdm-product`，发布 `crm.opportunity.won.v1` 等三个事件 |
| 14 | 14 个组件一起 `brickkit up`，跑通延伸链路 | 附录 E 延伸版真机验证；Saga 补偿、超时查询、DLQ 进出各真实触发一次 |
| **第 5 部分 · 新增平台断言** | | |
| 15 | 本阶段新增的平台表面积落进 `be-acceptance/platform` | Python 120s 冷启动宽限、TS 组件健康检查、DLQ SDK 级往返——常驻资产追加，不是另起一套 |
| **第 6 部分 · 出档** | | 复盘 → 改设计书 → 写阶段四计划 |

**十一个人工检查点**（到了就停下，等人建好仓库再继续，话术见总纲 §3.5 步骤 B）：

| 检查点 | 在 | 要建的仓库 |
|---|---|---|
| **CP-7** | Task 1 | `be-sdk-python` |
| **CP-8** | Task 2 | `be-sdk-ts` |
| **CP-9** | Task 4 | `infra-authz` |
| **CP-10** | Task 7 | `infra-iam-casdoor` |
| **CP-11** | Task 8 | `infra-workflow` |
| **CP-12** | Task 9 | `infra-notification` |
| **CP-13** | Task 9 | `integration-im-dingtalk` |
| **CP-14** | Task 10 | `infra-print` |
| **CP-15** | Task 11 | `infra-bff-mobile` |
| **CP-16** | Task 12 | `frontend-standard` |
| **CP-17** | Task 13 | `crm-opportunity` |

---

## 九个组件 + 两个 SDK 的顺序，和为什么是这个顺序

设计书 §4.2 同步图里，本阶段新增组件的出边是这样的：

```
infra-iam-casdoor  → （无出边，日常认证走浏览器直连官方镜像，见 §6.1）
infra-authz        → （无出边，被拉取 bundle 不算依赖边，见 §14.1.4）
infra-workflow     → （无出边，铁律：严禁反向同步调用业务组件）
infra-notification → （无出边，铁律：只发不查）
infra-print        → （无出边，纯渲染引擎）
infra-bff-mobile   → 调用后端组件的 batchGet（读，非侵入业务边界）
frontend-standard  → 只认网关，不进同步图
crm-opportunity    → mdm-customer, mdm-product
```

**七块零出边或近零出边，一块（`crm-opportunity`）有真实强依赖。** 顺序原则与阶段二相同：先把地基类的立起来，
最后用有依赖边的那一块把全链路串起来。但本阶段比阶段二多一层——**权限判定本身是这一档的地基**，所以顺序上
还要满足"`infra-authz` 必须早于阶段二五个组件回填真实权限键"这一条：

| 顺序 | 块 | 为什么排这个位置 |
|---|---|---|
| 1 | `be-sdk-python`、`be-sdk-ts` | 两个新语言的组件都要用它们；`be-sdk-python` 必须晚于已经被真组件用过一轮的 `be-sdk-go`（总纲 §3.5.1：反过来做两边一定分叉） |
| 2 | `infra-authz` | 本阶段真正的地基。阶段二五个组件的路由要"回填真实权限键"，必须先有真实的 bundle 可拉；`ScopeFilter` 中间件要下场，必须先有真实数据权限判定源 |
| 3 | 阶段二五个组件回填真实权限键（Task 6） | **在新组件铺开之前先把旧账还掉**——这正是阶段二 Task 1"现在还账改 1 个组件，拖到阶段三还要改 13 个"那条判据的翻版：本阶段自己的九个新组件从第一天起就该用真实权限键写，不该重复"先标 Public 再回填"这一步 |
| 4 | `infra-iam-casdoor`、`infra-workflow`、`infra-notification`、`integration-im-dingtalk`、`infra-print` | 五个零出边的基建砖，谁先谁后互不影响，按 Python 组件（`infra-print`）需要 `be-sdk-python` 已经就绪这一条硬顺序排在 SDK 之后即可 |
| 5 | `infra-bff-mobile`、`frontend-standard` | 两个 TS 组件，`frontend-standard` 要通过 `be-sdk-ts` 生成的 API client 与 `infra-bff-mobile` 的 GraphQL 端点联调，所以排在基建砖之后 |
| 6 | `crm-opportunity` | 唯一的链上一环，且是**触发**这条链的起点（"CRM 赢单"），必须等 `mdm-customer`/`mdm-product`（已有）与所有下游基建砖都真实存在，才能把全链路盘一遍 |

⚠️ **`crm-opportunity` 不对 `erp-sales` 建同步边。** 两者之间是**事件边**（`crm.opportunity.won.v1`），设计书
§1.4 铁律"CRM 与 ERP 零同步边，仅事件握手"——阶段二复盘 §2.3 已经论证过跨组件同步调用要用 vendored-contract
模式，但那是给"确实需要同步调用"的边界用的；`crm-opportunity` 完全不需要同步调用 `erp-sales`，**顺手加一条比
阶段二任何一次都更容易犯错**，因为这次两个域名字上完全不像会有关系，反而更容易被忽略这条铁律。

---

## 第 0 部分 · 两个新 SDK 与门禁扩展（Task 1–3）

#### Task 1：`be-sdk-python` 建仓库 → CP-7 → 实现 ✅（骨架部分）

**与 `be-sdk-go` 的关系不是"照着抄一遍"，是"逐个能力对应着搬"**（总纲 §3.5.1、决策 100）：两份 SDK 的公开 API
要同名、同参数顺序、同语义，否则同一个人在两种语言间切换要重新学一遍，AI 也会把 Go 的写法直接套过来。

- [x] `Config`（**从第一个提交起就实现对 camelCase→SCREAMING_SNAKE_CASE 转换**，含幂等性回归测试）、
      `Endpoint()`（剥 `http://` 前缀，二值返回，导读第 1 条）、`WithTx`（`SET LOCAL ROLE`，铁律二）——三者是
      "结构三件套"，真实实现，九门禁 + 19 条测试全绿，`with_tx` 真机连了本机 `be-postgres` 验证
- [x] `RunStandalone`/`NewGinEngine` 等价物（`run_standalone`/`new_fastapi_app`）、`Module`、`Runtime`：真实实现，
      对齐 be-sdk-go **首次提交**的范围（不是 Task 1 全部完成的范围，见下）
- [x] OTel 初始化与 Blackhole 降级、日志（结构化 JSON + 自动 PII 脱敏）、指标 registry、Outbox 推送：Task 10
      开工前发现 `run_standalone`/`bootstrap` 的实际调用链会立刻炸在这四个仍是 `NotImplementedError` 的骨架上
      （`infra-print` 是第一个真正走到这条调用链的 Python 组件，之前没人踩到），已用真实 Postgres/NATS 测试
      补全实现（`be-sdk-python@v0.3.0`），四份文档见 `otel.py`/`logging.py`/`metrics.py`/`outbox.py`。
      **`besdk.consume` 仍未补**——本阶段没有 Python 组件需要消费事件（`infra-print` 零消费），留给下一个需要
      它的 Python 组件
- [x] ⚠️ **`RequirePermission`/`ScopeOf`/`UserClient`/`SystemClient` 与本条原计划有一处刻意偏离，记录在案**：
      原计划要求"本阶段第一次实现就带真实判定，不像阶段二 `be-sdk-go` 先立 stub 形状"。实现时判断这样做代价更高
      ——`be-sdk-go` 自己在 Task 1 开工时**仍然是** fail-closed stub（真实 bundle 轮询是 Task 5 才与三个 `be-sdk-*`
      一起补），若 `be-sdk-python` 单独先做真实判定，会造成"Python 版比 Go/TS 版先进"的临时不对称，且要为一个
      当时还没有真实服务器可对照测试的契约设计并锁死实现。**改为按 be-sdk-go 当前形状写**（签名真实、判定 stub），
      Task 5 时三者一起换成真实 bundle 轮询。理由写在 `be-sdk-python` 的 `authz.py` 模块文档里，不是漏读任务清单
- [x] `configEnvVarName` 等价物：与 `be-sdk-go` 逐字对应的算法移植，回归测试覆盖 camelCase 与已是
      SCREAMING_SNAKE_CASE 两种输入

**验证（已完成部分）：** 九门禁全绿；19 条测试全部真机跑过（不是 mock）。过程中发现一个真实的平台级差异
——**`asyncpg.Pool` 会在 `release()` 时自动 `RESET ALL`，Go 的 `database/sql` 不会**——这让最初设计的"看下一个
借用者是否读到脏值"这种回归测试方法在 asyncpg 下测不出 `SET` 与 `SET LOCAL` 的区别（故意改坏代码复现过，测试
仍然绿），改成直接 spy 执行的语句本身才真正锁死。已记入 `docs/dev/实测踩坑记录.md` A6。

**验证（尚未做，随 OTel/Outbox/events 骨架填平时一起补）：** 用一个最小假组件真机 `brickkit up` 一次，走完
Bootstrap→健康检查→`/healthz` 探测全流程。

#### Task 2：`be-sdk-ts` 建仓库 → CP-8 → 实现 ✅（骨架部分）

**只有一半能力**（总纲 §5.10）：无 `withTx`（TS 组件不直连 DB，`infra-bff-mobile` 严禁直连 DB 是设计书 §6.5
的铁律）、无冷热路由（同理）、多一条 GraphQL 深度限制（`max_depth: 5`、`max_complexity: 100`、
`persisted_queries_only: true`，§11.4.3）。

- [x] `Config`（camelCase 转换）、`Endpoint()`、`Runtime`/`Module`、`runStandalone`：真实实现，九门禁 +
      18 条测试全绿（真装依赖、真 `tsc --noEmit`、真 `vitest run`）
- [x] `newGraphQLServer`（`NewGinEngine`/`new_fastapi_app` 的 GraphQL 版）：真实实现，Yoga + graphql-armor
      深度/复杂度限制 + Persisted Operations 三者接线完成
- [x] DataLoader 封装（`createBatchGetLoader`）：真实实现，工厂函数形态强制"必须 per-request"这条纪律
- [ ] OTel、健康检查以外的日志/指标：仍是签名 + 抛异常的骨架，对齐 be-sdk-go/python 首次提交的做法，
      留到 Task 11（`infra-bff-mobile` 真正需要）之前用 TDD 填平
- [x] ⚠️ `RequirePermission` 的 TS 版与本条原计划同样有一处刻意偏离（与 Task 1 的 `be-sdk-python` 一致）：
      按 be-sdk-go 当前 fail-closed stub 形状写，Task 5 三个 `be-sdk-*` 一起换成真实判定，理由记在
      `be-sdk-ts` 的 `authz.ts` 模块文档里

**验证（已完成部分）：** 九门禁全绿；18 条测试全部真机跑过。GraphQL 深度限制那条回归测试按计划写了
"故意发一个 depth=6 的查询，断言被拒绝"，**且做了故意改坏配置验证测试真的会红**——过程中发现两处真实的
Yoga/graphql-armor 行为（默认的 `maskedErrors` 会把权限错误与深度校验错误都掩盖成通用 "Unexpected error."，
两者走的不是同一条掩盖路径），已记入 `be-sdk-ts` 的 `README.md`/`authz.ts`，测试断言据此调整为断言"确实被拒、
无数据返回"这个可观察事实，不断言消息文本。另发现 `prom-client` 已被官方标记 deprecated，换成
`@prometheus-io/client`。

**验证（尚未做，随 OTel/日志/指标骨架填平时一起补）：** 最小假组件真机 `brickkit up` 一次。

#### Task 3：`make gates` 扩展 + 两张表转正 ✅ 已完成

阶段一/二 `make gates` 的三条扫描（铁律六 import、`SystemClient` 误用、裸 `gin` 路由）目前只扫 Go 代码。本阶段
第一次出现 Python/TS 组件，扫描器要跟上：

- [x] `SystemClient` 误用扫描的 Python/TS 版：判据不变（只许出现在 `Start()`/事件 handler，不许出现在请求路径），
      语法层面按各自语言的 AST 或正则适配。**实际实现**：Go 保留 `go/ast`；Python/TS 没有对应语法树可用，
      退化成逐行正则（`system_client(`/`systemClient(`），已知精度上限（不理解字符串字面量）记在源码注释里，
      靠"这两个目录本来就是约定死的、不该有别的东西"这条硬约定兜底
- [x] 裸路由扫描的 TS 版：`infra-bff-mobile` 的 GraphQL resolver 层没有"裸 `gin.GET`"这个概念，判据要改成
      "resolver 是否经过权限键包装"——**这条不是照抄 Go 版能过的，要重新设计判据**，写清楚"GraphQL 场景下权限键
      强制长什么样"。**实际实现**：`BareGinScan` 改名 `BareRouteScan`（CLI 子命令 `bare-gin-scan` → `bare-route-scan`）；
      Python 版判据只认装饰器形态（`@router.get(...)`）与 `add_api_route` 直调，不认任意 `.get(...)`——
      `dict.get()`/配置读取里的 `.get(` 极常见，纯文本匹配会把这些全部误判；TS 版判据是"resolver 字段的值
      是否以 `requirePermission(` 开头"，不是就再看它是否长得像函数（箭头函数/`function` 关键字），是则违规
- [x] **新拍板并写回文档的两个目录约定**（此前全仓库都没有文档化，写这两条扫描时才发现空白）：Python 组件的
      `backend/app/http`、`backend/app/grpc`（对应 Go 的 `backend/internal/http`/`grpc`，写进总纲 SOP-B）；
      TS GraphQL BFF 的 resolver map 一律放 `src/resolvers/`（`docs/design/infra-bff-mobile.md` 待决问题 #1
      就此了结）
- [x] `registry/permissions.tsv`/`registry/data-scopes.tsv` 从阶段一"空表 + 校验"转正：`be-ops` 产出 9/10 开始
      真的扫描全部已发布组件的 `assembly.yaml`，聚合出这两张表的真实内容——这是这两张"只增不改"的表第一次真正
      被写入数据。**实际实现**：新增 `internal/authzreg` 包；`permissions.tsv` 只增不改（新 key 追加、已有 key
      允许刷新 title/type/owner_component、`deprecated` 列本命令从不自动设置也不清空、孤儿 key 打警告但不删行、
      同一 key 被两个组件声明直接报错）；`data-scopes.tsv` 纯派生每次全量重生成；省略 `data_scopes` 段当场报错
      （用 `*dataScopesField` 指针类型区分"完全省略"与"标量 none"）
- [x] **新增 `make version-check`**（阶段二复盘 §4 第 1 条留的决定，这里拍板：加）：扫描全部 submodule，
      检查是否存在"HEAD 领先最新 tag（有已提交的改动）"的情况——阶段二真的因为这条纪律单靠约定而漂移过三个
      仓库（`mdm-customer`/`mdm-product`/`erp-inventory` 升级 `be-sdk-go` 依赖后忘了打新 tag），机械化检查比
      指望人记住更可靠。加进 `make gates` 或单独一个目标均可，判据：任何 submodule 的 `git describe --tags`
      结果带 `-N-g<hash>` 后缀就报警（N>0）。**实际实现**：独立 `make version-check` 目标（`infra/scripts/version-check.sh`），
      **第一次真跑就抓到两条真实漂移**：`be-sdk-python`/`be-sdk-ts` 建仓库后忘了打 `v0.1.0`（连一个 tag 都没有），
      当场补上

**验证：** 故意在一个 Python/TS 假组件里写违规代码，两条新扫描必须能抓到——**实测**：真的在 `components/` 下建
了一对假组件（`_fixture-fake/pyfake`、`_fixture-fake/tsfake`），`make gates` 在真实 CLI 层面（不只是 Go 单测）
抓到全部 4 条注入的违规，删掉假组件后恢复全绿；`be-ops permissions`/`data-scopes` 命令跑一次，两张表出现阶段
二五个组件的真实权限键（21 条）/数据权限声明（4 条）——本阶段新组件（`infra-print`/`infra-bff-mobile` 等）要到
Task 8 起才会真的建仓库，届时重跑会自动并入；故意让一个 submodule 提交一笔改动但不打 tag，`make version-check`
必须报警，打上 tag 后必须转绿——**实测**：在 `mdm-customer` 里造了一笔真实漂移确认报红，`reset --hard` 撤销后
确认转绿；`permissions.tsv` 的"只增不改"也做了真实验证：临时从 `mdm-customer` 的 `assembly.yaml` 删掉一个已发布
权限键重新生成，确认该键仍保留在表里、只是多了一条警告，而不是被静默删除。

---

## 第 1 部分 · 权限判定真正上线（Task 4–6）

> **这三个任务是本阶段真正的门槛。** 业务链路能不能跑通，很大程度上是"把阶段二证明过的模式再做九遍"；
> 权限判定从 stub 换成真实实现，是这一档**独有**的、没有先例可抄的部分。

#### Task 4：`infra-authz` 建仓库 → CP-9 → 契约 + 实现 ✅ 已完成

- [x] 契约（HTTP，不是 gRPC——`GET /authz/bundle` 是给 `be-sdk` 轮询用的，不是组件间同步调用）：
      `GET /authz/bundle`（`ETag` 支持，未变化返回 `304`）、角色管理 CRUD、角色-权限分配、用户-角色分配。
      **实际实现**多做了两件设计文档没细化的事：给人单独加一个权限（`POST /api/admin/users/{sub}/permissions`，
      走专属角色 `u:<sub>`）、阶段三极简部门表的 CRUD + 用户分配（`ResolveClaims` 的 `dept_path` 来源）；
      gRPC 面（`BatchGetRoles`/`ResolveClaims`/`GetBundle`）与 REST 共用同一个 service 层
- [x] `stale_since` 有界列表机制（§14.1.6）：人的角色变更要在 ~15 秒内生效，不能等 JWT 刷新（TTL 级）——
      这条不是"锦上添花"，是设计书明确点出的"日常操作时延分配是反的"那个问题的解法，**没有它这个组件就不算做完**。
      **实际实现**：窗口 = 2× `accessTokenTtlSeconds`，来源是 `role_changes`（管理员显式的 granted/revoked/kicked）
      与 `user_roles.expires_at`（自然到期，直接从落在窗口内的行推导，不需要任何后台扫描任务或事件）的 UNION；
      角色内容变更（勾/取消权限键）刻意不触碰这条机制，靠 bundle 的 `roles` 字段直接刷新
- [x] 数据模型：**这是本组件第一次真正持有状态**（角色表、角色-权限分配表、用户-角色分配表）——与阶段二三个
      枢纽"零表零迁移"的既有印象不同，`infra-authz` 恰恰是"权限体系里唯一需要自己持久化状态"的组件
- [x] 降级行为（§14.1.9）：authz 不可达时业务组件怎么办——**绝不能让它的可达性影响 `/healthz`**（导读第 23 条、
      §12.3.6），要走"沿用内存里上一次拉到的 bundle，直到能重新拉到"这条路，不是"判定失败就全部拒绝"或
      "判定失败就全部放行"。**范围澄清**：本组件自己的 `/healthz`（`besdk.NewGinEngine` 统一提供）从不查依赖，
      这一半在 Task 4 里已验证；"沿用旧 bundle 继续跑"这条 fail-static **轮询缓存逻辑**是调用方（`be-sdk`）的
      职责，真正落地在 Task 5

**实际发现并拍板的三个此前文档没有先例的设计点**（写进 `docs/design/infra-authz.md`/组件 README）：
`config.permissionCatalog` 的编码格式（`,`分隔条目、`|`分隔字段）；自举机制（迁移种结构、`Start()` 读
`bootstrapAdminSub` 幂等授权，解决"第一个管理员怎么来"）；本组件的管理 API 自吃自己下发的 bundle
（`authzBundleUrl` 指向自己）。

**真实验证（不是只跑通单元测试）**：24 条 repo/service 测试全绿（对"专属角色必须出现在 bundle 里"这条最高
优先级断言做过故意改坏代码验证测试真的会红）；8 个 Makefile 门禁全绿，Docker 镜像确认带 sh+wget；真机端到端
冒烟——起真实服务器连真实 Postgres+NATS，REST 全链路（建角色→勾权限→授权→查我的权限→踢人）与 gRPC
（`ResolveClaims`/`GetBundle`）都真的跑通，ETag 条件请求真的返回 304。过程中发现一个真实 bug：pgx 的
`database/sql` 通用接口不会自动把 PostgreSQL `text[]` 解成 Go `[]string`（记入踩坑记录）。

**顺带发现并修复一个与本任务无关但影响诊断准确性的真实 bug**：`git submodule add` 挂载本组件后核对
`git submodule status`，发现 `tools/be-sdk-go` 显示成"落后最新 tag 3 个提交"的假象——根因是它的 v0.1.8/v0.1.9
当初打成了轻量 tag，而 `git submodule status` 内部的 `git describe` 不带 `--tags`、只认带注解的 tag。
已改打成带注解 tag 修复（`components/erp/finance` 的同类问题一并修），并给 `make version-check` 补一条判据
防止同类漂移再次不被发现（记入踩坑记录类别 F）。

**顺带发现并修复一个严重得多的真实 bug——`brickkit up` 真机跑六个组件时发现的部署漂移**：`mdm-customer`/
`mdm-product`/`erp-inventory`/`erp-sales` 四个组件的 `component.yaml` 里 `deployment.image` 的 tag 一直落后
`metadata.version` 一整个版本（如 `metadata.version: 1.0.1` 但 `deployment.image` 还写着 `:1.0.0`）——对比
`docker inspect` 的镜像创建时间与对应版本的 git 提交时间，确认镜像**从未在那次版本升级后重新构建过**，
`brickkit up` 部署的容器名字对得上新版本、里面跑的却是旧二进制。根因：`check-version` 门禁从写出来那天起
就只比对 git tag 与 `metadata.version`，从没检查过 `deployment.image` 是否也跟着走，纯靠人记住手动同步。
**修复**：给全部 6 个组件的 `check-version` 补一条判据（`deployment.image` 的 tag 必须与 `metadata.version`
逐字一致），对四个已经漂移的组件各做一次纯重新构建（不改业务逻辑，只升版本号 + 重新 `docker build`）——
`mdm-customer@1.0.2`、`mdm-product@1.0.2`、`erp-inventory@1.0.4`、`erp-sales@1.0.4`（后者额外因为依赖版本号
同步漏了一条 `erp/finance@1.0.2` 又追加一次修复，`erp-finance` 自己也因为门禁加固单独升到 `1.0.2`）。
修复后真的 `brickkit down` + `docker compose down --remove-orphans` + `brickkit up` 完整重跑一遍，确认 6 个
组件全部 healthy 且镜像 tag 与声明版本一致，`make tier0` 六项全部转绿（顺手发现并修了 `tier0_test.go` 自己
硬编码在 `mdm-customer:1.0.0` 的容器名/镜像名——同一类"版本升级了，配套的东西没跟上"，只是这次是测试
脚本自己）。记入踩坑记录类别 C（C5）。

**参考实现（SOP-R）：** OPA（CNCF 毕业项目）的标准部署形态就是这个模式（策略下发 + 本地决策），bundle 端点的
设计直接照抄它的思路；**不要看 Zanzibar/SpiceDB/OpenFGA 这类集中式 PDP**——设计书 §14.1.4 已经论证过它们的
p95<10ms 是"几万台机器+专门团队"换来的，对"客户本地一台电脑"的形态不成立，读了也用不上。

#### Task 5：三个 `be-sdk-*` 补真实判定 ✅

- [x] `RequirePermission`/`require_permission`/`requirePermission` 从"fail-closed stub，Public 放行其余一律
      403"换成"查内存 map，查不到才 403"——三份 SDK 判定链逐字对应（设计书 §14.1.6 第 3 步、§14.1.9）：
      PUBLIC 不验签 → 没配 `iamJwksUrl` 时保持阶段二 fail-closed 403 → JWT 本地验签失败 401 →
      `jwt.iat < stale_since[sub]` 401 `token_stale` → AUTHENTICATED 放行不查权限键 → bundle 从没连上过 503
      （不是 403，语义更准）→ 具体权限键查不到 403
- [x] bundle 轮询后台循环（15 秒条件 GET，ETag 未变化不重新解析，单次失败 fail-static 沿用旧内容）——
      这个循环归 `be-sdk` 提供，业务代码看不见（§14.1.4："组件里没有任何一张权限表"这条继续成立，变的只是
      map 从"永远空"变成"真的有内容"）。Go 用 goroutine+ctx 取消，Python 用 `asyncio.create_task`（随事件循环
      结束而结束，不需要显式 Stop），TS 用自我重排的 `setTimeout` 链条（同理不需要显式 Stop）
- [x] `ScopeFilter`/`ScopeOf`/`scope_of`/`scopeOf` 真的下场：从"恒不限"变成按 JWT 的 `dept_path`/`sub` 算出
      `all`/`prefix`/`exact`/`owner` 四个字段（§14.2.3 的纯函数写法，§14.2.4）。⚠️ **三份 SDK 各自独立发现并
      纠正的同一处方向性 bug**：取不到 Claims 时不能返回零值 `ScopeFilter{}`——§14.2.4 的 SQL 约定"空字符串
      表示不限"，零值会被下游解读成放行一切，是 fail-**open** 方向反了；改成 Go `panic`/Python
      `raise RuntimeError`/TS `throw`，让编程错误在联调阶段就现形，不是安静地多返回几行数据
- [x] **Task 4 实现 `infra-authz` 时发现的真实缺口，本任务顺带补上**：三个 `be-sdk-*` 都加了新哨兵值
      `Authenticated`/`AUTHENTICATED`——验签 JWT 通过、不 stale 就放行，不查 bundle map，`GET /api/me/permissions`
      这类"任何登录用户都该能查"的端点现在有了正确的表达方式（`infra-authz` 此前用的临时 debug header 可以
      正式换掉，留给 Task 6/7 落地）
- [x] **TS 版的两处必要差异**（GraphQL 没有路由，只有字段；resolver 的 `context` 参数取代隐式的
      `context.Context`/contextvar）：`requirePermission` 的判据从"路由注册函数强制要求权限键参数"平移成
      "每个字段 resolver 必须经过包装"；`requirePermission` 验签成功后把 `ScopeFilter` 挂到 `context` 对象上
      （`Symbol` 键避免撞名），`scopeOf(context)` 从同一个对象读回——同一次请求内 GraphQL 执行引擎把同一个
      `context` 实例传给每层 resolver，效果等价于 Go/Python 的隐式上下文。401/403/503 通过
      `createGraphQLError` 的 `extensions.http.status`/`extensions.http.headers` 真的映射成 HTTP 响应状态码/
      响应头（真机核对过 `graphql-yoga` 的 `getResponseInitByRespectingErrors` 源码）

**真实验证**（三份 SDK 各自的测试细节见各自 README"现状"节，不在这里重复）：JWT 验签全部用自建 RSA
密钥对 + 真实绑定端口的本地 HTTP 服务器当 JWKS 端点（`infra-iam-casdoor` 要到 Task 7 才建，暂无真实签发方），
覆盖合法签名、签名篡改、`alg:none` 攻击、缺少必填 claim；bundle 轮询覆盖 ETag/304、fail-static、以及一条
**真等 15 秒**（不 mock 时钟）验证"改角色分配不重启组件也能生效"的测试；`RequirePermission` 全状态机每条
分支都有断言，关键的时间比较方向/503 与 403 分支顺序/fail-open-fail-closed 方向全部做过故意改坏代码验证
测试真的会红；三份 SDK 都起了本地 `infra-authz` 容器做真机对接测试，直接打真实的 `GET /authz/bundle`，
确认认得出自举种子数据 `authz_admin`/`infra.authz.admin`。**顺带发现一个测试设计坑**（TS 版）：签名篡改测试
最初翻转 Base64URL 编码签名段的最后一个字符，因编码填充位的原因是假阳性，记入踩坑记录 A8。

`be-sdk-go@v0.2.0`、`be-sdk-python@v0.2.0`、`be-sdk-ts@v0.2.0` 均已打注解 tag 并推送，装配仓库的 submodule
指针已同步更新。

#### Task 6：阶段二五个组件回填真实权限键 + 数据权限真正启用 ✅

- [x] `mdm-customer`/`mdm-product`/`erp-inventory`/`erp-finance`/`erp-sales`（连同 `infra-authz` 自己）：
      `besdk.GET/POST/PATCH(..., perm, ...)` 的 `perm` 参数从 `besdk.Public` 换成 `assembly.yaml` 里已经声明过的
      真实权限键。`infra-authz` 额外把 `/api/me/permissions` 换成 `besdk.Authenticated`、`/api/admin/**` 换成
      `infra.authz.admin`，并把 Task 4 遗留的临时 `X-Authz-Debug-Sub` header 正式退役，改用
      `besdk.ScopeOf(ctx).Owner` 取已验签的真实 `sub`
- [x] `erp-sales`（`org`+`owner`）/`erp-inventory`（`warehouse`）/`erp-finance`（`legal_entity`）的 `data_scopes`
      从"写了但判定恒真"变成真实生效的参数化 SQL 谓词（§14.2.4），读写两条路径都受约束（只保护读接口、放过
      写接口等于给"看不到就该动不了"这条判据留后门）：
      - `erp-sales`：`dept_id`/`dept_path`/`owner_id` 在 `CreateOrder` 时从 `besdk.ScopeOf(ctx)` 取真实值落成
        创建时快照；`ListOrders` 新增 `?view=mine|dept` 两个视图；`GetOrder`/`ConfirmOrder`/`CancelOrder`/
        `ShipOrder` 都校验订单在调用者的 org/owner 范围内（OR 不是 AND）
      - `erp-inventory`/`erp-finance`：**Task 6 讨论中发现的真实缺口**——`warehouse`/`legal_entity` 两维的
        "谁能访问哪个"分配数据，全项目任何地方都没有天然来源（不像 `org`/`owner` 直接读 JWT），也不该塞进
        JWT（那是业务概念，会把功能权限/数据权限的分界搞模糊）。拍板：分配数据归数据的宿主组件自己维护——
        两个组件各自新增一张分配表（`warehouse_access`/`legal_entity_access`）+ 一组管理接口（新增权限键
        `erp.inventory.manage_access`/`erp.finance.manage_access`）
- [x] `make gates` 的 `SystemClient` 扫描继续守，本任务未新增违规（0 条，`make gates` 已确认）

**真实验证**（各组件测试细节见各自 commit message，不在这里重复）：新增/扩展的测试覆盖 grant/revoke 幂等、
读写两条路径的越权拒绝、List 端点的 SQL 级过滤（含"没有任何授权=一条都看不见"的 fail-closed 方向），关键
分支全部做过故意改坏代码验证测试真的会红。真机 `brickkit up` 六个组件 + `make tier0` 全绿：确认
`authzBundleUrl` 真的能拉到 `infra-authz` 的真实 bundle 数据，`make gates` 裸路由扫描 0 违规。

⚠️ **诚实的范围边界**：`infra-iam-casdoor`（真实 JWT 签发方）要到 Task 7 才建仓库，本任务完成时全系统没有
任何调用方能拿到一个真的验签通过的 JWT——`iamJwksUrl` 配置项对全部 6 个组件都留空，`RequirePermission`
现在统一退化成"没配 iamJwksUrl"的 fail-closed stub（真机 curl 确认非 Public/Authenticated 路由现在都是
403，`/healthz` 与 `infra-authz` 自己的 `/authz/bundle` 不受影响）。本任务原计划"用两个不同角色的真实
JWT 打接口"这条端到端验证，因此改在 SDK/component 测试层面完成（`besdk.ContextWithClaims` 构造真实
Claims + 真实 DB），部署层面的真实 JWT 全链路验证留给 Task 7 之后补——这不是遗漏，是这两个任务的既定
先后顺序的直接后果。发现并修复一个连带的真实缺口：5 个组件的 `component.yaml` 的 `configSchema` 从 Stage 2
起就只声明了 `iamJwksUrl`、漏了 `authzBundleUrl`，`brickkit up` 会打印"配置项不会生效"的警告——已在全部
5 个组件补上这一项 schema 声明；`tools/be-acceptance` 的 `tier0_test.go` 又一次因为硬编码的
`mdm-customer` 容器/镜像名版本号漂移而失败（第二次遇到同一类坑，接受这是手动维护成本，不做成动态解析），
且其中一条断言（创建/查询客户期望 200）因为路由现在是真实权限键而需要相应改成期望 403。

---

## 第 2 部分 · infra 基建四块（Task 7–10）

> **通则同阶段二：每块砖照现有模板走 SOP-B，本计划只写与已有组件不同的地方。**

#### Task 7：`infra-iam-casdoor` 建仓库 → CP-10 → 实现 ✅

- [x] Casdoor 官方镜像作为带外容器（不进 `brickkit.yaml`）+ Go 薄适配层组件（这一层才是 `slot:iam`，§6.1）；
      两个 token 架构落地：Casdoor 只证明身份，应用 token（`roles[]`/`dept_path`/`org_id`）由本组件用自己的
      RSA 私钥签，登录/刷新各现问一次 `infra-authz` 的 `ResolveClaims`，不缓存
- [x] `/api/tenant/features`：数据来源是 `be-ops` 生成 `brickkit.yaml` 时写进 `config.enabledComponents` 的
      逗号分隔字符串，适配层读 `ENABLED_COMPONENTS` 环境变量原样下发
- [x] Webhook 事件桥接（共享密钥 header 严格校验 + payload 宽容解析）、首次部署初始化（建 Casdoor 组织/应用/
      webhook，每次启动对账，单步幂等）
- [x] ⚠️ **日常登录流不经过这一层**：浏览器直接走 OIDC 标准协议与 Casdoor 官方镜像通信，适配层只在"认证成功
      之后"接手（POST /api/iam/token 换应用 token 那一步）

**真实验证**（真实 Postgres + 真实 Casdoor 容器 + 真实 `infra-authz` 容器，非 mock）：`keys`/`tokens` 两个
基础包（RFC 7638 官方 kid 推导示例、签名篡改/typ 混淆/密钥轮换窗口）、`casdoor` 包（管理 API 全套+OIDC 发现
+JWKS 验签）、`repo` 包（refresh token 签发/轮换/重放检测/连坐撤销、webhook 去重与"publish 失败整体回滚"）、
`bootstrap` 包（三步幂等可重跑）单元/集成测试全绿；`service` 包端到端把 `ExchangeToken → RefreshToken →
Logout` 全链路对着真实 `infra-authz` gRPC 走了一遍。**真机 `brickkit up` 七个组件 + curl 全链路冒烟**：
真实 Casdoor 用户走密码授权拿到 id_token → 换出应用 token → 用它打 `mdm-customer` 的真实业务路由拿到
`403 无权限`（不是 401，证明"验签+查权限键"这条真判定链完整跑通，而不是"没配 iamJwksUrl"的 fail-closed
stub）→ 刷新/重放检测/登出全部行为符合预期；Prometheus 指标与访问日志确认状态码真实（见下方 be-sdk-go
bug）。`make tier0` 全绿（档 0_2 断言口径同步更新为 401，见下）。

⚠️ **真机验收过程中发现并修复的真实问题**（均已 fix + 回归测试 + deliberate break 验证过，详见
`docs/dev/实测踩坑记录.md` 类别 C 续三/续四）：
1. 带外 Casdoor 容器的 `origin` 配置（`infra/casdoor/conf/app.conf`）漏了端口，OIDC 发现文档的
   `issuer`/`jwks_uri` 因此指向一个连不通的地址——已修配置文件本身
2. `webhook_deliveries` 按 `received_at` 分区导致组合唯一约束对真实重投形同虚设，去重从未真正生效——改成
   显式 `SELECT EXISTS`
3. `webhook_deliveries.payload` 建成 `JSONB` 会让不合法 JSON 的投递体 `INSERT` 直接失败，与"宽容存储"
   设计意图矛盾——改成 `TEXT`
4. OIDC 发现文档给出的 `jwks_uri` 用的是 Casdoor 全局 `origin`，从本组件容器内部连不通——新增
   `rebaseHost`，只保留发现文档给出的路径，host 强制换成 `casdoorBaseUrl` 自己的
5. **`be-sdk-go` 自身的 bug**（影响全项目）：`NewGinEngine` 的中间件注册顺序导致 `recoveryAndErrorMapping`
   把 `c.Error(...)` 翻译出的真实状态码写回去之前，RED 指标与访问日志已经读完了 Gin 的默认状态码（200）——
   真实响应没问题，只有可观测性数据把所有走 `c.Error` 路径的失败错记成 200。已在 `be-sdk-go v0.2.3` 修复
   （重排中间件顺序），本组件与 `be-sdk-go` 自身已升级，其余六个已建组件仍留在 v0.2.1，留给各自下次改动时
   顺带升级（同项目里版本漂移的既有容忍度）

⚠️ **敏感配置的处理**：`casdoorAdminPassword`/`webhookSharedSecret`/`appTokenSigningKeyPem`（RSA 私钥）是
本项目第一次需要 `brickkit.yaml` 级别的真正敏感配置。核实过 brickKit 本身支持 `${ENV_VAR}` 展开（对整份
YAML 文档递归生效，不只是 `resources` 段——`internal/config/parse.go` 的 `expandEnvNode`），所以三项都写成
`${VAR}` 引用，真实值放在 `.env`（已 gitignore），`brickkit.yaml` 本身不落地任何秘密——不需要绕过整机部署
验收。**这条结论第一次是判断错的**（浅层 grep 没搜到就以为平台不支持），用户事后要求"以防万一"复核，读完
`internal/config/parse.go`/`internal/cli/up_secrets.go` 才发现 brickKit 为 `components[].config` 专门配了
一整套 `${VAR}` 展开 + 明文密钥告警机制，比最初以为的支持得更完整；完整经过见
`docs/dev/实测踩坑记录.md` 类别 G（G1）。

#### Task 8：`infra-workflow` 建仓库 → CP-11 → 实现

**三条铁律**（设计书 §6.6）：**严禁包含业务规则判断、严禁直接查询业务数据库、严禁反向同步调用业务组件**——
这三条不是软约束，是这个组件存在的设计前提，`make gates` 应该有能力抓其中至少一条（反向同步调用可以用铁律六
的 import/依赖边扫描抓住；业务规则判断不易机器抓，靠 code review）。

- [x] 接收业务组件注册的审批任务/异常处理任务，维护全局待办列表，处理审批动作，记录审批历史，发布事件——
      `infra-workflow@1.0.0` 已建仓库、实现、真机部署验证（见下方状态说明）
- [x] **异常任务的交互模式**：业务规则校验失败时不暴露"重试"按钮，而是创建异常待办，通知用户修正数据后重新
      提交——这是阶段二 `erp-sales` 的 TCC 补偿链"连续失败 3 次 → 标 `SUSPENDED` + 告警"最终要对接的地方
      （阶段二设计计划 §4.4.4 待决，本阶段落地）。`erp-sales@1.0.6` 已接通：`compensateReserve` 补偿连续
      失败 3 次真调 `CreateTask` 建异常待办，消费 `task.completed.v1`（`action: APPROVED`）把订单从
      `SUSPENDED` 恢复回 `DRAFT`——本组件从不调 `CloseTask`，异常待办由人在 `infra-workflow` 的"我的待办"
      UI 里点"同意"来关闭，`erp-sales` 只是这条事件的消费者，见 `docs/design/erp-sales.md` §3.1/§4.4.4
- [x] 审批人路由：本阶段没有 `mdm-org`（阶段五才建），审批人来源用最简单的形态过（配置项或角色）——**不要为了
      "看起来完整"提前实现组织树路由**，那是 §14.2.3 明确留给 `mdm-org` 上线后接管的事。`infra-workflow`
      完全不做路由判断，`assignee_sub`/`assignee_dept_path` 100% 由调用方算好传入；`erp-sales` 侧新增
      `exceptionAssigneeSub` 配置项（留空 = 跳过建待办只打日志，同弱依赖缺失的判据）

**当前状态**：`infra-workflow@1.0.0` 与 `erp-sales@1.0.6` 均已出档，Task 8 全部完成。组件本身（`repo`/
`service`/`http`/`grpc`/`module` 全部落地，claim-first 幂等、状态流转、数据权限两维 OR/AdminView、超期扫描）
已真机验证——`brickkit up` 与另外 7 个组件一起跑通，`/healthz`/`/metrics` 200，REST 无 token 正确 401，
**真实 gRPC 调用** `CreateTask → GetTaskStatus(PENDING) → CloseTask → GetTaskStatus(RESOLVED)` 全链路走通，
`event_outbox` 里 `task.created.v1`/`task.completed.v1` 两条都已发布到 NATS（`published_at` 非空，不是只写了
库）。`erp-sales` 一侧：真拨号 `infra-workflow` 验证补偿连续失败 3 次真建异常待办（`assignee_sub`/来源四元组/
`type: EXCEPTION` 全部核对）、真发布真订阅验证收到 `task.completed.v1` 后订单真的从 `SUSPENDED` 恢复回
`DRAFT`、三层过滤（`source_component`/`source_aggregate`/`action`）确认不会误恢复别的记录。全部两个组件
共 12+5（`infra-workflow`）+ 3+2+2（`erp-sales` 新增）条测试真跑 PostgreSQL/NATS 通过（`-race`），两边门禁
全绿，`brickkit up` 八个组件（含 `erp-sales@1.0.7`）一起真机部署 healthy。

✅ **真机测试过程中意外发现的严重问题已修复**（`docs/dev/实测踩坑记录.md` C11）：`erp-inventory` 的
`Receive`/`Adjust`/`GetBalance`/`ListMovements` 四个 REST-only 方法暴露在 gRPC 面且没有防呆，被直接 gRPC
调用时 `besdk.ScopeOf` 会 panic，而 `be-sdk-go` 的 gRPC server 没有 panic-recovery 拦截器，会直接崩溃整个
容器进程（真机复现，`RestartCount` 从 0 涨到 3）。`be-sdk-go@v0.2.4` 已给 `serveExtraPort` 加
`grpcRecoveryInterceptor`（同 HTTP 侧 `recoveryAndErrorMappingMiddleware` 的判据），`erp-inventory@1.0.6`/
`erp-sales@1.0.7` 已升级并真机重新验证：同一条曾经崩容器的调用现在返回干净的 `codes.Internal`，
`RestartCount` 保持 0。✅ **`erp-sales` 侧的遗留问题也已一并修复**：`receiveRealStock`/`getRealBalance`
两个 helper 改成直接对 `erp-inventory` 真实 Postgres schema 读写（复用 `besdk.WithTx`，同它自己 `Receive`
一样的 SQL），不再经过那两个从来就不该被 gRPC 调用的方法；顺带修了另一处同类问题（`tcc.CreateOrder`
同样调 `besdk.ScopeOf(ctx)`，这几条测试直接调 orchestrator 层导致 ctx 里没有 Claims，用
`besdk.ContextWithClaims` 解决）。`erp-sales` 全部既有 e2e 测试（5 条）+ Task 8 新增测试（2 条）共 9 条
真机全部通过，详见踩坑记录 C11。

**验证：** `erp-sales` 的一笔订单补偿连续失败 3 次，真的在 `infra-workflow` 里出现一条异常待办；处理这条待办后
`erp-sales` 收到对应事件、订单状态跟着变。✅ 已验证（见上方状态说明）。

#### Task 9：`infra-notification` + `integration-im-dingtalk` 建仓库 → CP-12/CP-13 → 实现

两者放一个任务里写，因为 `notification` 是路由中枢、`dingtalk` 是它的第一个真实 channel adapter，脱离对方
都测不出真实行为。

- [x] `infra-notification`：接收业务组件的"通知意图"（不是"给张三发条钉钉"，是"告诉张三他的审批任务来了"），
      根据用户的通道偏好并发路由给下层已装配的 `channel_adapter`——**通道选择的判定权在 `notification`，不在
      发起通知的业务组件**。两层偏好模型（全局天花板 + 分类子集）+ critical 类别不可关闭（`workflow_task`）
      已实现并真机验证
- [x] `integration-im-dingtalk`：只监听事件 + 调钉钉官方 API + 发回调事件，**严禁包含业务逻辑**（§6.9 对全部
      `integration-*` 适配器的统一要求）。`channel:im` 族第一个成员，族级契约（proto 包名/事件 subject）已定稿

⚠️ **建仓库时的范围收窄，随后已补齐**：建仓库时没有可用的钉钉沙盒/测试企业，用户明确选择"跳过钉钉真机验证，
只做到能编译通过的程度"——这是本计划原文"真调钉钉的开放平台 API（沙盒/测试群），不是 mock"这条要求的一次
**授权收窄**，不是静默降级。`integration-im-dingtalk` 的钉钉客户端（`gettoken`/`getbymobile`/
`asyncsend_v2`/`getsendresult`）当时按公开文档写、用 `httptest.Server` 模拟接口形状验证过内部逻辑（请求
构造、响应解析、错误分类、token 缓存、重试状态机），但从未用真实钉钉企业凭据验证过"钉钉真的这样回"。
**2026-09-09 当天用户免费建了一个钉钉个人团队 + 企业内部应用，把真实 Client ID/Secret/AgentId 给了过来**——
这条范围收窄在同一天就被用户主动补齐，不是留到未来某个不确定的时间点。

**当前状态**：`infra-notification@1.0.0` 与 `integration-im-dingtalk@1.0.0` 均已出档。两个组件的
`repo`/`service`/`http`/`grpc`/`consumer`/`module` 全部落地，真机跑通 9 个门禁（除 `smoke`，见
`brickkit up --dry-run`）：`check-version`/`test`/`image`/`migrate-idempotent`/`dag-check`/
`contract-check`/`import-scan`/`module-check`/`docs-check` 全绿。真实 Postgres + 真实 NATS 测试覆盖：
`infra-notification` 两层偏好解析（含 critical 兜底、"分类不能突破全局"、停用用户覆盖 critical 兜底）、
`SetPreferences` 整份替换语义、通知状态机 `PENDING→ACCEPTED→SENT/RETRYING→FAILED_PERMANENT`；
`integration-im-dingtalk` token 缓存的"未过期不重复调"与"强制刷新一定真的调"两条语义分开验证、
`target_adapters` 族级自过滤、`ACCEPTED→CONFIRMED` 完整链路（含延迟回查退避轮次）。

✅ **实现过程中发现并修复的两个真实 bug**（不是设计阶段发现的，是写测试/走查时踩出来的）：① `infra-notification`
重试重发 `dispatch.im.v1` 时最初一直用 `Event.Version: 1`，而这条 subject 的 `aggregate_id` 固定是
`record_id`，`event_inbox` 按 `(subject, aggregate_id)` 只放行版本严格递增的事件——重试沿用旧 version 会被
`integration-im-dingtalk` 的 `event_inbox` 当重复消息静默吞掉，症状是 `retry_count` 涨了对方却什么都没收到，
且没有任何报错。改成 `Version = attempt`（`attempt = retry_count + 1`），`integration.im.result.v1` 那边
同步定了 `version = attempt*10 + phase_ordinal` 的算法保证多阶段结果事件也严格递增。② `integration-im-dingtalk`
的 `tokenmgr.Refresh`（供 `POST /admin/token/refresh` 应急强制刷新用）最初在拿到锁后又重新检查了一次缓存
有效性，导致如果缓存恰好没过期，"强制刷新"会直接跳过调用——管理员点了刷新按钮，实际什么都没做。修正为
"重新检查"只属于 `EnsureValid`（面向日常发送场景的单飞优化），`Refresh` 永远无条件调用。

✅ **真机端到端验证（占位凭据阶段，`brickkit up` 十个组件一起跑）**：真实 `grpcurl` 调
`infra-workflow.CreateTask` → 真实 NATS 事件 `infra.workflow.task.created.v1` → `infra-notification` 真实
消费并按偏好建 `notification_records`（无手机号的收件人正确落 `FAILED_PERMANENT` + 原因；有手机号的收件人
正确发出 `infra.notification.dispatch.im.v1`）→ `integration-im-dingtalk` 真实消费、`target_adapters`
自过滤通过、真的对公网 `oapi.dingtalk.com` 发起了 4 次真实 HTTP 请求（占位测试凭据，无法通过鉴权），
每次都收到真实的钉钉错误响应（`errcode=40096`）并正确解析、分类为可重试、按 `attempt` 递增重试 3 次后
转 `FAILED_PERMANENT`，`integration.im.result.v1` 的 4 条结果事件 version 序列真实验证为 `[12, 22, 32, 42]`
（`attempt*10+phase_ordinal` 公式的真机确认）。这条链路证明了组件间事件路由、族级自过滤、重试状态机、
version 单调递增全部真实工作。

✅✅ **真机端到端验证（真实凭据阶段，同一天补上）**：用户免费建了一个钉钉个人团队（不需要真实企业营业
执照）+ 企业内部应用，给了真实 Client ID/Secret/AgentId，换掉 `.env` 里的占位值后重新部署。真机验证过程中
又发现并修了两个真实 bug（都是"只有真实响应才能暴露"的那一类，`httptest` 模拟测不出来）：

1. **`getbymobile` 缺权限被误判成可重试**：第一次真实调用返回 `{"errcode":88,"sub_code":"60011","sub_msg":
   "应用尚未开通所需的权限：[qyapi_get_member_by_mobile]…"}`，`ClassifyError` 当时落进"区分不了就重试"的
   默认分支，会白白重试 3 次——这类错误需要人去开发者后台点一次"申请权限"，重试不会自愈，已改判不可重试
   （`integration-im-dingtalk@1.0.1`）。用户在开发者后台点开权限后，`getbymobile` 真实解析出了 userid。
2. **`GetSendResult` 假设的响应字段不存在**：以为响应里有 `status`/`progress_in_percent`（建仓库时没有沙盒，
   照公开文档猜的），真实响应根本没有这两个字段，而是直接把收件人分到 `read_user_id_list`/
   `unread_user_id_list`/`failed_user_id_list`/`invalid_user_id_list`/`forbidden_list`（最后一个字段名
   也不是猜测的 `forbidden_user_id_list`）几个名单里——按 `status>=2` 判断"已完成"时零值恒为 0，会让一条
   已经真实送达的消息在 `notification_records` 里永远卡在 `ACCEPTED` 直到重试轮次耗尽。改成按目标 userid
   出现在哪个名单里判断，真机验证过（`integration-im-dingtalk@1.0.2`）。

**修完之后，`infra-workflow.CreateTask` → `infra-notification` → `integration-im-dingtalk` → 钉钉真实
API → 用户真实手机上的钉钉 App 完整跑通**：`notification_records.status` 真实推进到 `SENT`，
`integration.im.result.v1` 的 `sent.v1` 事件真实发布，用户在自己的钉钉 App 上确认收到了工作通知——不是
断言"调用过某个 mock 函数"，是真的有一条消息出现在了真实用户的钉钉里。

⚠️ 额外发现一个测试基础设施的坑：`integration-im-dingtalk` 是全项目唯一一个测试会触发真实外部副作用的
组件——它的 `consumer_test.go` 直接往本机共享的 NATS 发布裸事件，如果真实部署的容器同时订阅着同一个广播
subject，会把测试事件也当真事件处理一遍，真的调一次钉钉 API（这次验证期间已经真实触发过一次，因为撞上
一条陈旧的 fake 缓存 userid 才没有真的打扰到真人）。已记入 `integration-im-dingtalk` 的 `AGENTS.md`：
测试前必须先 `docker stop` 掉真实部署的容器。

**验证：** `infra-workflow` 产生一条审批待办事件 → `infra-notification` 路由 → `integration-im-dingtalk` 真的
把一条消息发进测试群（真机验证，不是断言"调用过某个 mock 函数"）。✅ **已完整达成**，见上方"真实凭据阶段"
的验证记录。

#### Task 10：`infra-print` 建仓库 → CP-14 → 实现

**本阶段第一个 Python 组件。** 决策 108 的物理理由这次要真正验证：FastAPI + 异步 handler + `asyncpg`，不是
Flask/Django、不是同步 gRPC、不是多 worker 的 gunicorn（Outbox 推送线程会跑 N 遍）。

- [x] 管理打印模板（HTML 模板，A4 送货单、斑马打印机条码标签 ZPL 指令）
- [x] 渲染接口：业务组件传 `template_id` + 数据（JSON），返回 PDF 文件流或打印机指令流
- [x] 模板版本管理（上传、预览、版本回滚）
- [x] **严禁包含业务逻辑**——数据由调用方整个传入，本组件不回查任何业务库；渲染引擎藏在内部接口
      （`Renderer` Protocol）后面，业务代码不直接 `import weasyprint`

**当前状态**：`infra-print@1.0.2` 已出档。`repo`（模板版本管理 + 审计/Outbox）/`service`（REST/gRPC 共用编排）/
`render`（PDF/ZPL 双路径）/`http`/`grpc`/`module`/`migrate`/`partition` 全部落地，`make gates` 9 门禁全绿
（含真实起容器的 `smoke`——`brickkit up --dry-run` 与之后的真实 `brickkit up` 都验过）。真实 Postgres + 真实
NATS + 真实 WeasyPrint 测试覆盖（27 项）：模板 CRUD/版本递增/回滚、`batchGet` 省略查不到的 id、游标分页排他性、
`record_job_and_publish` 事务性（job+outbox 同事务）、真实渲染出带中文的 A4 PDF 并用 `pdftotext` 确认无乱码、
ZPL 通道纯文本替换、模板不存在/已停用/渲染语法错误三条失败路径都正确落 `FAILED` job、`preview` 不写 job、
真实 gRPC in-process server 调用、真实 JWT + bundle 轮询的 HTTP 权限判定链路（401/403/200 三态）、周/月分区
维护幂等、迁移连跑两次幂等。

⚠️ **实现阶段的一处设计判断，偏离了设计计划 §9 待决问题 3 的倾向**：渲染引擎选了完整 Jinja2（支持
`{% for item in items %}` 循环），不是"纯变量替换 + 格式化，不做计算"——真实的送货单/发票需要行项目循环，
纯变量替换做不到。"模板不许有业务逻辑"这条边界改用代码审查守，不用语法沙箱强制，同本项目"很多规则靠
review 而不是纯技术手段"的既有模式。已回填 `docs/design/infra-print.md` §9 与组件自己的 `AGENTS.md`。

✅✅ **真机 `brickkit up` 十一个组件一起跑，意外发现并修复一个影响全部 Python 组件的 `be-sdk-python` 级
bug**：`infra-print` 第一次真机部署，容器一直 `unhealthy`，日志反复 `"HEAD /healthz 405"`。根因：
`be-sdk-python` 的 `fastapi_app.py` 此前写着"Starlette（FastAPI 的底层）默认会为 GET 路由自动应答 HEAD，
不需要像 Gin 版那样显式注册"——**这句话本身是错的**。查证 Starlette 源码，`Route.__init__` 确实有
`if "GET" in methods: methods.add("HEAD")`；但 **FastAPI 的 `APIRoute.__init__` 整个不调用
`super().__init__()`**，自己重新赋值 `self.methods`，那一步逻辑没有被带过来——`@app.get(...)` 在 FastAPI 下
对 HEAD 请求一律 405，这是 FastAPI 本身的行为，此前只是查了 Starlette 的文档/源码就下了结论，从没有真的跑
一次代码验证。同 be-sdk-go A4h 的修法：`/healthz` 同时用 `@app.get`/`@app.head` 两个装饰器显式各注册一次。
已发布 `be-sdk-python@v0.3.1`（补了此前完全没有测试覆盖的 `tests/test_fastapi_app.py`），`infra-print` 升级
依赖后重新部署验证通过（`component.yaml` 1.0.1→1.0.2）。记入根 `docs/dev/实测踩坑记录.md` A9。

**验证：** 真机渲染一张 A4 送货单 PDF，人工核对内容与模板对得上。✅ **已完整达成**——修复上述 bug 后，
`brickkit up` 十一个组件全部 `healthy`，真实 `grpcurl`/Python 客户端调 `Render`（容器 gRPC IP 直连）渲染出
一张真实的中文 A4 送货单 PDF，`pdftotext`/`pdftoppm` 转图人工核对内容（客户名、单号、品名、数量表格）与模板
完全对得上；同时确认 `print_jobs` 落一条 `SUCCESS` 记录、`infra.print.rendered.v1` 真实发布到 NATS 且标记
`PUBLISHED`。镜像基底带 shell+wget（多阶段构建，`git`/`build-essential` 只留 build 阶段）；启动宽限用
Python 的 120 秒；`/healthz` 不查依赖，GET/HEAD 均返回 200。

---

## 第 3 部分 · TS 与前端（Task 11–12）

#### Task 11：`infra-bff-mobile` 建仓库 → CP-15 → 实现

**本阶段第一个 TS 组件。**

- [x] GraphQL BFF，默认服务移动端，也服务 PC 端复杂聚合页面
- [x] **严禁业务逻辑，严禁直连 DB**（§6.5）——它只是聚合层，数据全部来自后端组件的 gRPC/HTTP
- [x] 强制 DataLoader 绑定后端组件的 `batchGet` 防 N+1（客户/产品，`data_scopes: none` 的组件）
- [x] 强制 Persisted Queries；`max_depth: 5`、`max_complexity: 100`（§11.4.3）——`be-sdk-ts` 的
      `newGraphQLServer` 统一挂载，本组件真机验证过空清单 fail-closed（拒绝一切非清单内查询）

**当前状态**：`infra-bff-mobile@1.0.1` 已出档。`src/{clients,resolvers}` + `schema.ts`/`module.ts`/
`context.ts` 全部落地，9 门禁全绿。真实 mdm-customer/mdm-product/infra-workflow/erp-sales 容器测试覆盖：
gRPC `BatchGet`（枚举前缀剥离、缺失 id 按顺序补 `undefined`）、schema 按装配裁剪（没装的组件字段根本不在
schema 里）、真实子进程起服务后 401/403/200 三态、Docker 镜像内在 Node 20 上真机跑通完整 gRPC 查询链路。

⚠️ **实现时发现一处设计计划没提过的真实数据权限缺口，用户明确拍板处理方向**：设计计划 §1"不归我"表原文说
"下游按调用者身份做数据权限过滤"，但全项目目前没有任何组件的 gRPC 层转发/验证身份——"组件间协议不做数据
权限过滤"是整个平台的既有约定。这意味着对 `mdm-customer`/`mdm-product`（`data_scopes: none`）成立，但对
`erp-sales`（org/owner）/`erp-inventory`（warehouse）/`infra-workflow`（org/owner）这三个有真实数据权限的
组件不成立——照搬 gRPC `BatchGet` 的模式会绕开数据权限，`query { order(id: "别人的订单id") { ... } }` 能
直接查到。**处理方式**：这三类字段的 resolver 改走裸 `fetch()` 转发 `Authorization` 头去打各自的 REST
端点，让下游 REST 面的 `besdk.ScopeOf` 去判，不走 gRPC。已回填 `docs/design/infra-bff-mobile.md` §9 与
`infra-bff-mobile` 自己的 `AGENTS.md`。

✅✅ **真机发现并修复了三个 `be-sdk-ts` 的真实 bug**（都不是设计阶段能预判到的，`infra-bff-mobile` 是第一个
真正触发这条调用链的 TS 组件，同 `infra-print` 之于 `be-sdk-python` 的角色）：

1. **`otel.ts`/`logging.ts`/`metrics.ts` 仍是 `NotImplementedError` 骨架**（Task 2 留的）——`runStandalone`
   的实际调用链第一次真的被走通时立刻炸。补真实实现时又发现：`@opentelemetry/api` 默认的 context manager是
   彻头彻尾的 no-op，只 `setGlobalTracerProvider` 不够，`trace.getActiveSpan()` 永远拿不到刚建的
   span——Python 版没有这个坑（`contextvars` 天生活跃），JS 需要显式注册 `AsyncLocalStorageContextManager`
   （`be-sdk-ts@0.3.0`）。
2. **`npm install` 从 git 装 `be-sdk-ts` 之后 `Cannot find module 'besdk'`**：没有 `prepare` 脚本，git
   依赖装完只有 `src/`没有 `dist/`（`v0.3.1` 补上）；补上之后还是找不到——`dist/` 被 `.gitignore` 兜底的
   打包排除列表过滤掉了，打包时需要显式 `"files": ["dist"]` 白名单（`v0.3.2`）。
3. **`userClient` 直接抛 `Cannot compose insecure credentials`**：最初用 `CallCredentials` 转发身份，
   `@grpc/grpc-js` 故意拒绝把调用凭据组合进 insecure `ChannelCredentials`（安全防线，不是配置问题）。改用
   grpc-js 的 `Interceptor`，与 Python 版 `client.py` 的既有心智模型重新对齐（`v0.3.3`）。

三个都补了回归测试（此前 `otel.ts`/`logging.ts`/`client.ts`/`fastapi_app.py` 等价物完全没有测试覆盖），
记入根 `docs/dev/实测踩坑记录.md` A10/A11。

**验证：** 一次 GraphQL 查询联动 `mdm-customer`+`erp-sales` 两个后端组件的数据，用请求日志确认走的是
`batchGet` 而不是循环单条 `Get`；故意发一个超深度查询，断言被拒绝。✅ **已完整达成**——真机 `brickkit up`
十二个组件全部 healthy，真实 `curl`/Node 客户端打 `POST /graphql`，`customer(id)` 走 gRPC `BatchGet` 真实拿
到 mdm-customer 数据，`order(id)` 走 REST 转发真实打到 erp-sales，两条路径在同一次 GraphQL 请求里都被
真实触发；`persisted_queries_only: true` 的空清单默认拒绝一切非清单内查询（含超深度/任意查询），fail-closed
行为真机确认。

#### Task 12：`frontend-standard` 建仓库 → CP-16 → 实现

**只做本阶段这几个模块的页面**（商机看板、订单相关的审批与通知展示、送货单打印入口），不是把 62 个组件的
页面都做出来。

- [ ] Vue3 + Uni-app，PC 端 AntDV v4 + vxe-table，走 `packages/ui-kit-pc` 的页面级模板（`BeListPage`/
      `BeDetailPage`/`BeFormPage`）——决策 113，不从空白 `<div>` 起手
- [ ] 通过 `GET /api/tenant/features` 动态获取启用的组件清单，动态注册路由和菜单——**这是本阶段第一次有前端
      真的去调用 Task 7 建的这个接口**，之前只在 `infra-iam-casdoor` 自己的测试里验过格式
- [ ] `packages/design-tokens` 运行时可写的 CSS 变量（决策 114：这条必须在写第一个页面之前就定死，晚了改的是
      每一个页面）
- [ ] 前端三层权限判定（`features`+`permissions`，导读第 20 条：菜单看得见但整页 403 是没判 `permissions` 的
      典型症状）

**验证：** 装配一个只启用本阶段这几个组件的假环境，前端菜单只显示对应模块；换一个权限键不够的用户登录，
对应菜单项应该判 `permissions` 后不可见或点进去 403（两种表现都要与后端行为对齐，不能前端自说自话）。

---

## 第 4 部分 · 链上一环与全链路（Task 13–14）

#### Task 13：`crm-opportunity` 建仓库 → CP-17 → 契约 + 实现

| 项 | 值 |
|---|---|
| 依赖 | `mdm/customer`、`mdm/product`（`batchGet` 校验客户/产品，做法与阶段二 `erp-sales` 同源——直接复用 `contracts/vendor/` 的模式，见阶段二复盘 §2.3） |
| 发布事件 | `crm.opportunity.won.v1` / `.lost.v1` / `.stage_changed.v1`（§4.3 事件图；`won.v1` 的字段名从一开始就要和 `erp-sales` 消费它时的真实预期对齐——阶段二 `erp-finance` 消费 `sales.order.created.v1` 字段名猜错的教训（复盘 §2.1）不要在这里重演，写契约前先去读 `erp-sales` 打算怎么消费这个事件） |
| 数据权限 | 按设计书 §14.2.2，`org`/`owner` 维度大概率适用（CRM 私海、按部门可见）——写 `assembly.yaml` 前先核对 `erp-sales` 的先例，两边"我的订单"式的场景判据应该一致 |

- [ ] 商机跟进、阶段推进、赢单预测、漏斗分析
- [ ] "标记赢单"动作：更新状态、写 Outbox、发布 `crm.opportunity.won.v1`——**这一步之后本组件不再关心后续**，
      建单是 `erp-sales` 消费事件之后自己的事，不是本组件同步调用出去的

**验证：** 真实创建一个商机、标记赢单，断言 `crm_opportunities` 状态变化 + Outbox 记录 + 事件真的发布到总线
（不是只测本组件内部状态，事件发布这一步必须真验）。

#### Task 14：14 个组件一起 `brickkit up`，跑通延伸链路

```bash
brickkit up                    # 阶段二 5 个 + 本阶段 9 个 = 14 个组件
# 标记商机赢单 → 断言：订单已建、库存已锁、应收已生成、
#   审批待办已生成 → 处理审批 → 钉钉群收到通知 → 打印送货单 PDF 内容正确
```

⚠️ **这一步验证的重点与阶段二不同。** 阶段二验的是"平台的每条承诺是否成立"；本阶段验的是**"业务链条的每一环
真的连得上"**——包括阶段二已经验证过的 TCC 补偿链，这次要在真实的、更长的调用链里再走一遍，因为多了
`crm-opportunity` 这个新的触发源、多了 `infra-workflow`/`infra-notification`/`infra-print` 这三个新的下游消费者：

- [ ] **Saga 补偿走一遍**：真实制造一次库存不足，确认 `erp-sales` 的补偿链在这条更长的链路里依然正确（订单
      没落库、库存没被占、`crm-opportunity` 的赢单状态不回滚——**CRM 侧的状态与 ERP 侧的补偿是两回事**，事件
      握手不是分布式事务，这条边界必须在真机验证里体现出来）
- [ ] **超时查询走一遍**：同阶段二 `confirm_test.go` 的思路（真实 `context.WithTimeout` 掐短），确认整条链路
      在新增的下游组件存在的情况下依然不会误杀/漏杀
- [ ] **DLQ 进出各一遍**：重试耗尽后消息真的进 DLQ、`hop_count > 5` 真的被丢弃、能被重新投递——这是阶段二
      `be-sdk-go` 的 SDK 层能力，本阶段第一次在真实业务场景下触发它（阶段二只做了平台断言层面的验证）；
      ⚠️ **`infra-dlq-monitor` 不在本阶段**（档 4a），这里验的是 SDK 层的死信通道本身，不是管理界面

**验证方式延续阶段二的纪律：真故障注入，不是 mock。** 阶段二复盘 §4 第 3 条已经论证过"TCC/补偿类逻辑 mock
测试等于没测"，本阶段链路更长，这条判据只会更重要。

---

## 第 5 部分 · 新增平台断言（Task 15）

**这一部分延续 `be-acceptance/platform/` 作为常驻资产的定位**（阶段二复盘 §5）——不是另起一套验收机制，
是往 `make tier1` 里加新断言。本阶段引入的平台表面积，阶段二的 20 条完全没覆盖到：

| 新断言 | 验什么 | 怎么验 |
|---|---|---|
| Python 组件冷启动宽限 | `startPeriodSeconds` 对 Python 组件是否真的按 120 秒生效（不是 Go 的 60 秒） | 类似阶段二用例 10b 的思路，用 `infra-print` 真机验证一次 |
| TS 组件基本行为 | TS 组件的健康检查、`/healthz` 语义、`Config` 的 camelCase 转换是否与 Go/Python 一致 | `infra-bff-mobile` 真机验证一次 |
| DLQ 往返 | 重试耗尽进 DLQ、`hop_count>5` 丢弃、重新投递，SDK 层的行为本身 | Task 14 已经真机触发过一次，这里落成可重跑的 `go test` |
| GraphQL 深度/复杂度限制 | `max_depth`/`max_complexity` 是否真的拒绝超限查询 | Task 11 已经验过，落成回归测试 |

⚠️ **不必凑数。** 阶段二 20 条是因为设计书 §9.6.2 明确列了清单；本阶段没有对应的强制清单，**只加真的在
Task 1–14 过程中发现"平台在这里有一条新承诺"的断言**，判据同阶段二导读："发现平台真有问题时先在这里记一条
用例，再回 brickKit 仓库修——不许在业务侧绕过去"。

---

## 第 6 部分 · 阶段三出档

**🏁 出档检查：**

```bash
make check
make registry-check
make gates                          # 含本阶段新增的 Python/TS 版扫描
make docs-check REPO=infra-authz    # 九个新组件各跑一次
make docs-check REPO=infra-iam-casdoor
make docs-check REPO=infra-workflow
make docs-check REPO=infra-notification
make docs-check REPO=integration-im-dingtalk
make docs-check REPO=infra-print
make docs-check REPO=infra-bff-mobile
make docs-check REPO=frontend-standard
make docs-check REPO=crm-opportunity
make tier0                          # 十四个组件各自档 0 六项验收
make tier1                          # 平台断言（阶段二 20 条 + 本阶段新增的）
```

加两条人工检查：`make arsenal-check` 绿；`git status` 干净（14 个 submodule 指针与 `brickkit.yaml` 自洽）。

### 出档之后要做的事（不许直接开阶段四）

按 [`docs/retrospectives/02-阶段二-验平台-复盘.md`](../retrospectives/02-阶段二-验平台-复盘.md) 定下的新流程——每个阶段计划结束、下一阶段开工前先写复盘：

1. **写阶段三复盘**：`docs/retrospectives/03-阶段三-业务闭环-复盘.md`——问题/矛盾/brickKit 理念问题/流程改进/
   是否要重排阶段。⚠️ 本阶段第一次涉及 Python/TS/GraphQL，第一次让权限判定真正生效，预期会有比阶段二更多
   "原来设计书没想到的地方"，认真写。
2. **改设计书**：复盘里发现的回填项一次性改掉（同阶段二 Part 3 第 2 项的模式）。
3. **判定新发现的槽位族候选**：本阶段的审批路由（`infra-workflow`）、通知渠道优先级（`infra-notification`
   路由多个 channel 时的判定策略）都是设计书 §5.11.1 已经点名"可能是族候选，要先确认没有依赖边"的位置，
   出档时按 R-4 走一遍判定——**审批路由这条阶段二的槽位族发现已经提示"要先确认它不违反 §6.6"**（§5.11.1 示例
   表），现在 `infra-workflow` 真的建出来了，是把这个悬而未决的例子判掉的时候。
4. **写阶段四计划**（`docs/plans/04-阶段四-做外壳验拆回.md`）：把这 14 个组件合成 2 个外壳，`be-ops` 要补齐
   产出 4/7/8（外壳合并配置、每外壳环境变量表、外壳间 `depends_on`）。

⚠️ **阶段三预期会验不到的**（写在这里，免得阶段四又忘）：

| 验不到 | 为什么 | 什么时候能验 |
|---|---|---|
| 合并部署形态下的权限判定 | `infra-authz` 本阶段是独立容器，bundle 轮询在合并态下是"5 个外壳进程各一份"（§14.1.4），本阶段只有 14 个独立容器，验不到"同一份 bundle 被多个模块共享"这个合并态特有的形态 | 阶段四 |
| K8s 全拆下的路由/权限 | 本阶段仍然是 `deploy.target: docker` | 阶段七 |
| `infra-dlq-monitor` 的管理界面与积压告警 | 装配角色 `default` 但不在档 2 切片，档 4a 才补 | 阶段五 |
| `mdm-org` 接管审批人路由 | 阶段五才建 | 阶段五 |
