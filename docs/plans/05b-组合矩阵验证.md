# 阶段五 B · 组合矩阵验证

> **这是唯一一份记录"部署组合怎么测"的文档**：要测什么、为什么这么测、组件参与清单、真机过程、结果、结论，全部写在同一份文档的同一个任务节里，不拆成计划/变体设计/结果这类互相引用的多份文档。准备工作（把装配仓库迁移到真实 `servedBy`）在 `docs/plans/05a-迁移到servedBy.md`，本文档只管正式的组合排列测试，默认 05a 的全部结论已经成立。
>
> **每个 Task 都是独立、完整的一节，不依赖、不引导到别的 Task 去看结果**——包括版本变体：同一个拓扑分组里的四个 Task（比如"纯外壳"的 Task 4/5/6/8）各自有一份完整的版本变体测试，不是"选一个代表性 Task 测完整、其它 Task 只抽样复核"。哪怕两个 Task 测的版本变体在道理上是同一件事，也分别在各自的环境下真机构造、各自记录，因为运行环境本身（裸进程 / Docker / K8s）往往会让"怎么构造这个变体"的具体做法不一样，写清楚这个做法本身就是这个 Task 的产出。

## 动机

现在只有 14 个真实组件，本机资源还绰绰有余；等军火库真的铺到接近 62 个组件时，光是"起一遍全部组件"这件事本身就可能撑不住本机资源，到时候再想补一遍"部署方式到底靠不靠谱"这类基础验证，成本会高得多。趁现在数量还合理，把"组件"和"外壳"两种部署形态、在"本地/Docker/K8s"三种运行环境下、以及两者混搭和多版本共存的全部排列组合，能验的都真机验一遍，每一种组合独立记录过程和结果，**包括那些肯定会出错的组合也要真机构造、记录报错内容和理由，不能因为"预期会失败"就跳过不测**。

能够部署不代表整个项目真的可用——每个任务除了"能不能起来"，都必须至少做一次真实的跨组件调用，确认协作符合预期。

## 参考资料（背景机制，不重复解释）

- `docs/dev/给brickKit的反馈.md` 第七部分——最初的 `servedBy` 提案。
- brickKit 仓库 `docs/zh/patterns/shell-implementers-guide.md`（造壳指南）+ `docs/zh/patterns/servedby-deployment-checklist.md`（部署方检查清单）——`servedBy` 的权威使用文档。
- brickKit 仓库 `AGENTS.zh.md` §5.7、§9.21——平台侧对这个功能的正式说明与边界论证。
- **调研结论摘要**（设计每个任务时随时回查）：
  1. `servedBy: <组件ID>@<精确版本>`，格式/自引用/链式嵌套/与 `local:true` 互斥共 6 条静态校验 + 目标存在性/运行态 2 条生成期校验 + 端口/端点变量/标签 3 条合并冲突校验，全部有 brickKit 自己的单元测试覆盖。
  2. 环境变量合并**只**覆盖 `*_ENDPOINT` 类，`COMPONENT_ID`/`COMPONENT_VERSION`/`configSchema` 配置值/资源连接变量**无条件排除**，有专门测试断言"即使同名同值也不会被合并"。
  3. `BRICKKIT_SERVED_MEMBERS`——逗号分隔版本化服务名，字典序；**零成员时是显式空字符串，不是变量缺失**。
  4. Docker：外壳的 compose service 挂上每个成员的版本化服务名作网络别名；依赖了某个 `servedBy` 成员的组件，`depends_on` 自动改指向外壳；成员自己不生成 service。K8s：成员生成一个 `selector` 指向外壳 Pod 标签、`targetPort` 用成员自己端口的 Service，不生成 Deployment/Job；NetworkPolicy ingress/egress 自动跟着调整。
  5. ⚠️ **`expose`/`exposePort`/`hostname`/`replicas`/`resources`/`serviceAccountName` 写在 `servedBy` 成员条目上完全不生效**（只有警告）。
  6. **多版本共存**：同一个外壳收编同一组件的两个不同版本——**已确认支持**（`TestResolveGroupsTwoVersionsOfSameComponentUnderOneShell`），唯一约束是端口不能撞；`EnvPrefix` 不带版本号，若两个版本的同名 config 值不同会直接报错（05a Task 0.6 收尾已确认这是合并进同一进程固有的性质，不是缺陷）。同一个 `(id, version)` 不能同时独立部署又被 `servedBy`。**两个不同外壳各自收编同一组件的不同版本**——brickKit 自己完全没有测试或文档覆盖，是真正的未知领域。
  7. `local: true` 的字段语义、K8s 下的拒绝逻辑，`servedBy` 落地前后完全没变。

---

## 本轮验证的真实 14 个组件（2026-09-17 基线快照）

下文所有"真实全部 14 个组件"都指下面这份清单，逐一写明是为了未来军火库铺到更多组件之后，回头能准确对照"当时到底测的是这 14 个、现在测的是哪些"，不依赖"14 个"这个数字本身（数字以后会变，名字和版本才是真正的凭据）。**这份清单只在这里维护一份，各 Task 的"组件参与清单"直接写组件名，不重复贴版本号**——版本号以每个 Task 自己"结果"里记录的真机执行时间点为准（真机执行时可能已经不是下表这个版本，参照 §"每个任务/变体的强制要求"第 1 条）。

| # | 组件 | 2026-09-17 版本 | 当前部署形态（baseline，即变体 a） |
|---|---|---|---|
| 1 | `mdm/customer` | 1.0.9 | `servedBy: shell/go-core` |
| 2 | `mdm/product` | 1.0.10 | `servedBy: shell/go-core` |
| 3 | `erp/inventory` | 1.0.17 | `servedBy: shell/go-core` |
| 4 | `erp/finance` | 1.0.13 | `servedBy: shell/go-core` |
| 5 | `erp/sales` | 1.0.25 | `servedBy: shell/go-core` |
| 6 | `infra/authz` | 1.0.7 | `servedBy: shell/go-infra` |
| 7 | `infra/iam-casdoor` | 1.0.9 | `servedBy: shell/go-infra` |
| 8 | `infra/notification` | 1.0.4 | `servedBy: shell/go-infra` |
| 9 | `infra/workflow` | 1.0.4 | `servedBy: shell/go-infra` |
| 10 | `integration/im-dingtalk` | 1.0.5 | `servedBy: shell/go-infra` |
| 11 | `crm/opportunity` | 1.0.12 | `servedBy: shell/go-backoffice` |
| 12 | `infra/print` | 1.0.7 | `servedBy: shell/py-render` |
| 13 | `infra/bff-mobile` | 1.0.19 | 独立部署（结构性不可能被 servedBy，见下方"环境定义"） |
| 14 | `frontend/standard` | 1.0.0 | 独立部署（无后端进程，同上） |

下文两处经常出现的子集，都是这张表的固定行区间，同样不重复贴名字：
- **"11 个 Go 组件"**（Task 4/5/6/8/9/10/11/12 的"纯外壳"/"混搭"清单常用）＝上表第 1–11 行（`mdm/customer` 到 `crm/opportunity`），即全部 Go 语言编写、且当前 baseline 下确实 `servedBy` 某个外壳的组件。
- **"12 个 servedBy 成员"**＝上表第 1–12 行（在 11 个 Go 组件之外再加 `infra/print`，唯一的 Python 组件，`servedBy: shell/py-render`）。
- 其余场景（比如"其余 12 个组件保持独立部署陪跑"）如果不是这两个固定子集，会在当次 Task 里直接写"全部 14 个减去 xxx/yyy"，不再单独定义新名词。

⚠️ **未来重跑这份 05b 计划、或对照它的结论时，第一步永远是先重新生成这张表**（`brickkit.yaml` 当时的 `components:` 段一份一份抄一遍，而不是信任这份写死的快照）——这张表本身会过期，过期之后唯一还能相信的是"当时确实是这 14 个、这些版本"这件**历史事实**，不是"未来也应该长这样"。

---

## 组合矩阵总览

**拓扑 × 环境**（12 个基线任务，每个格子对应一个 Task）：

| 拓扑 \ 环境 | 本地 | Docker | K8s | Docker+本地混合 |
|---|---|---|---|---|
| **纯独立组件** | Task 1 | Task 2 | Task 3 | Task 7 |
| **纯外壳**（`servedBy`） | Task 4 | Task 5 | Task 6 | Task 8 |
| **组件+外壳混搭** | Task 9 | Task 10 | Task 11 | Task 12 |

在这 12 个格子之外：Task 13（`local:true`+K8s 目标负向验证）、Task 14（`servedBy` 静态校验负向验证）、Task 15（出档）——这三个不属于拓扑×环境矩阵本身，是矩阵之外的收尾/边界验证。

**每个格子内部还有一层版本变体**（维度三）——同一个拓扑分组内的 4 个 Task，各自独立测完整的版本变体集合，集合大小取决于这个拓扑有几类"独立可升级单元"：

| 拓扑 | 独立可升级单元 | 版本变体集合（每个 Task 各自完整测一遍） |
|---|---|---|
| 纯独立组件 | 每个组件各自独立 | a. 全新版本 baseline<br>b. 依赖方新、被依赖方旧<br>c. 依赖方旧、被依赖方新 |
| 纯外壳 | 外壳镜像本身 + 外壳内每个成员的 `servedBy` 版本声明 | a. 镜像和全部成员声明都最新 baseline<br>b. 镜像升级了，某个成员声明还指向旧版本<br>c. 外壳镜像本身没升级，但想升级某个成员<br>d. 同一个外壳收编同一组件的两个不同版本<br>e. 两个不同外壳各自收编同一组件的不同版本 |
| 混搭 | 外壳 + 独立组件两类单元都有 | a. 全部同步 baseline<br>b. 外壳同步了，独立组件没同步<br>c. 独立组件同步了，外壳没同步<br>d. 极端场景：多个独立组件各自依赖同一下游组件的不同版本<br>e. 一个版本合并进外壳、另一版本完全独立部署 |

变体 a 就是该 Task 本体（baseline），不重复写一份，其余变体（b/c/……）各自单开一节，写在对应 Task 正文之后。

### 每个任务/变体的强制要求（不满足不算做完）

1. **组件参与清单**：明确写出用了真实 14 个组件里的哪些、各自什么版本、排除了哪些、为什么排除。执行完之后要把"上一个 tag"这类相对说法替换成真实用到的确切版本号（比如 `1.0.9` → `1.0.8`），相对说法只允许出现在执行前的计划阶段。
2. **代表性沟通测试**：不能只验证"能不能部署起来"，必须至少发起一次真实跨组件调用确认协作符合预期，不用覆盖所有链路，1-2 条有代表性的就够。如果这条任务本身预期就是失败（负向验证），写清楚"不适用，本任务测的是失败本身"。
3. **结果直接写在本节的"结果"/"结论"里**，缺哪一项要写清楚"为什么没有"，不能悄悄空着。执行前先填好"组件参与清单"和"测试方法"，执行后立刻补"结果"和"结论"，不攒到最后一起写。不管结果是成功还是失败/报错/行为不符合预期，都要如实记录具体现象（报错文案、日志片段、实际观察到的行为），不能只写"通过"/"符合预期"这种无法复核的结论。
4. **禁止用"同 Task X"代替具体场景描述**：哪怕两个 Task 的构造在道理上完全一样，也要在本节内把"组件参与清单"和"测试方法"完整重写一遍，不能只写"同 Task 5b 的场景"就结束——未来审批只会直接跳到某个矩阵格子看，不会顺着文档去翻别的 Task。允许的例外是纯粹的背景说明/对比（比如"这条在 Docker 侧已验证过，这里独立复现"），前提是即使不看那句话，本节自己的步骤和结论也完整、可执行、可理解。
5. **结果里要写明真机执行的日期**，配合第 1 条的确切版本号一起构成"这次验证是在什么状态下做的"的快照，供以后组件铺到更多数量时，回头比对同一个矩阵格子"当时 vs 现在"具体发生了什么变化。
6. **鉴权面组件是任何真实业务调用绕不开的前提，必须补进组件参与清单**：Task 1 真机执行前读 `be-sdk-go` 的 `authz.go` 确认——组件启动不会因为连不上 `infra-iam-casdoor`/`infra-authz` 而失败（不阻断启动是设计好的行为），但只要"代表性沟通测试"要发起一次真的带权限键的业务调用（而不是只测端口通不通），就必须有：① 真实、能被本地 JWKS 验签通过的 JWT（需要真的走一遍 Casdoor OAuth 登录 + `infra-iam-casdoor` 拿 `id_token` 换 app token 的完整链路，各组件 `scripts/seed.sh` 的 `get_jwt()` 函数就是这么做的）；② `infra-authz` 真的可达并成功轮询过一次 `/authz/bundle`，否则具体权限键判定直接 503。因此**每个 Task 只要"代表性沟通测试"需要真实业务调用，`infra-iam-casdoor` + `infra-authz` 就要按这个 Task 当前的拓扑/环境形态一起补进"组件参与清单"**（本地裸进程任务里这两个也裸进程跑；Docker 任务里这两个也是 Docker 容器；纯外壳任务里这两个也在对应外壳里，等等），不能因为"这次测的主题不是鉴权"就默认忽略——这两个本身也是 14 个真实组件之一，不是可以当基础设施忽略不计的东西。

### 环境定义

**"纯外壳"（Task 4/5/6/8）的组件参与清单，跟"混搭"（Task 9-12）不一样，两者不是同一份拓扑测两遍**：`frontend-standard` 没有后端进程（纯前端 SPA/H5，压根不存在可以被合并的东西）、`infra-bff-mobile` 按设计明确保持独立——这两个组件结构性地永远不可能被 `servedBy`。所以：
- **纯外壳（Task 4/5/6/8）用缩减清单**：只含 11 个 Go 组件 + `infra-print`，**全部** `servedBy` 收编进各自外壳，`frontend-standard`/`infra-bff-mobile` 这次测试**不参与**（临时从 `brickkit.yaml` 里去掉，或者 `enabled: false`）。这样测出来的才是真正"零独立组件、100% 合并"的场景。
- **混搭（Task 9-12）用真实全量清单**：真实 14 个组件——12 个 `servedBy` 收编 + `frontend-standard`/`infra-bff-mobile` 独立，这本来就是本项目当前唯一的真实部署形态。

这样 Task 5 和 Task 10 就不是测同一份拓扑——一个是"能不能做到 100% 纯合并"，一个是"我们真实项目现在长什么样"，两者都有独立价值。

**"Docker+本地混合"的准确定义**：绝大部分组件走 Docker（不管是独立部署还是被某个外壳收编），额外挑 1 个**完全独立**的组件（不受 `servedBy` 影响）用 `local: true` 拉到宿主机调试——这是唯一一种 `local:true` 和"这次测的拓扑"会同时出现在同一份 `brickkit.yaml` 里的组合，brickKit 自己的回归测试（`TestLocalStillWorksAlongsideServedBy`）已经确认两者不冲突，这里在自己的真实组件上复核一遍。

**"纯本地"（Task 1/4/9）的准确定义，区别于"Docker+本地混合"**："本地"是跟"Docker+本地混合"并列的独立一档，不是它的子集——"纯本地"任务里**全部参与组件都跑成宿主机裸进程**（`go run`/`uvicorn` 之类，手动喂环境变量模拟平台注入），不借助 `local:true` 机制（那个机制的前提就是"其它组件在 Docker 里跑着"，属于"Docker+本地混合"那一档）。

---

## Task 1：纯独立组件 × 本地

**组件参与清单**：`erp-sales`（依赖方）+ `mdm-customer` + `mdm-product`（业务被依赖方）+ `infra-iam-casdoor` + `infra-authz`（鉴权面，代表性沟通测试要发真实业务调用绕不开的前提），其余不参与（验证的是"单个组件本地调试"这个机制本身，不需要全量组件陪跑）。五者都用 `brickkit.yaml` 当前锁定的最新版本（变体 a / baseline）。⚠️ **计划阶段的两次修正**：① 原计划只写 `erp-sales`+`mdm-customer` 两个组件，执行前读 `backend/internal/tcc/create.go` 发现 `CreateOrder`（哪怕是最简单的建草稿单）内部同时调用 `fetchActiveCustomer`（`mdm-customer`）和 `fetchActiveProducts`（`mdm-product`，用于按分类取价），不存在"只连 `mdm-customer` 就能完成一次有业务意义调用"的路径，补 `mdm-product` 进清单；② 执行前读 `be-sdk-go` 的 `authz.go` 确认，任何带权限键的真实业务调用都需要真实可验签的 JWT（走 Casdoor OAuth 登录 + `infra-iam-casdoor` 换 app token）和 `infra-authz` 提供的权限 bundle（否则 503），补 `infra-iam-casdoor`+`infra-authz` 进清单，两者本次也跑裸进程。

**测试方法**：
- [x] 真机走一遍纯本地裸进程流程：手动起五个组件各自需要的依赖（DB/NATS/Casdoor OAuth 复用装配仓库已经在跑的 `be-postgres`/`be-nats`/`be-casdoor` 基础资源），手动喂环境变量（模拟平台会注入的值，临时把这五个组件在 `brickkit.yaml` 里的 `servedBy` 换成 `local: true` 跑一次 `brickkit up --dry-run` 生成 `local-debug.*.env` 拿到清单，记完立刻 `git checkout` 复原 `brickkit.yaml`——这一步只用于读取清单，不通过 `brickkit up` 真的拉起这五个组件），宿主机直接 `go run` 起这五个组件，确认正常响应。⚠️ 注意这条不是 `local:true`（那是"Docker+本地混合"，见 Task 7）——这里验证的是"完全不碰 Docker、纯裸进程"这条路径本身走不走得通，组件本身不通过 `brickkit up` 拉起。
- [x] 走一遍 `get_jwt`（参照 `erp-sales/scripts/seed.sh` 里的实现）：向裸进程 `infra-iam-casdoor` 暴露的 `/api/iam/token` 换真实 app JWT（前提是 be-casdoor 里已有 `dev.superuser` 这个测试账号，没有就先按该组件 `make seed` 的方式补建）。

**代表性沟通测试**：`erp-sales` 裸进程用真实 JWT 发起一次 `CreateOrder` 建草稿单调用，真实经过 `infra-iam-casdoor` 验签 + `infra-authz` 权限判定，再真实 gRPC 调用 `mdm-customer`+`mdm-product` 裸进程，确认业务逻辑本身在纯裸进程运行形态下没有被破坏（不只是进程活着，是订单真的建成功、字段快照正确）。

**结果**（2026-09-17 真机执行，版本快照：`erp-sales@1.0.25`、`mdm-customer@1.0.9`、`mdm-product@1.0.10`、`infra-iam-casdoor@1.0.9`、`infra-authz@1.0.7`）：

1. **组件参与清单确认**：五个组件全部以 `go run ./backend/cmd/server` 裸进程跑在宿主机（PID 分别为 mdm-customer 233244、mdm-product 233594、infra-authz 233660、infra-iam-casdoor 233913、erp-sales 234046，`go run` 会 fork 真正监听端口的子进程，收尾按端口而不是按这个包装 PID 结束），依赖的 DB/NATS/Casdoor 全部复用装配仓库已经在跑的 `be-postgres`/`be-nats`/`be-casdoor` 三个基础资源，`brickkit_db` 里对应 5 个 schema（`erp_sales`/`mdm_customer`/`mdm_product`/`infra_authz`/`infra_iam_casdoor`）此前已经存在，`./build/migrate up` 全部输出"迁移完成"（幂等，未产生新变更）。`/healthz` 五个端口（8080/8082/8084/8200/8223）全部 200。
2. **环境变量清单获取过程中发现的两个真实问题（不是本组件的 bug，是 brickKit CLI 生成侧的问题，会影响后续所有需要走这条路径的 Task，值得反馈给 brickKit）**：
   - **`local-debug.*.env` 写多行 config 值会截断**：`infra-iam-casdoor` 的 `appTokenSigningKeyPem`（RSA PEM，多行）在生成的 `local-debug.infra-iam-casdoor-1-0-9.env` 里只剩下 `APP_TOKEN_SIGNING_KEY_PEM=-----BEGIN PRIVATE KEY-----` 一行，PEM 主体和 `-----END PRIVATE KEY-----` 全部丢失，而同一份文件里其它单行密钥（如 `WEBHOOK_SHARED_SECRET`）完整无缺——说明这是 CLI 序列化多行字符串到 `.env` 格式时的真实 bug，不是脱敏策略（脱敏的话单行密钥也该处理）。本次绕过方式：直接从装配仓库根目录 `.env` 里取这个组件在真实部署里已经在用的同一把完整私钥。
   - **`casdoorBaseUrl` 生成值 `http://host.docker.internal:8000` 在裸进程场景下不可用**：这个地址只在"平台生成的 Docker 容器"里能解析，宿主机裸进程要手动改成 `http://localhost:8000`——这是"local:true 的地址生成假设了另一端在 Docker 网络里"这个既有认知的一个新证据。
   - **2026-09-17 复核结果（两轮）**：这两条反馈都已交给 brickKit。`casdoorBaseUrl` 这条第一轮就确认按建议做成了文档（`AGENTS.md`/`AGENTS.zh.md`，无代码改动，真机核对过 diff），问题解决，反馈文档已删除。多行 PEM 这条第一轮（v0.4.4）**只修了一半**——`shellQuote` 解决了"已经拿到完整值之后怎么安全写出去"，没解决"从 `.env` 里怎么把多行值先取出来"这一步更早的截断（根因是 `internal/cli/up_k8s.go` 的 `readDotEnv` 按物理行扫描 `.env`，在第一个 `=` 处切断）；补了这个发现之后 brickKit 出了 **v0.4.5**（commit `3e30d14`），把 `readDotEnv` 整个重写成真正的 dotenv 解析器（双引号转义+跨行、单引号原样跨行、`export` 前缀、行内注释规则，`TestParseDotEnvMatchesDockerCompose` 拿真实 `docker compose config` 逐条对照过）。**没有直接采信，自己从 brickKit 源码把这个 commit 编译出一个二进制，跑了它自己的全部单测（`go test ./internal/cli/... ./internal/compose/...` 全绿，含新增的两条端到端回归和对照测试），又原样复现了一遍最初的场景**：`infra-iam-casdoor` 改 `local:true`，`brickkit up --dry-run` 生成 `local-debug.infra-iam-casdoor-1-0-9.env`，`source` 出来 `APP_TOKEN_SIGNING_KEY_PEM` 长度 1703（跟最初手工核对的完整私钥长度一致），`openssl pkey -check -noout` 确认是一把合法的 RSA 私钥——这次是真的修好了，不是文字上说修好了。K8s 那条路径没有本机 minikube/真实 k8s target 配置去完整跑一遍（05b 排到 Task 3/6/11 才会做这件事），但 brickKit 自己新增的 `TestUpK8sDeploymentEnvResolvesMultilineDotEnvValue` 这条单测我自己跑过确认通过，可信度足够，不算完全没验证。反馈文档已删除，这条彻底关闭。
3. **鉴权链路真实跑通**：Casdoor 管理员登录（`admin/123`）→ 取 `local-dev-seed-app` 的 `clientId`/`clientSecret` → `dev.superuser`（密码 `DevSeed123!`，此前 `make seed-data` 已经建好）走 `grant_type=password` 换到 Casdoor `id_token`（含 `sub=e88c4f5a-b59c-4c48-8c6d-72c2fcae936d`）→ 裸进程 `infra-iam-casdoor` 的 `POST /api/iam/token` 用这个 `id_token` 真实签出应用 JWT，JWT payload 里 `roles:["dev_superuser"]`、`sub` 与上面一致——这条链路此前只在容器化部署里跑过，这是第一次在纯裸进程组合下验证。
4. **`CreateOrder` 真实成功**：`curl -X POST -H "Authorization: Bearer <上面拿到的 JWT>" http://localhost:8084/erp/sales/orders -d '{"idempotency_key":"05b-task1-order-1","customer_id":"211","items":[{"product_id":"4","qty":"3"}]}'` 返回 `HTTP 200`，响应体 `customer_name` 正确解析成 `「本地测试」华南电子科技有限公司`（`mdm-customer` 记录）、`items[0]` 正确带出 `product_name=Widget`/`product_sku=P-001`/`unit_price=100.00`/`tax_rate=0.130000`（`mdm-product` 记录），`total_amount=300.00` 计算正确，`order_no=SO455`。直接查 `brickkit_db.erp_sales.sales_orders where id=455` 确认真实落库，字段与响应体一致。这些字段值只能来自对 `mdm-customer`/`mdm-product` 的真实 gRPC 查询（erp-sales 自己的 schema 里不存这些数据），证明跨进程调用链路真实工作，不是某种降级/兜底路径。
5. **一个自愈的次要现象，如实记录**：`mdm-customer`/`mdm-product` 启动初期（`infra-iam-casdoor`/`infra-authz` 尚未监听端口那几秒）各打过一条 `拉取 JWKS/authz bundle 失败，沿用内存里已有的旧版本` 的 WARN/ERROR 日志，随后台轮询自动恢复，没有导致进程退出或请求失败——真机验证了"鉴权面不可达不阻断启动、且能后续自愈"这条设计确实成立（对应 `be-sdk-go` `authz.go` 里"不 exitf"的注释）。
6. **收尾**：五个进程按监听端口 `lsof -ti` 精确结束（不是杀 `go run` 包装进程，那个不是真正监听端口的进程），`brickkit.yaml` 之前两次临时改动（分别用于拿 `erp-sales`/`mdm-customer`/`mdm-product` 三者、以及 `infra-authz`/`infra-iam-casdoor` 两者的 `local-debug.*.env`）均已用 `git checkout` 复原，`git status` 干净。测试建的订单 455 留在 `brickkit_db` 里未删除（本地演示库允许假数据常驻，同其它 `make seed` 产出的记录同级对待）。

**流程说明（事后补记）**：本条实际执行时，`local: true` 临时改动在拿到 `local-debug.*.env` 后就立刻 `git checkout` 复原了，五个进程用的是誊抄整理过的独立环境变量文件，不是直接 `source` brickkit 生成的文件本身——跟用户后续明确的要求（"一切测试最终部署请使用 brickkit"，即 `local:true` 改动应保留到这条变体测完、直接用 brickkit 生成的文件而不是誊抄一份）不完全一致。内容上二者等价（誊抄时逐字核对过，唯二的修正——PEM 截断、`casdoorBaseUrl`——都已如实记录在上面），所以不重跑，但从 Task 1b 起改用新流程。

**结论**：Task 1 通过。"纯独立组件 × 本地"这个机制本身走得通——三个业务组件 + 两个鉴权面组件全部能以宿主机裸进程形态独立运行、互相之间用真实网络地址（而非 Docker 网络别名）完成一次完整的认证+跨组件业务调用，业务逻辑（客户/产品快照、定价、金额计算）在裸进程环境下没有被破坏。同时确认了两件真实发现，两件都已经跟 brickKit 走完"反馈→修复→自己重新编译验证"的完整闭环，彻底解决：① `local:true` 生成的 `.env` 文件不能安全处理多行 config 值（v0.4.4 只修了写出那一半，v0.4.5 才把 `.env` 读取那一半也修完，自己编译 v0.4.5 二进制真机复核过）；② 该地址生成逻辑对"另一端也在 Docker 网络里"这个假设，在纯裸进程场景下需要手动纠正（按建议做成了文档）。这两点此前会在后续所有"本地裸进程"类 Task（4/7/9/12 及其变体）里重复出现，现在 CLI 装到 v0.4.5 之后第一条已经不用再手工绕过了，第二条仍然需要手动把带外容器地址换成 `localhost`。

### Task 1b：依赖方新、被依赖方旧

**组件参与清单**：`erp-sales`（最新版本，裸进程）+ `mdm-customer`（人为切到上一个 tag 的源码，裸进程）+ `mdm-product`（最新版本，裸进程，陪跑用——同 Task 1 的发现，`CreateOrder` 离不开它，不是这条版本错配测试的焦点）+ `infra-iam-casdoor`+`infra-authz`（最新版本，裸进程，鉴权面陪跑用，同 Task 1 的发现），其余不参与。

**测试方法**：
- [x] 从 `mdm-customer` 组件仓库 `git checkout` 到上一个版本 tag，`go run` 起来（用旧版本自己的环境变量清单）；`erp-sales`、`mdm-product`、`infra-iam-casdoor`、`infra-authz` 都保持最新版本源码，同样 `go run` 起来。五者手动喂各自需要的环境变量，确认能各自独立启动。

**代表性沟通测试**：用 Task 1 同样方式拿到的真实 JWT，`erp-sales` 新版本裸进程发起 `CreateOrder`，调用 `mdm-customer` 旧版本裸进程一个双方版本都有的方法（`mdm-product` 走最新版本正常应答），确认成功；如果 `erp-sales` 新版本用到了 `mdm-customer` 尚未实现的字段/方法，记录具体报错——这是预期内的失败，不是 bug。

**结果**（2026-09-17 真机执行，版本快照：`erp-sales@1.0.25`、`mdm-customer@v1.0.8`（人为切到上一个 tag 源码，其余四者最新）、`mdm-product@1.0.10`、`infra-iam-casdoor@1.0.9`、`infra-authz@1.0.7`。本条起改用真实 `brickkit up` 驱动部署，不再手工编排后复原，遵循用户新确认的流程）：

1. **本地路径改为真实走 `brickkit up`**：`mdm-customer` 组件仓库 `git checkout v1.0.8`（`git tag --sort=-v:refname` 确认上一个 tag，源码 diff 显示 v1.0.8→v1.0.9 之间"无业务行为/契约变更"，纯 `scripts/seed.sh` 适配修复），`brickkit.yaml` 里 `mdm/customer` 版本改 `1.0.8`、连同 `erp-sales`/`mdm-product`/`infra-authz`/`infra-iam-casdoor` 一起从 `servedBy` 改成 `local: true`（改动保留到测完，不提前复原），`brickkit up`（真实命令）驱动生成部署产物。
2. ~~**发现一个 brickKit 真实校验缺口——`--dry-run` 和真实 `up` 的资源绑定校验强度不一致**~~——**已撤销，是我方自己的观测失误，不是 brickKit 的问题**。当时的判断依据是"Task 1 用 `--dry-run` 生成同样的 `local:true` 组合时一切正常"，但这个"一切正常"是错的：Task 1 执行时用 `brickkit up --dry-run 2>&1 | tail -60`/`tail -80` 截取了输出尾部，而这条警告其实一直都在——只是出现在完整输出的第 42 行（总共 420 行），被 `tail` 截没了。2026-09-17 用现在装好的 v0.4.4 CLI 重新构造同一个场景（`mdm/customer` 改 `local:true`、不加任何 `resources.bindings`），跑 `brickkit up --dry-run`（这次拿全量输出，不截断），第 42 行清清楚楚写着：`⚠️ 警告：资源依赖未满足（--dry-run 不阻断）`，后面跟着具体缺哪个资源、`--dry-run` 不阻断但真实 `up` 会阻断的说明——跟真实 `up` 用的是同一个 `CheckRunningResourceBindings` 调用，只是命令层把"阻断"降级成"警告"，逻辑一直是对的。查 brickKit 提交历史确认这条降级逻辑从 2026-08-23~28 就存在（`bb83e74`/`bc1892a`），比这次测试早了三周多，不是为了应付反馈临时加的。**教训**："用 `tail -N` 截断输出去确认某个警告/错误不存在"这个判断方式本身不可靠——只能证明"最后 N 行里没有"，证明不了"完全没有"，得先看全量输出再下结论，已经单独写了一条新记忆。
3. **发现一个更根本的版本解析语义，直接推翻了"改顶层 version 字段就能模拟旧版本依赖"这个直觉假设**：第一次 `brickkit up` 成功生成，但 `docker ps` 里意外多出一个**真实运行的 `mdm-customer-1-0-9` 容器**——排查发现 `erp-sales/component.yaml` 自己的 `dependencies.components` 硬编码写死 `mdm/customer@1.0.9`（依赖版本是每个组件自己源码里的精确字面量，不是从 `brickkit.yaml` 顶层动态取的），`crm/opportunity`（未参与本次测试、仍按 baseline 部署）也一样声明 `mdm/customer@1.0.9`——brickKit 的解析器对不同精确版本各自视为独立节点，只要有任何依赖边声明了某个精确版本，就会把它解析成一个真实要跑起来的节点（这次是普通 Docker 容器，因为没人把这个版本声明成 `local:true`），跟 `brickkit.yaml` 顶层怎么声明这个 componentId 完全无关。生成的 `erp-sales` 环境变量证实了这一点：`MDM_CUSTOMER_ENDPOINT` 一开始指向 `localhost:18080`（`10000+8080`，即那个自动冒出来的 1.0.9 容器的宿主机映射端口），根本没有指向我准备好的 1.0.8 裸进程。**结论：要真机验证"依赖方新、被依赖方旧"，必须同时临时改依赖方自己 `component.yaml` 里的精确依赖版本号，只改 `brickkit.yaml` 顶层的版本声明是不够的**——临时把 `erp-sales/component.yaml` 的 `mdm/customer@1.0.9` 改成 `mdm/customer@1.0.8`（注释标记临时）之后重新 `brickkit up`，`MDM_CUSTOMER_ENDPOINT` 才变成 `localhost:8080`（我准备的旧版本裸进程），`mdm-customer-1-0-9` 容器仍然存在（`crm/opportunity` 那条边没有改动，继续需要它），两个版本互不干扰地并存——这本身也顺带印证了"同一部署里多个精确版本节点能真实共存"这条此前只在 brickKit 单测里见过的能力。
4. **顺手发现第三个 brickKit CLI 的真实 bug**：`local-debug.infra-authz-1-0-7.env` 里 `PERMISSION_CATALOG` 这个值内部用 `|` 分隔字段（`crm.opportunity.edit|编辑商机|action|crm/opportunity,...`），生成文件里完全没加引号——`source` 时 `|` 被 bash 当管道操作符解析，产生一长串 `command not found`。手工给这一行补引号后问题消失，`infra-authz` 迁移/启动均正常。这是跟 Task 1 记录的"多行 PEM 截断"同一类"CLI 序列化到 `.env` 文件时没处理好特殊字符"的问题，合并写进同一份反馈文档。**2026-09-17 v0.4.4 复核：这条确认修好了**——重新生成的文件里这一行变成 `PERMISSION_CATALOG='crm.opportunity.edit|编辑商机|...'`，整体用单引号包住，`source` 后拿到完整 2085 字符，跟修复前手工核对的原始值逐字符一致。
5. **发现第四个真实问题，波及面最广，但属于本项目自己已经确认过的既有设计取舍，不是新的 brickKit 反馈**：`iamJwksUrl`/`authzBundleUrl` 是每个依赖方 `config:` 里**手写的字面量字符串**（如 `"http://infra-authz-1-0-7:8223/authz/bundle"`），不是 brickKit 按拓扑动态算出来的地址——把 `infra-authz`/`infra-iam-casdoor` 改成 `local:true` **完全不会**触发这两个字符串自动改写。`erp-sales`/`mdm-customer`/`mdm-product` 三者生成的 `local-debug.*.env` 里这两个值原样保留 Docker 服务名形式，裸进程直接用会在 JWKS/bundle 拉取阶段報 DNS 解析失败（`dial tcp: lookup infra-iam-casdoor-1-0-9 on 127.0.0.53:53: server misbehaving`），token 验签失败（401，`key not found`）。跟已有踩坑记录 C18（`field-tested-pitfalls-log.md`，升版本要同步全部 25 处引用）是同一个根因的另一种触发方式：**不只是升版本会漏改，切换部署形态（servedBy→local:true）同样会漏改**。C18 当时已经把"要不要让 brickKit 把这两个值改成原生依赖注入"这个问题问过一遍并确认**不行**（会破坏 `slot:iam`/`slot:authz` 的可替换性，见 C18 的"Update (Phase 4b Task 0.4)"），手写字面量+人工同步是刻意的设计取舍，这次只是同一个已知限制的新触发场景，已经追加进 C18 的记录里，不重复整理成给 brickKit 的反馈。手工把三个组件各自的这两个值改成 `localhost` 之后（重启这三个进程）问题消失。
6. **最终代表性调用成功**：`dev.superuser` 真实 JWT → `POST /erp/sales/orders`（`customer_id=211`、`product_id=4`）→ `HTTP 200`，`erp-sales`（1.0.25，最新）真实经过认证与权限判定后，用真实 gRPC 调用了 `mdm-customer`（v1.0.8，人为回退的旧版本源码）与 `mdm-product`（1.0.10，最新）成功建单（`id=456`，`order_no=SO456`，客户名/产品名/单价/税率全部正确回填）。因为 v1.0.8→v1.0.9 之间"无业务行为/契约变更"（见组件参与清单的版本 diff 说明），这个结果本身在业务语义上是预期内的"平淡"成功，价值主要在于第 2/3/4/5 点揭示的机制性发现。
7. **收尾**：五个裸进程按端口 `lsof -ti` 结束；`brickkit down` 停掉这次真实起来的 7 个容器（4 个外壳 + `frontend-standard`/`infra-bff-mobile`/`mdm-customer-1-0-9`，数据卷保留）；`brickkit.yaml`、`components/erp/sales/component.yaml`、`.brickkit/manifests/erp-sales-1.0.25.yaml`（`brickkit up` 会同步这份归档快照，一并出现在 diff 里）全部 `git checkout` 复原；`mdm-customer` 子模块 `git checkout v1.0.9` 切回最新版本；`git status` 确认只剩本文档的改动。

**结论**：Task 1b 通过，业务结论很简单——"erp-sales 新版本调用 mdm-customer 旧版本"在这一对相邻版本之间完全兼容（本来就没有破坏性变更）。真正有分量的是过程中意外触发的一整套"发现→反馈→修复→自己重新编译验证"的完整闭环，2026-09-17 当天走完两轮：① "`--dry-run` 校验缺口"——**撤销**，是 Task 1b 执行时 `brickkit up --dry-run | tail -60` 把警告截没了，真实代码里这条警告从三周前就有，不是 brickKit 的问题；② "想用 brickKit 真机验证版本错配，必须同步改依赖方自己 `component.yaml` 里的精确依赖版本号"——**这条不是 brickKit 的问题，是本项目自己使用方法论上的坑**，继续有效，后面所有版本错配类 Task 都要用到；③ "`PERMISSION_CATALOG` 含 `|` 未加引号"——v0.4.4 修复并验证，**确认解决**；④ "多行 PEM 会截断"（Task 1 发现）——v0.4.4 只修了一半，反馈回去之后 brickKit 出了 v0.4.5 把根因（`readDotEnv`）也重写了，自己编译 v0.4.5 二进制、跑通它的全部单测、原样复现最初场景+`openssl` 校验密钥合法性，**确认彻底解决**；⑤ "`casdoorBaseUrl` 假设 Docker 网络"（Task 1 发现）——**确认按建议做成文档，问题解决**。三份反馈文档全部处理完毕并已删除，没有遗留。这次经历也验证了"不直接采信对方的'已修复'说法，自己重新编译验证"这条自查习惯是有效的——如果第一轮就直接信了 v0.4.4 的回复，会把一个实际只解决一半的问题当成已关闭。

### Task 1c：依赖方旧、被依赖方新

**组件参与清单**：`erp-sales`（人为切到上一个 tag 的源码，裸进程）+ `mdm-customer`（最新版本，裸进程）+ `mdm-product`（最新版本，裸进程，陪跑用，同 Task 1 的发现）+ `infra-iam-casdoor`+`infra-authz`（最新版本，裸进程，鉴权面陪跑用，同 Task 1 的发现），其余不参与。

**测试方法**：
- [x] `erp-sales` 组件仓库 `git checkout` 到上一个版本 tag，`go run` 起来；`mdm-customer`、`mdm-product`、`infra-iam-casdoor`、`infra-authz` 都保持最新版本，`go run` 起来。五者手动喂各自需要的环境变量。

**代表性沟通测试**：用 Task 1 同样方式拿到的真实 JWT，`erp-sales` 旧版本裸进程正常发起 `CreateOrder` 调用 `mdm-customer` 新版本裸进程，确认响应完全符合旧版本的预期（新增字段被忽略，不影响解析）——验证"新版本发布不影响老调用方"这条契约铁律（设计书 §3.4 Iron Rule 3：只加不减不改）在纯裸进程环境下依然成立。

**结果**（2026-09-17 真机执行，版本快照：`erp-sales@v1.0.24`（人为切到上一个 tag 源码）、`mdm-customer@1.0.9`、`mdm-product@1.0.10`、`infra-iam-casdoor@1.0.9`、`infra-authz@1.0.7`，均为最新。本条延续 Task 1b 的正确流程：`local:true` 改动保留到测完，直接 `source` brickkit 生成的文件，不誊抄）：

1. **构造这条版本错配的具体方式，跟 Task 1b 不是简单对称**：`erp-sales` 组件仓库 `git checkout v1.0.24`（`git tag --sort=-v:refname` 确认上一个 tag，改动是"删掉 deployment.labels 的 prometheus.io/port"，无业务/契约变更），但 v1.0.24 自己的 `component.yaml` 依赖声明写的是 `mdm/customer@1.0.8`/`mdm/product@1.0.9`（它自己当时的最新版）——这是 Task 1b 已经踩过的同一个坑：如果不改这两行，brickKit 会为了满足这条旧声明，额外真的起一个 `mdm-customer@1.0.8` 容器，erp-sales 会被路由过去，而不是我准备好的 `1.0.9` 裸进程。临时把这两行改成 `mdm/customer@1.0.9`/`mdm/product@1.0.10`（注释标记临时），这次才是真的构造出"依赖方旧代码、指向新部署的依赖方"这个场景，不是"依赖方旧代码、指向另一个同样旧的依赖方"。
2. **这次还额外撞见一个新变体：连带依赖也要对齐，否则会多起不相干的旧容器**：`erp-sales@v1.0.24` 自己没改过的另外两条依赖边（`erp/inventory@1.0.16`、`erp/finance@1.0.12`）跟当前 `go-core` 外壳里实际在跑的版本（`1.0.17`/`1.0.13`）不一致，`brickkit up` 因此又要多起两个不在这次测试范围内的旧版本容器（这两个不参与 Task 1c，纯粹是 erp-sales 旧 manifest 的历史包袱）。把这两行也临时对齐到 `1.0.17`/`1.0.13`（跟 go-core 外壳当前实际版本一致）之后，就不再触发这两个多余容器——这两个组件本来就不在 Task 1c 的代表性沟通测试范围内（`CreateOrder` 用不到它们），对齐版本纯粹是为了让 `brickkit up` 的拓扑保持干净，不是测试内容本身。
3. **还撞见第三个连带效应：`infra-bff-mobile` 对 `erp/sales@1.0.25`（未修改的最新版）的弱依赖边，会让最新版 erp-sales 也被多起一份**：因为 `infra-bff-mobile` 完全没被这次测试触碰，它的 manifest 仍然声明依赖 `erp/sales@1.0.25`，导致 brickKit 除了我要测的 `erp-sales@1.0.24`（local:true）之外，还要再单独起一个 `erp-sales@1.0.25` 容器去满足这条边——而这个额外节点因为没有对应的顶层 `config:` 块，直接报"缺少必填配置 defaultWarehouseId"生成失败。因为 `infra-bff-mobile` 本来就不在 Task 1c 的组件参与清单里，临时给它加 `enabled: false`（注释标记临时）之后问题消失，`brickkit up` 最终干净地只起 4 个外壳 + `frontend-standard` 共 5 个容器。
4. **意外发现：Task 1b 里"这些绑定不会注入任何真实连接串"的说法是错的，已经在这补充更正**——这次因为要用 `postgres-go-core`/`postgres-go-infra` 满足 local:true 的资源绑定校验（同 Task 1b 的必要操作），生成的 `local-debug.*.env` 里这次**真的带上了 `DATABASE_HOST`/`DATABASE_NAME`/`DATABASE_PASSWORD`/`DATABASE_PORT`/`DATABASE_USER`（`shell_go_core`/`shell_go_infra`，外壳自己的共享登录角色）/`MQ_HOST`/`MQ_PORT`**，不再需要像 Task 1/1b 那样手工额外补一份。用这套自动生成的凭据（`shell_go_core`/`shell_go_infra`，不是 `postgres` 超级用户）直接跑 `./build/migrate up`，五个组件全部成功（因为迁移在之前的 Task 里已经跑到最新，这次是幂等空转，没有实测到这套共享角色是否真的有权限执行 DDL——这一点留到以后哪个 Task 真的需要从零建表时再验证）。
5. **鉴权面地址仍然需要手工纠正，跟 Task 1/1b 一致**：`erp-sales`/`mdm-customer`/`mdm-product` 生成文件里的 `IAM_JWKS_URL`/`AUTHZ_BUNDLE_URL` 还是指向 Docker 服务名（因为这两个字面量写在各自 `config:` 里，不会因为对方是 `local:true` 就自动改写，这是已确认的既有限制），手工改成 `localhost` 之后正常。`infra-iam-casdoor`/`infra-authz` 之间的互相引用本来就是 `127.0.0.1` 字面量（这两个组件设计上总是同处一个外壳，字面量从一开始就是回环地址，跟 local:true 无关），不需要改。
6. **代表性调用成功**：`dev.superuser` 真实 JWT → `POST /erp/sales/orders`（`customer_id=211`、`product_id=4`）打给 `erp-sales@v1.0.24` 裸进程 → `HTTP 200`，`id=457`、`order_no=SO457`，`customer_name`/`product_name`/`product_sku`/`unit_price`/`tax_rate` 全部正确回填，`brickkit_db.erp_sales.sales_orders where id=457` 核实落库一致。因为 `mdm-customer`/`mdm-product` 这两级版本跳跃（1.0.8→1.0.9、1.0.9→1.0.10）都是"无业务行为/契约变更"，这次同样是预期内的"平淡"成功，没有触发任何新增字段解析问题——**这本身也是"契约铁律真的落实了"的一种验证方式**：如果这几个版本之间真的有契约变更却没人发现，这条链路的行为会跟预期不一致，而它没有。
7. **收尾**：五个进程按端口 `lsof -ti` 结束；`brickkit down` 停掉这次的 5 个容器；`brickkit.yaml`、`components/erp/sales/component.yaml`、`.brickkit/manifests/erp-sales-1.0.24.yaml` 全部 `git checkout` 复原；`erp-sales` 子模块 `git checkout v1.0.25` 切回最新版本；`git status`/`git submodule status` 确认干净。

**结论**：Task 1c 通过。业务结论跟 Task 1b 对称——"erp-sales 旧版本调用 mdm-customer/mdm-product 新版本"在这几级相邻版本之间完全兼容，契约铁律在裸进程环境下同样成立。但这条任务真正的产出是方法论：**构造"依赖方旧、被依赖方新"这类版本错配场景，不是简单把 Task 1b 的步骤反过来做**——依赖方一旦切换到旧源码，它自己 manifest 里所有依赖边（不只是这次要测的那一条）都会变成历史快照，任何跟当前实际拓扑不一致的边都可能触发额外的、不在测试范围内的容器，甚至因为缺配置直接生成失败；此外，*其他未参与测试的组件*对被替换成旧版本的那个组件的依赖边（这次是 `infra-bff-mobile` 对 `erp/sales@1.0.25` 的依赖）同样会造成连带的额外节点。构造这类测试时要检查**全部**相关方向的依赖边，不能只看"我要测哪两个组件之间的关系"这一条。另外确认了 Task 1b 一处记录有误：local:true 的资源绑定并非"不注入真实连接串"，绑定之后 brickKit 会用外壳自己的共享登录角色自动注入完整的 `DATABASE_*`/`MQ_*`，本条起可以直接依赖这一点，不用再手工补。

---

## Task 2：纯独立组件 × Docker

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"），全部独立部署（`brickkit.yaml` 里没有任何 `servedBy`/`local: true`）——这是唯一一个"没有任何组件被排除"的任务，因为这正是"纯独立组件"这个拓扑的完整定义。版本上是变体 a / baseline：全部组件都用当前锁定的最新版本。

**测试方法**：
- [x] `make check` + `brickkit up` + `make tier0`/`make tier1` 全绿。这是阶段一到四的默认状态，复核不重做。

**代表性沟通测试**：`erp-sales` → `mdm-customer` 真实强依赖 gRPC 调用（阶段三附录 E 已验证过的链路，这里在"全部独立部署"这个已知状态下复核一次），外加 `frontend-standard`/`infra-bff-mobile` 打一次真实 GraphQL 请求确认前端到后端的整条链路通。

**结果**（2026-09-17 真机执行，版本快照：真实全部 14 个组件——`mdm/customer`@1.0.9、`mdm/product`@1.0.10、`erp/inventory`@1.0.17、`erp/finance`@1.0.13、`erp/sales`@1.0.25、`infra/authz`@1.0.7、`infra/iam-casdoor`@1.0.9、`infra/notification`@1.0.4、`infra/workflow`@1.0.4、`integration/im-dingtalk`@1.0.5、`crm/opportunity`@1.0.12、`infra/print`@1.0.7、`infra/bff-mobile`@1.0.19、`frontend/standard`@1.0.0，镜像全部本机已有、未重新构建）：

1. **部署方式**：没有手工一个个改 `servedBy`，用了项目自带的 `make teardown-up`（内部调用 `infra/scripts/patch-teardown-bindings.py` 打临时资源绑定/`expose:true`/禁用 4 个外壳条目，再 `brickkit up --ignore-served-by`）——这正是这条机制原本设计要覆盖的场景，比手工编排更贴近"项目自己会怎么做"。`brickkit up` 一次成功，14 个容器全部 `running (healthy)`，无需重试。
2. **`make check`**：5 个基础资源（postgres/nats/traefik/casdoor/rustfs）全部 healthy。
3. **`make tier0`**：6 项全绿。本机一开始没装 `grpcurl`，`Test档0_3` 先 SKIP，`go install github.com/fullstorydev/grpcurl/cmd/grpcurl@latest` 补装后重跑，6/6 真正全绿，不是带着一项跳过收尾。
4. **`make tier1`**：28 项全绿（当前实际断言数比 AGENTS.md 表格里记的"23 条"多，这条表格该更新了，留给下次touch这份文档时一并处理，不在本任务范围内展开）。
5. **代表性沟通测试——业务侧，比原计划测得更深**：不只 `CreateOrder`，额外做了一次 `ConfirmOrder` 走完整 TCC 链。第一次拿 `product_id=4`（`Task 1` 系列一直用的那个）库存不足，真实报 `预留库存失败: ... 库存不足`——这是**真实数据缺口**（这个产品在 `erp-inventory` 里从来没有过库存记录），不是系统故障，本身就证明 `erp-inventory` 的 `Reserve` 是真的被调用了、真的按库存判定失败。换一个真实有货的产品（`product_id=195`，`on_hand_qty=192`）重新建单+确认，`ConfirmOrder` 真实成功（`reservation_id=22`，`status=CONFIRMED`），直接查 `erp_inventory.inventory_reservations` 核实 `order_id=459`/`product_id=195`/`qty=3`/`status=RESERVED` 真实落库——`erp-sales → mdm-customer → mdm-product → erp-inventory` 这条强依赖链在"全部独立部署"这个拓扑下端到端真实打通，比 Task 1/1b/1c 只测到 `CreateOrder`（不碰库存）更完整。
6. **代表性沟通测试——前端侧，发现一个真实的项目现状缺口，不是部署问题**：`infra-bff-mobile` 的 `/graphql` 端点本身可达（`/healthz` 200），但它启用了 `persisted_queries_only`（只认预先在构建期注册好的查询哈希），`contracts/persisted-operations.json` 现在还是空的 `{}`；`frontend/standard` 仓库里逐字搜索 `*.graphql` 一个文件都没有。真机验证：裸查询、甚至 `__schema` introspection 查询，全部被拒绝，报 `PersistedQueryOnly`（`CANNOT_SEND_PQ_ID_AND_BODY`），fail-closed 行为符合设计（这条限制本来就是"没有合法查询就什么都不该放行"），但**"前端到后端真实打一次 GraphQL 请求"这件事，在项目当前状态下不可能做到，因为压根没有已发布的合法查询可用**——这跟部署/环境无关，是 `frontend-standard` 尚未真正对接 `infra-bff-mobile` 这个更早的项目进度缺口。这条代表性沟通测试改成"不适用，原因如上"，不强行伪造一个查询哈希去凑一个"成功"。
7. **收尾**：`make teardown-down`（`brickkit down` + `git checkout -- brickkit.yaml`），`docker ps` 确认只剩 5 个基础资源容器，`git status` 干净。

**结论**：Task 2 通过，而且是目前 05b 里业务侧验证得最完整的一条——TCC 链条真的走到了 `erp-inventory` 的 `Reserve`（含一次真实的"库存不足"负向反馈 + 一次真实的成功预留），不再停留在 `CreateOrder` 这一步。同时发现一个跟本次拓扑测试无关、但值得记下来的真实项目缺口：`infra-bff-mobile` 的 persisted-operations 机制目前无法被真实调用，因为 `frontend-standard` 还没有发布任何查询——这不是 Task 2 的失败，是"这条链路还没建成"的诚实记录，后续任何涉及 `infra-bff-mobile` 真实 GraphQL 调用的 Task（5/6/10/11/12 等）预计都会撞到同一个限制，除非在那之前 `frontend-standard` 补上了这部分工作。

### Task 2b：依赖方新、被依赖方旧

**组件参与清单**：`erp-sales`（新版本容器）+ `mdm-customer`（人为锁定到上一个 tag 的镜像），其余 12 个组件（上方"本轮验证的真实 14 个组件"表格里除 `erp-sales`/`mdm-customer` 之外的全部 12 个：`mdm/product`、`erp/inventory`、`erp/finance`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`）保持独立部署、正常最新版本陪跑（保证整体拓扑仍然是"全部独立部署"，不引入合并变量）。

**测试方法**：
- [x] `brickkit.yaml` 把 `mdm-customer` 的版本临时改成上一个 tag，`erp-sales` 保持最新，`brickkit up` 真机起两者的独立容器，确认都能正常启动并通过健康检查。

**代表性沟通测试**：`erp-sales` 容器调用 `mdm-customer` 旧版本容器一个双方版本都有的方法，确认成功；如果 `erp-sales` 用到了 `mdm-customer` 尚未实现的字段，记录具体报错文案，这是预期内的失败。

**结果**（2026-09-17 真机执行，版本快照：`erp-sales@1.0.25`（源码不变，仅临时改依赖声明）+ `mdm-customer@1.0.8`（沿用本机已有镜像），其余 12 个组件独立部署、最新版本）：

1. **构造方式沿用 Task 1b 的教训，但这次更简单**：不需要 `git checkout` 任何源码（Docker 容器测试用的是镜像，不是裸进程跑源码），只临时把 `components/erp/sales/component.yaml` 的 `dependencies.components: mdm/customer@1.0.9` 改成 `mdm/customer@1.0.8`（本机已有 `brickenterprise/mdm-customer:1.0.8` 镜像，不需要重新构建）；`brickkit.yaml` 顶层的 `mdm/customer` 条目完全没动，继续声明 `1.0.9`。用 `make teardown-up` 一次性把 14 个组件全部还原成独立部署（同 Task 2 的方式）。
2. **brickKit 的解析结果完全符合 Task 1b 建立的预期**：`brickkit up` 自动多起了一个 `mdm-customer-1-0-8` 容器（15 个容器而不是 14 个），依赖图清楚显示 `erp/sales@1.0.25 → mdm/customer@1.0.8`，而 `crm/opportunity@1.0.12`/`infra/bff-mobile@1.0.19` 仍然指向 `mdm/customer@1.0.9`——两个精确版本节点在同一次部署里真实并存，互不干扰（`mdm-customer-1-0-9` 保留原来的宿主机端口映射，`mdm-customer-1-0-8` 只在 Docker 内部网络可达，没有 `expose: true`，因为它是自动多出来的节点，不在 `patch-teardown-bindings.py` 原来扫描到的 servedBy 成员列表里）。直接 `docker exec` 到 `erp-sales` 容器里 `echo $MDM_CUSTOMER_ENDPOINT`，确认真实值是 `http://mdm-customer-1-0-8:8080`，不是猜测。
3. **代表性调用成功**：真实 JWT → `POST /erp/sales/orders`（`customer_id=211`、`product_id=195`）→ `HTTP 200`，订单 `id=460`/`order_no=SO460`，`customer_name` 正确回填——`erp-sales`（新）真实通过 Docker 内部网络调用了 `mdm-customer`（旧）容器，不是本地裸进程，这条链路第一次在 Docker 真机层面验证过。
4. **收尾**：`make teardown-down`（`brickkit down` 停掉 15 个容器 + `git checkout -- brickkit.yaml`），`components/erp/sales/component.yaml`、`.brickkit/manifests/erp-sales-1.0.25.yaml` 一并 `git checkout` 复原，`docker ps` 确认只剩 5 个基础资源容器，`git status` 干净。

**结论**：Task 2b 通过。"依赖方新、被依赖方旧"这个版本错配场景在 Docker 独立部署层面真实成立，而且比 Task 1b 的裸进程版本更简单——不需要碰任何源码/镜像构建，只改一行 manifest 依赖声明就能让 brickKit 自动算出正确的多版本共存拓扑。这也是第一次在 Docker 真机层面亲眼确认"同一次部署里，一个组件的两个精确版本可以真实同时跑着，各自服务各自的调用方"，跟 brickKit 自己的单元测试结论一致。

### Task 2c：依赖方旧、被依赖方新（最常见的真实场景）

**组件参与清单**：`erp-sales`（人为锁定到上一个 tag 的镜像）+ `mdm-customer`（最新版本容器），其余 12 个组件（`mdm/product`、`erp/inventory`、`erp/finance`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`）保持独立部署、正常最新版本陪跑。

**测试方法**：
- [x] `brickkit.yaml` 把 `erp-sales` 的版本临时改成上一个 tag，`mdm-customer` 保持最新，`brickkit up` 真机起两者的独立容器。

**代表性沟通测试**：`erp-sales` 旧版本容器正常调用 `mdm-customer` 新版本容器，确认响应完全符合旧版本的预期（新增字段被忽略，不影响解析）——验证**新版本发布不影响老调用方**这条契约铁律真的在 Docker 真机层面成立。

**结果**（2026-09-17 真机执行，版本快照：`erp-sales@1.0.24`（沿用本机已有镜像，容器本身就是旧版本，不是像 Task 2b 那样只改依赖声明）+ `mdm-customer@1.0.9`/`mdm-product@1.0.10`（最新），其余 10 个组件独立部署、最新版本，`infra-bff-mobile` 本次临时 `enabled: false` 不参与，理由见第 2 点）：

1. **发现一个新机制：非当前 checkout 版本的清单，brickKit 读的是 `.brickkit/manifests/<id>-<version>.yaml` 归档快照，不是子模块工作区**——这次 `erp-sales` 子模块工作区仍然停在 `v1.0.25`（没有 `git checkout` 到旧 tag，Docker 测试不需要源码），只把 `brickkit.yaml` 顶层 `erp/sales` 的 `version` 改成 `1.0.24`，`brickkit up --dry-run` 直接就正确解析出了 `erp/sales@1.0.24 → mdm/customer@1.0.8、mdm/product@1.0.9、erp/inventory@1.0.16、erp/finance@1.0.12`——这四个依赖版本跟子模块当前工作区（v1.0.25）里写的完全不一样，只可能来自 `.brickkit/manifests/erp-sales-1.0.24.yaml` 这份 git 追踪的历史归档文件。这是一个此前没有真机验证过的真实机制，值得记住：**Docker 场景下要把一个组件的容器锁定到某个旧版本，不需要动它的 git 子模块，只需要 `brickkit.yaml` 顶层版本号对应的镜像 tag 和 `.brickkit/manifests/` 归档都存在**。
2. **跟 Task 1c 一样撞见同一类连带效应，处理方式相同**：`erp-sales@1.0.24` 的归档清单里 `mdm/customer@1.0.8`/`mdm/product@1.0.9`/`erp/inventory@1.0.16`/`erp/finance@1.0.12` 都是历史快照版本，如果不改会让 brickKit 多起四个不在这次测试范围内的旧容器（`mdm-customer@1.0.8` 本身就是这次特意要避开的、我们**不想**测的旧版本）。临时把 `.brickkit/manifests/erp-sales-1.0.24.yaml`（不是子模块的 `component.yaml`，因为这次读的就是这份归档文件）里的四条依赖对齐到当前实际版本（`1.0.9`/`1.0.10`/`1.0.17`/`1.0.13`，注释标记临时）。同时 `infra-bff-mobile` 对 `erp/sales@1.0.25`（未被改动的顶层默认版本）的弱依赖边，又会导致多起一个缺配置报错的 `erp-sales@1.0.25` 幽灵节点——原因、现象跟 Task 1c 一模一样，同样临时 `enabled: false` 掉 `infra-bff-mobile` 解决，它因此没有参与这次的组件清单。
3. **一次操作失误，如实记录**：`make teardown-up` 要求 `brickkit.yaml` 开始前必须是 git 干净状态，而我把"改 `erp-sales` 版本 + 禁用 `infra-bff-mobile`"这两步跟 `teardown-up` 自己要打的补丁写在了同一次改动里，导致 `teardown-up` 直接拒绝执行。改成先在干净状态下跑一次 `make teardown-up`（把其余 12 个组件变成独立部署），再在它已经改好的 `brickkit.yaml` 基础上追加我自己那两处改动，`brickkit up` 重新生成——**这次疏忽还牵出第二个真实教训**：`teardown-up` 打完补丁后的 `brickkit.yaml` 里，`servedBy: shell/go-core@0.5.4` 这类字面量其实还留在文件里（只是运行时靠 `--ignore-served-by` 在内存里当它不存在），所以后续任何一次在这个临时状态上再跑 `brickkit up` 都必须带上 `--ignore-served-by`，忘记带这个 flag 会报"servedBy 指向的外壳当前没有在运行"（因为 4 个外壳这时候已经被 `enabled: false` 关掉了）——这条本身不是 bug，是 `--ignore-served-by` 设计上"只影响这一次调用，不改文件"的必然结果，但很容易在同一个 teardown 会话里做多次 `brickkit up` 时忘记。
4. **代表性调用成功**：真实 JWT → `POST /erp/sales/orders`（`customer_id=211`、`product_id=195`）打给 `erp-sales@1.0.24` 容器 → `HTTP 200`，`id=461`/`order_no=SO461`，`customer_name`/`product_name` 全部正确回填；`docker exec` 进容器核实 `MDM_CUSTOMER_ENDPOINT=http://mdm-customer-1-0-9:8080`、`MDM_PRODUCT_ENDPOINT=http://mdm-product-1-0-10:8082`，确认真实指向最新版本容器，不是猜测。
5. **收尾**：`make teardown-down`（`brickkit down` 停掉 13 个容器 + `git checkout -- brickkit.yaml`），`.brickkit/manifests/erp-sales-1.0.24.yaml` 单独 `git checkout` 复原（`teardown-down` 不会管这个文件，需要自己记得），`docker ps` 确认只剩 5 个基础资源容器，`git status` 干净。

**结论**：Task 2c 通过，业务结论跟 Task 1c 对称——契约铁律在 Docker 真机层面同样成立。这条任务的真正价值是发现了一个全新的、此前文档里完全没提过的机制：**`.brickkit/manifests/<id>-<version>.yaml` 是"声明一个非当前 checkout 版本"时 brickKit 实际读取依赖/配置的地方**，Docker 场景下测版本错配，动的应该是这份归档文件（或者子模块被 checkout 到对应 tag 时的 `component.yaml`，两者殊途同归，看子模块工作区当前停在哪个版本），而不是想当然地以为"改 `brickkit.yaml` 顶层版本号"就完事——这条经验对后面所有 Docker/K8s 层面的版本错配 Task（3b/3c 等）都适用。附带发现 `--ignore-served-by` 是纯运行时开关，同一个 teardown 会话里每次 `brickkit up` 都要带上，不会因为跑过一次就自动记住。

---

## Task 3：纯独立组件 × K8s

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"），全部独立部署——同 Task 2，K8s 下同样没有理由排除任何组件。版本上是变体 a / baseline：全部组件用当前锁定的最新版本。

**测试方法**：
- [ ] 确认本机 `minikube` 可用。
- [ ] `brickkit.yaml` 的 `deploy.target` 临时改成 `k8s`（没有 CLI 覆盖参数），全部组件保持独立部署（不涉及 `servedBy`）。
- [ ] `brickkit up` 真实 `apply` 到 `minikube`，确认全部 14 个组件的 Deployment/Service/迁移 Job 真实起来、`kubectl get pods` 全部 `Running`，健康检查真实通过。
- [ ] 收尾清理，`git checkout -- brickkit.yaml` 恢复。

**代表性沟通测试**：`erp-sales` → `mdm-customer` 真实强依赖链，集群内部真实发一次请求确认可达。

**结果**：_(待执行——这是此前从未在本项目做过的真机验证，此前只有 `--dry-run` 级别，记录真实耗时供以后参考)_

**结论**：_(待执行)_

### Task 3b：依赖方新、被依赖方旧

**组件参与清单**：`erp-sales`（新版本 Deployment）+ `mdm-customer`（人为锁定到上一个 tag 的镜像/Deployment），其余 12 个组件（`mdm/product`、`erp/inventory`、`erp/finance`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`）保持独立部署陪跑。

**测试方法**：
- [ ] K8s 环境下，`mdm-customer` 的 Deployment 镜像临时改成上一个 tag，`erp-sales` 保持最新，`kubectl get pods` 确认两者都 `Running`。

**代表性沟通测试**：`erp-sales` Pod 调用 `mdm-customer` 旧版本 Pod 一个双方版本都有的方法，确认成功；用到了对方没有的字段则记录报错。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 3c：依赖方旧、被依赖方新

**组件参与清单**：`erp-sales`（人为锁定到上一个 tag 的镜像/Deployment）+ `mdm-customer`（最新版本），其余 12 个组件（`mdm/product`、`erp/inventory`、`erp/finance`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`）保持独立部署陪跑。

**测试方法**：
- [ ] K8s 环境下，`erp-sales` 的 Deployment 镜像临时改成上一个 tag，`mdm-customer` 保持最新。

**代表性沟通测试**：`erp-sales` 旧版本 Pod 调用 `mdm-customer` 新版本 Pod，确认响应符合旧版本预期，验证契约铁律在 K8s 环境下同样成立。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 4：纯外壳 × 本地

**组件参与清单**：11 个 Go 组件 + `infra-print`，`frontend-standard`/`infra-bff-mobile` **不参与**（结构性地不可能被合并）。挑其中一个外壳（`go-core`，收编 `mdm-customer`/`mdm-product`/`erp-inventory`/`erp-finance`/`erp-sales`）验证"外壳作为裸进程运行"这件事本身，不需要 4 个外壳同时起。版本上是变体 a / baseline：外壳裸进程运行的代码和手动模拟的每个成员版本声明全部对应最新版本。

**测试方法**：
- [ ] 不用 `go test`，真的用 `go run ./cmd/shell` 在宿主机上起 `go-core` 外壳进程，手动设置 `BRICKKIT_SERVED_MEMBERS_CONFIG`（模拟平台会注入的值，`configEnvVars` 是变量名不是值，手动模拟时要记得同时手动 `export` 那些变量名对应的真实值）+ 真实 `TEST_PG_DSN`/`TEST_NATS_URL`，确认外壳长期存活、`/healthz` 正常、按数据里的成员列表只实例化了对应模块。

**代表性沟通测试**：外壳内某个模块（`erp-sales`）真实响应一次外部请求，确认合并进这个裸进程之后业务逻辑依然正常。

**结果**：_(待执行——验证的是"外壳作为一个长期存活的宿主机进程，且真的按平台注入的数据做了按需装配"这件事本身，不是 `go test` 进程生命周期内跑一段代码)_

**结论**：_(待执行)_

### Task 4b：镜像升级了，但某个成员声明还指向旧版本

**组件参与清单**：`go-core` 外壳裸进程运行最新源码，手动构造的 `BRICKKIT_SERVED_MEMBERS_CONFIG` 里 `mdm-customer` 这一项的 `version` 字段写成上一个 tag。

**测试方法**：
- [ ] 手动编辑本地模拟的 `BRICKKIT_SERVED_MEMBERS_CONFIG` JSON，把 `mdm-customer` 的 `version` 改成旧 tag，但 `go run ./cmd/shell` 运行的仍是外壳仓库当前最新源码（源码里 import 的 `mdm-customer` 模块包是当前工作区里的最新代码，不存在"镜像只编译了一个版本"这个 Docker 特有的约束）。观察外壳启动日志：`moduleRegistry` 按 componentId 查表，`version` 字段只是数据，不影响装配哪一份代码——真机确认"裸进程下声明的旧版本号会不会被拿来做任何校验，还是纯粹被忽略"。

**代表性沟通测试**：调用 `mdm-customer` 模块的一个端点，确认它实际服务的是工作区里的最新代码行为（不受声明的旧版本号字符串影响）。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 4c：外壳镜像本身没升级，但想升级某个成员

**组件参与清单**：`go-core` 外壳裸进程运行**上一个 git tag** 的源码（`git checkout` 到旧 tag 再 `go run`），手动构造的 `BRICKKIT_SERVED_MEMBERS_CONFIG` 里 `mdm-customer` 声明成一个比这个旧 tag 更新的版本。

**测试方法**：
- [ ] 外壳仓库 `git checkout` 到上一个版本 tag，`go run ./cmd/shell`，手动构造的成员配置声明 `mdm-customer` 为最新版本。因为外壳源码本身没有升级（`internal/shell/real_modules_test.go` 里注册的 `mdm-customer` 模块 import 路径/版本是旧 tag 时锁定的），观察实际装配出来的模块是旧代码还是报错。

**代表性沟通测试**：调用 `mdm-customer` 模块的一个端点，用能区分新旧版本行为差异的探测方法，确认实际服务的版本。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 4d：同一个外壳收编同一组件的两个不同版本

**组件参与清单**：`go-core` 外壳裸进程，手动构造的 `BRICKKIT_SERVED_MEMBERS_CONFIG` 里放两条 `mdm-customer` 记录（一条旧版本、一条新版本，各自不同端口）。

**测试方法**：
- [ ] 手动构造这份两版本共存的 JSON，`go run ./cmd/shell` 启动，观察 `buildModules` 是否真的能同时装配 `mdm-customer` 两个版本各自的 `ModuleSpec`（各自的 `HTTPPort` 不冲突）。**如果撞上"同一个 Go 进程装不下同一 Go module 两个版本"这个语言层面的限制（两个版本的 `mdm/customer/backend/module` 包 import 路径完全相同，Go 编译器/`go.mod` 层面就不允许），这条任务的产出就是确认这个限制在裸进程场景下具体的报错形式，不算任务失败**。

**代表性沟通测试**：如果两个版本真的都装配成功，分别调用各自的端口，确认互不干扰；如果撞上限制，改成记录编译/启动阶段的具体报错。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 4e：两个不同外壳各自收编同一组件的不同版本

**组件参与清单**：`go-core` 外壳裸进程收编 `mdm-customer@<旧版本>`，`go-backoffice` 外壳裸进程（另一个终端窗口）收编 `mdm-customer@<新版本>`，两个外壳进程同时跑在宿主机上（各自绑定不同端口）。

**测试方法**：
- [ ] 两个终端窗口分别 `go run ./cmd/shell`，`SHELL_NAME` 分别设成 `go-core`/`go-backoffice`，各自手动构造的 `BRICKKIT_SERVED_MEMBERS_CONFIG` 里 `mdm-customer` 声明不同版本，确认两个裸进程各自独立监听、互不冲突。

**代表性沟通测试**：分别调用两个外壳进程各自收编的 `mdm-customer` 端点，确认返回的行为对应各自声明的版本，互不覆盖。

**结果**：_(待执行——brickKit 自己完全没有测试或文档覆盖这个场景，是真正的未知领域)_

**结论**：_(待执行)_

---

## Task 5：纯外壳 × Docker（`servedBy` 落地后的核心场景）

**组件参与清单**：11 个 Go 组件 + `infra-print`，**全部** `servedBy` 收编进各自的 4 个外壳，`frontend-standard`/`infra-bff-mobile` 临时从 `brickkit.yaml` 摘掉（或 `enabled: false`）——这次测的是"零独立组件、100% 合并"这个此前从未真正验证过的极端场景，不是平时跑着的那份真实 `brickkit.yaml`（那份留给 Task 10 测）。版本上是变体 a / baseline：4 个外壳镜像和全部 12 个成员的 `servedBy` 版本声明都是当前锁定的最新版本。

**测试方法**：
- [ ] `brickkit up`，确认 4 个外壳容器 + 12 个成员的地址正确路由（不生成独立容器）+ 网络别名生效 + `depends_on` 正确指向外壳，且**没有任何独立组件容器**（跟真实拓扑的 6 容器不同，这次应该是 4 容器）。
- [ ] 确认 `expose`/`exposePort` 这类字段写在 `servedBy` 成员上确实不生效（只有警告），不要因为"没报错"就误以为生效了。
- [ ] 确认迁移顺序是 `BRICKKIT_SERVED_MEMBERS_CONFIG` 数组的 componentId 字典序，不是 `brickkit.yaml` 里 servedBy 的声明顺序（05a 已经修正过失实注释，这里做一次真机确认：4 个外壳分别 `docker logs` 确认迁移执行顺序跟字典序一致，且顺序不影响任何一个模块的正确性）。
- [ ] `make tier0`/`make tier1`/`make tier2` 全绿（⚠️ 这三个 make 目标目前假设的是真实 6 容器拓扑，这次是 4 容器的缩减拓扑，跑之前确认是不是需要临时调整判据，不能想当然直接跑）。

**代表性沟通测试**：跨外壳依赖（`go-core`/`go-backoffice` 调 `go-infra` 暴露的 `authzBundleUrl`/`iamJwksUrl`）在这份缩减拓扑下依然可达。

**结果**：_(待执行——这是"外壳合并部署"这件事第一次真的测到"零独立组件"这个极端，是本文档里分量最重的一条任务)_

**结论**：_(待执行)_

### Task 5b：镜像升级了，但某个成员声明还指向旧版本

**组件参与清单**：同 Task 5 缩减清单，`brickkit.yaml` 里 `mdm-customer` 的 `servedBy` 声明版本人为写成上一个 tag，但外壳镜像本身（`brickenterprise/be-shell-go:x.x.x`）是当前最新构建、只编译了新版本代码。

**测试方法**：
- [ ] 真机构造这个声明与镜像不一致的场景，`brickkit up`，观察 brickKit 的实际行为：是生成阶段就报错（版本不存在于镜像内），还是生成成功但运行时该成员实际跑的是镜像里编译进去的那个版本（声明被忽略）。**这条事先不能只靠猜**——镜像通常只编译一份代码，"声明旧版本"这件事本身在合并部署下的语义就模糊，真机验证的产出恰恰是把这层模糊说清楚。

**代表性沟通测试**：如果生成/启动成功，调用 `mdm-customer` 确认它实际服务的是哪个版本的行为（用一个两版本间有明确行为差异的字段/方法探测）。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 5c：外壳镜像本身没升级，但想升级某个成员

**组件参与清单**：同 Task 5 缩减清单，外壳镜像锁定在一个旧的 `be-shell-go` tag（`brickkit.yaml` 的 `shell/go-core` 条目 `version`/`image` 都指向旧 tag），`mdm-customer` 的 `servedBy` 声明指向一个比这个镜像构建时更新的版本。

**测试方法**：
- [ ] 真机构造，`brickkit up`，确认这就是"外壳要不要单独发版"这件事的真实测试——外壳镜像本身的构建产物（`Dockerfile`/`go.mod` 锁定的各组件依赖版本）决定了它实际能跑哪些版本，`brickkit.yaml` 的声明如果超出镜像构建时的范围，应该在生成阶段就能查出来还是要等运行时才暴露，记录实际行为。

**代表性沟通测试**：同 5b，调用 `mdm-customer` 确认实际服务版本。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 5d：同一个外壳收编同一组件的两个不同版本

**组件参与清单**：`mdm-customer` 准备两个版本（当前最新版 + 上一个 tag），都 `servedBy` 收编进同一个外壳（`go-core`），其余 10 个 Go 组件 + `infra-print` 正常按 Task 5 baseline 收编。

**测试方法**：
- [ ] `brickkit.yaml` 里给 `go-core` 追加一条 `mdm-customer@<旧版本>` 的 `servedBy` 声明（跟已有的 `mdm-customer@<新版本>` 共存），`brickkit up`。brickKit 自己有单测佐证（`TestResolveGroupsTwoVersionsOfSameComponentUnderOneShell`），但那是隔离 fixture，不是真实组件——真机确认地址、端口、迁移都正确，且两个版本不会撞上"同一个 Go 二进制装不下同一 module 两个版本"这个已知的结构性限制。**如果真的撞上这个限制，这条任务的产出就是确认限制成立，记录具体报错，不算任务失败**。

**代表性沟通测试**：分别调用两个版本各自的端点，确认互不干扰。

**结果**：_(待执行——这条已知风险是 Go 编译期限制，Docker 镜像形态下预计比裸进程更容易真的撞上，因为镜像构建时锁死了具体依赖版本)_

**结论**：_(待执行)_

### Task 5e：两个不同外壳各自收编同一组件的不同版本

**组件参与清单**：`mdm-customer@<旧版本>` 被外壳 `go-core` 收编，`mdm-customer@<新版本>` 被外壳 `go-backoffice` 收编，其余组件正常按各自外壳收编。

**测试方法**：
- [ ] `brickkit.yaml` 里 `go-core` 的 `servedBy` 声明 `mdm-customer@<旧版本>`，`go-backoffice` 的 `servedBy` 声明 `mdm-customer@<新版本>`，`brickkit up`。brickKit 自己完全没有测试或文档覆盖，是真正的未知领域。真机构造，确认能不能正常生成、两边地址是否正确隔离、有没有意外的交叉污染。**这条如果真机验证通过，值得单独写一份"回复给 brickKit"的文档**——这是连他们自己都没验证过的场景，我们的结果对他们也是新信息。

**代表性沟通测试**：分别从两个外壳各自调用自己收编的那个版本，确认互不干扰、地址不会被对方覆盖。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 6：纯外壳 × K8s（此前不可能，现在是原生能力）

⚠️ **这条任务本身就是这次 `servedBy` 落地带来的最大变化**——手写 K8s 清单模拟已经作废，现在可以真的用 `brickkit up` 原生生成。

**组件参与清单**：同 Task 5——11 个 Go 组件 + `infra-print` 全部合并，`frontend-standard`/`infra-bff-mobile` 不参与。版本上是变体 a / baseline：全部最新版本。

**测试方法**：
- [ ] `deploy.target` 临时改 `k8s`，`brickkit.yaml` 用 Task 5 那份缩减拓扑。
- [ ] `brickkit up` 真实 `apply` 到 `minikube`：确认外壳自己的 Deployment/Pod 健康；确认每个成员生成了正确的 Service（`selector` 指向外壳 Pod 标签，`targetPort` 是成员自己的端口）；确认依赖某个成员的其它组件，Pod 内能通过成员的 Service 名正常访问到。
- [ ] **重点验证跨外壳依赖在 K8s 下的地址解析**——Docker 侧这条已经真机触发验证过（05a Task 0.4/0.5），K8s 侧完全没测过，是当前 12 个 baseline 任务里唯一的空白：`go-core`/`go-backoffice` 所在 Pod 调 `go-infra` 暴露的 `authzBundleUrl`/`iamJwksUrl`，确认走的是 K8s Service 名而不是 Docker 网络别名，地址格式、可达性都要真机确认。
- [ ] 确认 NetworkPolicy（如果本项目已经启用）的 ingress/egress 规则跟着 `servedBy` 关系正确调整。
- [ ] 收尾清理。

**代表性沟通测试**：一条跨外壳的真实强依赖调用（同上），加一条外壳内部模块间调用确认合并进程内部的路由也正常。

**结果**：_(待执行——这条任务成立，直接证明"外壳 + K8s"这个此前完全不可能的组合，现在是 brickKit 原生支持的能力，是整个 05b 里最值得写进 Task 15 出档说明的一条结论)_

**结论**：_(待执行)_

### Task 6b：镜像升级了，但某个成员声明还指向旧版本

**组件参与清单**：同 Task 6，`mdm-customer` 的 `servedBy` 声明版本人为写成上一个 tag，`go-core` 的 K8s Deployment 镜像是当前最新构建。

**测试方法**：
- [ ] 真机构造这个"`servedBy` 声明版本与外壳镜像实际编译内容不一致"的场景：`brickkit.yaml` 里 `mdm-customer` 的 `servedBy` 声明版本写成上一个 tag，`go-core` 的 K8s Deployment 用的镜像是当前最新构建（镜像里只编译了 `mdm-customer` 最新版本的代码）。`deploy.target: k8s`，`kubectl apply`，观察 brickKit 的实际行为：是生成阶段就报错（版本不存在于镜像内），还是生成成功但运行时该成员实际跑的是镜像里编译进去的那个版本（声明被忽略）。**这条事先不能只靠猜**——镜像通常只编译一份代码，"声明旧版本"这件事本身在合并部署下的语义就模糊，真机验证的产出恰恰是把这层模糊说清楚。

**代表性沟通测试**：调用 `mdm-customer` 对应的 K8s Service，确认实际服务的版本行为。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 6c：外壳镜像本身没升级，但想升级某个成员

**组件参与清单**：同 Task 6，`go-core` 的 K8s Deployment 镜像锁定在旧 tag，`mdm-customer` 的 `servedBy` 声明指向比这个镜像更新的版本。

**测试方法**：
- [ ] 真机构造这个"外壳镜像本身没升级，但想升级某个成员"的场景：`go-core` 的 K8s Deployment 镜像锁定在一个旧的 `be-shell-go` tag（镜像构建时编译进去的是 `mdm-customer` 旧版本代码），`brickkit.yaml` 的 `mdm-customer` `servedBy` 声明改成一个比这个旧镜像构建时更新的版本。`kubectl apply`，观察实际装配出来的模块是旧代码还是直接报错——这就是"外壳要不要单独发版"这件事的真实测试：外壳镜像本身的构建产物（`Dockerfile`/`go.mod` 锁定的各组件依赖版本）决定了它实际能跑哪些版本，`brickkit.yaml` 的声明如果超出镜像构建时的范围，应该在生成阶段就能查出来还是要等运行时才暴露，记录实际行为。

**代表性沟通测试**：调用对应 Service，确认实际服务版本。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 6d：同一个外壳收编同一组件的两个不同版本

**组件参与清单**：`mdm-customer` 两个版本都 `servedBy` 收编进同一个外壳（`go-core`），K8s 部署，其余组件正常按 Task 6 baseline 收编。

**测试方法**：
- [ ] `brickkit.yaml` 给 `go-core` 追加一条 `mdm-customer@<旧版本>` 的 `servedBy` 声明，`deploy.target: k8s`，`brickkit up` 真实 `apply`。确认每个版本各自生成了独立的 K8s Service（`targetPort` 不同），且不会撞上"同一个 Go 二进制装不下同一 module 两个版本"的限制（如果撞上，记录 Pod 启动失败的具体报错）。

**代表性沟通测试**：分别通过两个版本各自的 Service 调用，确认互不干扰。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 6e：两个不同外壳各自收编同一组件的不同版本

**组件参与清单**：`mdm-customer@<旧版本>` 被外壳 `go-core` 收编，`mdm-customer@<新版本>` 被外壳 `go-backoffice` 收编，K8s 部署，其余组件正常按各自外壳收编。

**测试方法**：
- [ ] `brickkit.yaml` 两个外壳各自声明不同版本的 `mdm-customer`，`deploy.target: k8s`，`brickkit up` 真实 `apply`。**重点确认 K8s 侧的地址解析机制（Service+Selector）跟 Docker 侧（网络别名）不同，两边是否都能正确隔离两个版本**——这是唯一一个"不同环境下代码路径本身就不一样"的多版本场景，Task 5e 已经在 Docker 侧测过，这里在 K8s 侧独立复现整个构造过程，不能只测一边就下结论。

**代表性沟通测试**：分别通过两个外壳各自的 K8s Service 调用各自收编的版本，确认互不干扰。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 7：纯独立组件 × Docker+本地混合

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"），其中 `mdm-customer` 临时 `local: true` 拉到宿主机，其余 13 个（含 `erp-sales`）继续在 Docker 独立部署。版本上是变体 a / baseline：全部组件最新版本。

**测试方法**：
- [ ] 全部组件独立部署（同 Task 2），额外把 `mdm-customer` 临时改成 `local: true` 拉到宿主机调试，其余组件继续在 Docker 里跑。确认两者共存、地址互通。

**代表性沟通测试**：Docker 里的 `erp-sales` 真实调用这个 `local:true` 拉出来的 `mdm-customer`，确认 `host.docker.internal` 这条地址路径在真实业务调用下没问题（不只是端口能连）。

**结果**：_(待执行——复核既有机制，不是新发现)_

**结论**：_(待执行)_

### Task 7b：依赖方新、被依赖方旧

**组件参与清单**：`mdm-customer`（`local: true`，人为锁定到上一个 tag 的源码）+ `erp-sales`（Docker 容器，最新版本），其余组件按 Task 7 baseline 独立部署。

**测试方法**：
- [ ] `mdm-customer` 组件仓库 `git checkout` 到上一个 tag，`local: true` 跑在宿主机；`erp-sales` 保持最新版本 Docker 容器。

**代表性沟通测试**：`erp-sales` 新版本容器调用 `mdm-customer` 旧版本本地进程，确认双方版本都有的方法调用成功，用到对方没有的字段则记录报错。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 7c：依赖方旧、被依赖方新

**组件参与清单**：`mdm-customer`（`local: true`，最新版本）+ `erp-sales`（Docker 容器，人为锁定到上一个 tag 的镜像），其余组件按 Task 7 baseline 独立部署。

**测试方法**：
- [ ] `mdm-customer` 保持最新版本 `local: true`；`erp-sales` 的 Docker 镜像临时改成上一个 tag。

**代表性沟通测试**：`erp-sales` 旧版本容器调用 `mdm-customer` 新版本本地进程，确认响应符合旧版本预期——验证契约铁律在 Docker+本地混合环境下同样成立。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 8：纯外壳 × Docker+本地混合

**组件参与清单**：11 个 Go 组件 + `infra-print`（4 个外壳全部收编，同 Task 5 缩减清单），额外把 `infra-bff-mobile`（本来就没有合并资格，天然是"完全独立"的组件）临时 `local: true` 拉到宿主机——这样既满足"额外一个完全独立的组件用 `local:true`"的定义，又不需要临时改动 12 个成员的合并关系。`frontend-standard` 不参与。版本上是变体 a / baseline：4 个外壳与 12 个成员声明全部最新版本，`infra-bff-mobile` 也是最新版本。

**测试方法**：
- [ ] Task 5 的拓扑基础上，把 `infra-bff-mobile` 从"独立 Docker 容器"改成 `local: true` 拉到宿主机运行。确认 4 个外壳容器照常运行，`infra-bff-mobile` 的本地调试进程能正常被外壳容器访问到，`servedBy` 那条路径不受干扰。

**代表性沟通测试**：`infra-bff-mobile` 本地进程真实调用某个外壳内的模块（比如 `go-core` 收编的 `mdm-customer`），确认合并态+本地调试混合场景下地址解析正常。

**结果**：_(待执行——复核 brickKit 自己 `TestLocalStillWorksAlongsideServedBy` 这条回归测试在真实组件上依然成立)_

**结论**：_(待执行)_

### Task 8b：镜像升级了，但某个成员声明还指向旧版本

**组件参与清单**：同 Task 8，`mdm-customer` 的 `servedBy` 声明版本人为写成上一个 tag，外壳镜像是当前最新构建，`infra-bff-mobile` 仍然 `local: true`。

**测试方法**：
- [ ] 真机构造这个"`servedBy` 声明版本与外壳镜像实际编译内容不一致"的场景：`brickkit.yaml` 里 `mdm-customer` 的 `servedBy` 声明版本人为写成上一个 tag，4 个外壳镜像本身是当前最新构建（镜像里只编译了 `mdm-customer` 最新版本代码），同时保留 `infra-bff-mobile` 的 `local:true` 状态不变。`brickkit up`，观察 brickKit 的实际行为：是生成阶段就报错，还是生成成功但运行时该成员实际跑的是镜像里编译进去的版本（声明被忽略）——并确认这层"额外多一个本地调试组件"的状态不会改变这个行为本身。

**代表性沟通测试**：`infra-bff-mobile` 本地进程调用 `mdm-customer`，用一个两版本间有明确行为差异的字段/方法探测，确认实际服务的是哪个版本的行为。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 8c：外壳镜像本身没升级，但想升级某个成员

**组件参与清单**：同 Task 8，外壳镜像锁定旧 tag，`mdm-customer` 声明指向更新版本，`infra-bff-mobile` 仍然 `local: true`。

**测试方法**：
- [ ] 真机构造这个"外壳镜像本身没升级，但想升级某个成员"的场景：外壳镜像锁定在一个旧的 `be-shell-go` tag（镜像内编译的是 `mdm-customer` 旧版本代码），`brickkit.yaml` 的 `mdm-customer` `servedBy` 声明指向一个比这个旧镜像更新的版本，同时保留 `infra-bff-mobile` 的 `local:true` 状态。`brickkit up`，观察实际装配出来的模块是旧代码还是报错，并确认额外的本地调试状态不影响这个结论。

**代表性沟通测试**：`infra-bff-mobile` 本地进程调用 `mdm-customer`，用能区分新旧版本行为差异的探测方法，确认实际服务的版本。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 8d：同一个外壳收编同一组件的两个不同版本

**组件参与清单**：`mdm-customer` 两个版本都收编进 `go-core`，其余组件按 Task 8 baseline 部署，`infra-bff-mobile` 仍然 `local: true`。

**测试方法**：
- [ ] 真机构造"同一个外壳收编同一组件两个不同版本"的场景：`brickkit.yaml` 里给 `go-core` 追加一条 `mdm-customer@<旧版本>` 的 `servedBy` 声明（跟已有的 `mdm-customer@<新版本>` 共存，各自不同端口），其余组件按 Task 8 baseline 收编，同时保留 `infra-bff-mobile` 的 `local:true` 状态。`brickkit up`，确认地址、端口、迁移都正确，且两个版本不会撞上"同一个 Go 二进制装不下同一 module 两个版本"的结构性限制（**如果真的撞上，产出就是确认限制成立、记录具体报错，不算任务失败**），并确认两层机制（多版本共存 + 本地调试）同时存在时互不干扰。

**代表性沟通测试**：`infra-bff-mobile` 本地进程分别调用 `mdm-customer` 的两个版本端点，确认都能访问、互不干扰。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 8e：两个不同外壳各自收编同一组件的不同版本

**组件参与清单**：`mdm-customer@<旧版本>` 被 `go-core` 收编，`mdm-customer@<新版本>` 被 `go-backoffice` 收编，`infra-bff-mobile` 仍然 `local: true`。

**测试方法**：
- [ ] 真机构造"两个不同外壳各自收编同一组件不同版本"的场景：`brickkit.yaml` 里 `go-core` 的 `servedBy` 声明 `mdm-customer@<旧版本>`，`go-backoffice` 的 `servedBy` 声明 `mdm-customer@<新版本>`，同时保留 `infra-bff-mobile` 的 `local:true` 状态。`brickkit up`，确认能不能正常生成、两边地址是否正确隔离、有没有意外的交叉污染，brickKit 自己完全没有测试或文档覆盖这个场景，是真正的未知领域。

**代表性沟通测试**：`infra-bff-mobile` 本地进程分别调用两个外壳各自收编的版本，确认互不干扰。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 9：组件+外壳混搭 × 本地

✅ **"本地"的口径**：跟"Docker+本地混合"是并列的独立一档，不是它的子集——这次任务里**外壳和独立组件全部跑成宿主机裸进程**，不借助 `local:true`（那个机制假设其它组件在 Docker 里跑着，属于 Task 12 的范畴），不碰 Docker。

**组件参与清单**：`go-core` 外壳（裸进程，收编 `mdm-customer`/`mdm-product`/`erp-inventory`/`erp-finance`/`erp-sales`）+ `infra-bff-mobile`（完全独立，裸进程），其余组件不参与——不需要全部 14 个同时跑成裸进程，这里的重点是"合并态+独立态混着跑、且都不碰 Docker"这件事本身。版本上是变体 a / baseline：两者都用最新版本。

**测试方法**：
- [ ] 起 `go-core` 外壳：`go run ./cmd/shell` 在宿主机上跑，手动设置 `BRICKKIT_SERVED_MEMBERS_CONFIG`（模拟平台会注入的值，`configEnvVars` 是变量名不是值，手动模拟时要记得同时手动 `export` 那些变量名对应的真实值）+ 真实 `TEST_PG_DSN`/`TEST_NATS_URL`。
- [ ] 起 `infra-bff-mobile`：宿主机直接 `npm run dev`（或等价命令），手动喂环境变量（模拟平台会注入的值，`brickkit up --dry-run` 生成的 `local-debug.*.env` 可以复用来拿到需要喂的环境变量清单），不通过 `brickkit up` 拉起，不进 Docker。
- [ ] 两者共存，确认都能正常启动。

**代表性沟通测试**：`infra-bff-mobile` 裸进程真实调用 `go-core` 外壳裸进程内的 `mdm-customer` 模块，确认"合并态裸进程" ↔ "独立态裸进程"这条此前完全没测过的路径可达——这条地址解析不能走 Docker 网络别名，需要确认手动喂的地址（`localhost:<port>` 之类）真的对。

**结果**：_(待执行——重点记录"手动模拟平台注入"这一步具体是怎么做的)_

**结论**：_(待执行)_

### Task 9b：外壳同步了，独立组件没同步

**组件参与清单**：`go-core` 外壳裸进程运行最新代码，`infra-bff-mobile` 裸进程人为切到上一个 tag 的源码。

**测试方法**：
- [ ] `infra-bff-mobile` 组件仓库 `git checkout` 到上一个 tag，裸进程跑起来；`go-core` 外壳保持最新代码裸进程。

**代表性沟通测试**：`infra-bff-mobile` 旧版本裸进程调用 `go-core` 外壳内已升级的模块，确认协作正常，契约层面（GraphQL schema/REST 契约只加不减）没有破坏旧调用方。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 9c：独立组件同步了，外壳没同步

**组件参与清单**：`go-core` 外壳裸进程人为切到上一个 tag 的源码，`infra-bff-mobile` 裸进程运行最新代码。

**测试方法**：
- [ ] `shells/go` 仓库 `git checkout` 到上一个 tag，`go run ./cmd/shell`；`infra-bff-mobile` 保持最新代码裸进程。

**代表性沟通测试**：`infra-bff-mobile` 最新版本裸进程调用外壳内旧版本模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 9d：极端场景——多个独立组件各自依赖同一个下游组件的不同版本

**组件参与清单**：临时把 `mdm-customer` 从 `go-core` 摘出来，改成两个裸进程独立实例：`mdm-customer@<旧版本>` 和 `mdm-customer@<新版本>`（各自不同端口）；`infra-bff-mobile`（裸进程，手动配置依赖 `mdm-customer@<旧版本>` 的地址）+ 临时从外壳摘出来单独裸进程运行的 `crm-opportunity`（手动配置依赖 `mdm-customer@<新版本>` 的地址）——这是专门为这条极端场景临时构造的拓扑，不是常规部署形态。

**测试方法**：
- [ ] 明知这个场景在真实开发中不合理、且应该被避免（版本管理会非常混乱），但仍然真机构造一次：两个 `mdm-customer` 裸进程实例同时监听不同端口，`infra-bff-mobile` 和 `crm-opportunity` 分别手动配置指向不同的那个实例地址。因为都是裸进程手动指定地址，这条本身不会报错（跟 brickKit 的 `servedBy` 冲突检测无关，纯粹是手动配置各连各的）——**这条任务的产出是确认"裸进程手动配置"这条路径本身没有任何机制阻止这种混乱局面**，跟 Task 10d（Docker/`brickkit.yaml` 生成层面的真实约束）形成对比。

**代表性沟通测试**：`infra-bff-mobile` 和 `crm-opportunity` 分别调用各自配置的 `mdm-customer` 实例，确认两条链路互不干扰、但也确认系统本身不会主动提示"你依赖了同一个下游的两个不同版本，这不合理"。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 9e：一个版本合并进外壳、另一个版本完全独立部署

**组件参与清单**：`mdm-customer@<新版本>` 被 `go-core` 外壳裸进程收编，`mdm-customer@<旧版本>` 作为完全独立的裸进程运行（同 Task 1 的裸进程方式）。

**测试方法**：
- [ ] `go-core` 外壳裸进程手动构造的 `BRICKKIT_SERVED_MEMBERS_CONFIG` 收编 `mdm-customer@<新版本>`；另起一个 `mdm-customer` 旧版本裸进程，独立监听另一个端口。brickKit 自己 `shell-implementers-guide` 文档明确描述过这个场景（"老调用方留在旧版本、独立部署，新调用方切到新版本、合并部署，两者不需要任何协调"），但只是文档转述，本项目从没真机验证过。

**代表性沟通测试**：一个依赖旧版本独立部署的调用方（裸进程）、一个依赖新版本合并部署的调用方（外壳内模块），分别调用各自锁定的版本，确认两条路径互不干扰、不需要任何额外协调。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 10：组件+外壳混搭 × Docker

⚠️ **跟 Task 5 不是同一件事**——Task 5 用的是缩减清单（零独立组件），这里用真实全量清单，这是本项目**当前唯一的真实部署形态**，`brickkit up` 平时跑的就是这份拓扑。

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"）——12 个 `servedBy` 收编进 4 个外壳，`frontend-standard`/`infra-bff-mobile` 独立。版本上是变体 a / baseline：`brickkit.yaml` 是 05a 迁移后的真实状态，全部最新版本。

**测试方法**：
- [ ] `brickkit up`，确认 6 容器（4 个外壳 + 2 个独立）全部 `running (healthy)`，`make tier0`/`make tier1`/`make tier2` 全绿——这条本质是日常在跑的状态，做一次干净的完整记录，是把"真实项目到底长什么样、能不能跑通"这件事正式写进本文档。

**代表性沟通测试**：`frontend-standard` → `infra-bff-mobile` → 后端（跨外壳）完整链路真实打通一次。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 10b：外壳同步了，独立组件没同步

**组件参与清单**：Task 10 本体的真实全量清单，其中外壳 `go-infra`（收编了 `infra-authz`/`infra-iam-casdoor`）已经升级到最新，但独立组件 `infra-bff-mobile` 人为锁定到上一个 tag。

**测试方法**：
- [ ] `infra-bff-mobile` 的镜像临时改成上一个 tag，`go-infra` 保持最新，`brickkit up`。真机构造，确认这类"外壳先动、独立组件没跟上"的真实升级场景不会导致协作失败——尤其是契约层面（`.proto`/`events/*.json` 只加不减不改这条铁律）。

**代表性沟通测试**：`infra-bff-mobile` 旧版本调用已升级外壳内的模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 10c：独立组件同步了，外壳没同步

**组件参与清单**：Task 10 本体的真实全量清单，反过来——`infra-bff-mobile` 升级到最新，外壳 `go-infra` 保持上一个 tag 的镜像。

**测试方法**：
- [ ] `go-infra` 的镜像临时改成上一个 tag，`infra-bff-mobile` 保持最新，`brickkit up`。真机构造，确认反过来的方向同样不出问题。

**代表性沟通测试**：已升级的 `infra-bff-mobile` 调用外壳内旧版本模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 10d：极端场景——多个独立组件各自依赖同一个下游组件的不同版本

**组件参与清单**：临时把 `mdm-customer` 从 `go-core` 摘出来，改成两个独立容器：`mdm-customer@<旧版本>`、`mdm-customer@<新版本>`；再临时把 `crm-opportunity` 从 `go-backoffice` 摘出来改成独立容器，跟 `infra-bff-mobile` 一起分别依赖这两个不同版本——这是专门为这条极端场景临时构造的拓扑，不是常规部署形态。

**测试方法**：
- [ ] `brickkit.yaml` 里让 `infra-bff-mobile` 依赖 `mdm-customer@<旧版本>`、`crm-opportunity` 依赖 `mdm-customer@<新版本>`，两个 `mdm-customer` 版本都作为独立组件声明（不参与 `servedBy`）。明知这个场景在真实开发中不合理、且应该被避免（版本管理会非常混乱），但仍然真机构造一次，`brickkit up --dry-run` 先看生成阶段的行为——确认 brickKit 的真实行为是直接报错拒绝生成（两个组件对同一个 componentId 的不同版本各自要求生成一份注入结果，可能在 `*_ENDPOINT` 变量命名上直接冲突），还是悄悄选一个版本让所有依赖方共用。

**代表性沟通测试**：不适用于常规意义上的"确认协作正常"——这条任务本身测的就是"这种混乱局面下系统会怎么表现"，记录实际行为（报错文案，或者悄悄选中的版本+选择逻辑）本身就是产出。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 10e：一个版本合并进外壳、另一个版本完全独立部署

**组件参与清单**：`mdm-customer@<新版本>` 被 `go-core` 外壳收编（servedBy），`mdm-customer@<旧版本>` 作为完全独立容器部署，其余组件按 Task 10 baseline 正常部署。

**测试方法**：
- [ ] `brickkit.yaml` 里 `go-core` 的 `servedBy` 声明 `mdm-customer@<新版本>`，同时新增一条独立的 `mdm-customer@<旧版本>` 组件条目（不带 `servedBy`），`brickkit up`。brickKit 自己 `shell-implementers-guide` 文档明确描述过这个场景，但只是文档转述，本项目从没真机验证过——这恰好是最贴近真实客户升级场景的组合。

**代表性沟通测试**：一个依赖旧版本独立容器的调用方、一个依赖新版本合并容器的调用方，分别调用各自锁定的版本，确认两条路径互不干扰、不需要任何额外协调。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 11：组件+外壳混搭 × K8s

⚠️ **跟 Task 6 不是同一件事**，原因同 Task 10——这里用真实全量清单。

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"）——12 个 `servedBy` 收编进 4 个外壳，`frontend-standard`/`infra-bff-mobile` 独立，跟 Task 10 是同一份真实拓扑，只是这次部署目标是 K8s。版本上是变体 a / baseline：全部最新版本。

**测试方法**：
- [ ] `deploy.target` 临时改 `k8s`，`brickkit.yaml` 是真实混搭拓扑（12 个组件 `servedBy`，2 个独立）。`brickkit up` 真实 `apply`，确认全部 Pod 健康、地址解析正常。

**代表性沟通测试**：`frontend-standard` → `infra-bff-mobile` → 后端跨外壳完整链路，这次在 K8s 环境下打通一次。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 11b：外壳同步了，独立组件没同步

**组件参与清单**：同 Task 11，`go-infra` 的 K8s Deployment 镜像最新，`infra-bff-mobile` 的 K8s Deployment 镜像锁定上一个 tag。

**测试方法**：
- [ ] 真机构造这个"外壳先升级、独立组件没跟上"的场景：`brickkit.yaml` 是真实混搭拓扑（12 个组件 `servedBy`，2 个独立），`go-infra` 的 K8s Deployment 镜像升级到最新（收编了 `infra-authz`/`infra-iam-casdoor` 的最新代码），`infra-bff-mobile` 的 K8s Deployment 镜像人为锁定到上一个 tag。`deploy.target: k8s`，`kubectl apply`，确认这类"外壳先动、独立组件没跟上"的真实升级场景不会导致协作失败——尤其是契约层面（`.proto`/`events/*.json` 只加不减不改这条铁律）在 K8s 环境下依然成立。

**代表性沟通测试**：`infra-bff-mobile` 旧版本 Pod 调用已升级外壳内的模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 11c：独立组件同步了，外壳没同步

**组件参与清单**：同 Task 11，反过来——`infra-bff-mobile` 最新，`go-infra` 的 K8s Deployment 镜像锁定上一个 tag。

**测试方法**：
- [ ] 真机构造反过来的方向：`brickkit.yaml` 是真实混搭拓扑，`infra-bff-mobile` 的 K8s Deployment 镜像升级到最新，外壳 `go-infra` 的 K8s Deployment 镜像人为锁定到上一个 tag。`deploy.target: k8s`，`kubectl apply`，确认"独立组件先动、外壳没跟上"这个方向在 K8s 环境下同样不出问题。

**代表性沟通测试**：已升级的 `infra-bff-mobile` Pod 调用外壳内旧版本模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 11d：极端场景——多个独立组件各自依赖同一个下游组件的不同版本

**组件参与清单**：临时把 `mdm-customer` 从 `go-core` 摘出来，改成两个独立 Deployment：`mdm-customer@<旧版本>`、`mdm-customer@<新版本>`；再临时把 `crm-opportunity` 从 `go-backoffice` 摘出来改成独立 Deployment，跟 `infra-bff-mobile` 一起分别依赖这两个不同版本——这是专门为这条极端场景临时构造的拓扑，不是常规部署形态。

**测试方法**：
- [ ] `brickkit.yaml` 里让 `infra-bff-mobile` 依赖 `mdm-customer@<旧版本>`、`crm-opportunity` 依赖 `mdm-customer@<新版本>`，两个 `mdm-customer` 版本都作为独立组件声明（不参与 `servedBy`），`deploy.target: k8s`。明知这个场景在真实开发中不合理、且应该被避免，但仍然真机构造一次，`brickkit up --dry-run` 先看生成阶段的行为——确认 brickKit 的真实行为是直接报错拒绝生成（两个组件对同一个 componentId 的不同版本各自要求生成一份注入结果，可能在 Service 命名/`*_ENDPOINT` 变量命名上直接冲突），还是悄悄选一个版本让所有依赖方共用；并对照本文档 Docker 侧同名场景记录的行为，确认 K8s 生成路径下的具体表现（Service 命名/Selector 生成逻辑）是否一致。

**代表性沟通测试**：不适用于常规意义上的"确认协作正常"——这条任务本身测的就是"这种混乱局面下系统会怎么表现"，记录实际行为（报错文案，或者悄悄选中的版本+选择逻辑）本身就是产出。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 11e：一个版本合并进外壳、另一个版本完全独立部署

**组件参与清单**：`mdm-customer@<新版本>` 被 `go-core` 外壳收编（`servedBy`），`mdm-customer@<旧版本>` 作为完全独立 Deployment 部署，其余组件按 Task 11 baseline 正常部署（12 个组件 `servedBy`，2 个独立）。

**测试方法**：
- [ ] `brickkit.yaml` 里 `go-core` 的 `servedBy` 声明 `mdm-customer@<新版本>`，同时新增一条独立的 `mdm-customer@<旧版本>` 组件条目（不带 `servedBy`），`deploy.target: k8s`，`brickkit up` 真实 `apply`。brickKit 自己 `shell-implementers-guide` 文档明确描述过这个场景，但只是文档转述，本项目从没在 K8s 环境下真机验证过——这恰好是最贴近真实客户升级场景的组合。

**代表性沟通测试**：一个依赖旧版本独立 Pod 的调用方、一个依赖新版本合并 Pod 的调用方，分别调用确认互不干扰。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 12：组件+外壳混搭 × Docker+本地混合

**组件参与清单**：真实全部 14 个组件（`mdm/customer`、`mdm/product`、`erp/inventory`、`erp/finance`、`erp/sales`、`infra/authz`、`infra/iam-casdoor`、`infra/notification`、`infra/workflow`、`integration/im-dingtalk`、`crm/opportunity`、`infra/print`、`infra/bff-mobile`、`frontend/standard`，版本见上方"本轮验证的真实 14 个组件"），`infra-bff-mobile` 临时 `local: true` 拉到宿主机，其余 12 个 `servedBy` 收编进 4 个外壳 + `frontend-standard` 独立部署。版本上是变体 a / baseline：全部最新版本。

**测试方法**：
- [ ] 混搭拓扑（同 Task 10）基础上，把 `infra-bff-mobile` 临时改成 `local: true` 调试。

**代表性沟通测试**：`infra-bff-mobile` 本地进程跟某个外壳内模块（`go-core` 收编的 `mdm-customer`）之间真实调用一次。

**结果**：_(待执行——验证 `infra-bff-mobile` 以 `local:true` 拉到宿主机调试时，跟合并部署在 4 个外壳容器里的模块之间地址解析、请求响应是否都正常，`servedBy` 那条路径不受这层额外本地调试状态干扰)_

**结论**：_(待执行)_

### Task 12b：外壳同步了，独立组件没同步

**组件参与清单**：同 Task 12，`go-infra` 最新版本，`infra-bff-mobile`（`local: true`）人为切到上一个 tag 的源码。

**测试方法**：
- [ ] `infra-bff-mobile` 组件仓库 `git checkout` 到上一个 tag，`local: true` 跑在宿主机；`go-infra` 保持最新容器。

**代表性沟通测试**：`infra-bff-mobile` 旧版本本地进程调用已升级外壳内的模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 12c：独立组件同步了，外壳没同步

**组件参与清单**：同 Task 12，`go-infra` 镜像锁定上一个 tag，`infra-bff-mobile`（`local: true`）最新版本。

**测试方法**：
- [ ] `go-infra` 的镜像临时改成上一个 tag，`infra-bff-mobile` 保持最新版本 `local: true`。

**代表性沟通测试**：已升级的 `infra-bff-mobile` 本地进程调用外壳内旧版本模块，确认协作正常。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 12d：极端场景——多个独立组件各自依赖同一个下游组件的不同版本

**组件参与清单**：临时把 `mdm-customer` 从 `go-core` 摘出来，改成两个独立容器：`mdm-customer@<旧版本>`、`mdm-customer@<新版本>`；再临时把 `crm-opportunity` 从 `go-backoffice` 摘出来改成独立容器，`infra-bff-mobile` 依赖 `mdm-customer@<旧版本>`、`crm-opportunity` 依赖 `mdm-customer@<新版本>`；额外把 `infra-bff-mobile` 改成 `local: true` 拉到宿主机——这是专门为这条极端场景临时构造的拓扑，不是常规部署形态。

**测试方法**：
- [ ] `brickkit.yaml` 里让 `infra-bff-mobile`（`local: true`）依赖 `mdm-customer@<旧版本>`、`crm-opportunity`（独立容器）依赖 `mdm-customer@<新版本>`，两个 `mdm-customer` 版本都作为独立组件声明（不参与 `servedBy`）。明知这个场景在真实开发中不合理、且应该被避免，但仍然真机构造一次，`brickkit up --dry-run` 先看生成阶段行为——确认 brickKit 是直接报错拒绝生成，还是悄悄选一个版本让所有依赖方共用；并确认额外多出的 `local:true` 状态不改变这个生成阶段行为本身。

**代表性沟通测试**：不适用于常规意义上的"确认协作正常"——这条任务本身测的就是"这种混乱局面下系统会怎么表现"，记录实际行为（报错文案，或者悄悄选中的版本+选择逻辑）本身就是产出，额外确认 `local:true` 状态下这条行为是否有任何不同。

**结果**：_(待执行)_

**结论**：_(待执行)_

### Task 12e：一个版本合并进外壳、另一个版本完全独立部署

**组件参与清单**：`mdm-customer@<新版本>` 被 `go-core` 收编，`mdm-customer@<旧版本>` 临时改成 `local: true` 拉到宿主机（而不是普通独立容器）——这是这条场景在"Docker+本地混合"环境下的自然形态：独立部署的那个旧版本恰好就是被临时拉出来本地调试的那个。

**测试方法**：
- [ ] `brickkit.yaml` 里 `go-core` 的 `servedBy` 声明 `mdm-customer@<新版本>`，另一条 `mdm-customer@<旧版本>` 组件条目改成 `local: true`。这是矩阵最深的一层——老调用方留在旧版本、独立部署且本地调试，新调用方切到新版本、合并部署稳定跑着，这是最贴近真实开发者会遇到的组合。

**代表性沟通测试**：本地调试中的旧版本 `mdm-customer` 与合并部署中的新版本 `mdm-customer` 分别被各自的调用方访问，确认 `local:true` 机制在多版本+混搭场景下依然可靠。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 13：`local:true` + K8s 目标的负向验证

⚠️ 本项目/根 `AGENTS.md` 反复引用"`local:true` 只能配 `deploy.target: docker`，K8s 目标下 CLI 生成阶段直接报错"这条结论（Decision 88），但本项目从来没有真机验证过，只是转述文档/决策记录。

**组件参与清单**：1 个组件（`mdm-customer`）足够——这条测的是校验逻辑本身，不需要真实拓扑陪跑。

**测试方法**：
- [ ] 把 `mdm-customer` 改成 `local: true`，`deploy.target` 改成 `k8s`，跑 `brickkit up --dry-run`，确认真的在生成阶段直接报错（不是警告，不是生成了错误的清单再在 apply 阶段失败），记录报错的具体文案和错误码。

**代表性沟通测试**：不适用，本任务测的是失败本身。

**结果**：_(待执行)_

**结论**：_(待执行)_

---

## Task 14：`servedBy` 静态校验的负向验证

⚠️ `servedBy` 有 6 条静态校验（格式/自引用/链式嵌套/与 `local:true` 互斥/同一 `(id,version)` 不能同时独立部署又被 `servedBy` + 目标存在性/运行态 2 条生成期校验），brickKit 自己都有单测，但本项目自己的真实组件上从没构造过。其中"目标不存在"/"目标未运行"两项 05a Task 0.6 迁移时其实已经真机撞见过（改 `servedBy` 版本忘了同步），当时没有专门留档，这次补一次干净复现。

**组件参与清单**：1-2 个组件足够，每个子项都是最小化构造。

**测试方法**：
- [ ] **14.1 自引用**：一个组件 `servedBy` 指向自己，确认静态校验直接拦下。
- [ ] **14.2 链式嵌套**：A `servedBy` B，B 自己又 `servedBy` C，确认静态校验直接拦下（外壳不能收编另一个外壳）。
- [ ] **14.3 目标组件不存在**：`servedBy` 指向一个 `brickkit.yaml` 里根本没有的组件 ID，确认报错文案清楚指出"目标不存在"。
- [ ] **14.4 目标外壳未运行**：`servedBy` 指向一个真实存在但被 `enabled: false` 或没有被启动的外壳，确认报错文案清楚指出"外壳没有在运行"（这条 05a Task 0.6 迁移时真机撞见过，这次是补一次干净复现，不是从零发现）。
- [ ] **14.5 同一个 `(id, version)` 不能同时独立部署又被 `servedBy`**：`mdm-customer@<同一精确版本>` 在 `brickkit.yaml` 里同时写一条独立声明和一条被某外壳 `servedBy` 收编的声明，`brickkit up --dry-run`。文档说是"重复声明"这条通用规则的推论，但调研没有找到直接对应的代码位置——真机构造，确认到底是报错还是别的行为。选 `--dry-run` 测，不需要真的起容器：这类校验发生在 manifest 解析/resolve 阶段，比生成部署文件更早，Docker/K8s/本地不会有任何差异。

**代表性沟通测试**：不适用，每一条都是预期失败。

**结果**：_(待执行——记录报错文案和错误码，确认"看这条报错能不能一眼定位到问题"，这才是真正有用的负向验证，不是走个形式)_

**结论**：_(待执行)_

---

## Task 15：出档

**测试方法**：
- [ ] 把 Task 1-14（含全部版本变体）的真机结果整理成一份面向未来部署者的选型说明（扩充 `docs/ops/部署手册.md`），至少覆盖：12 种拓扑×环境基础组合各自的真实可用性/限制、外壳+K8s 现在是原生支持的能力（这是最值得强调的变化）、多版本共存哪些场景成立哪些不成立（含哪些拓扑下的哪些变体真机验证通过、哪些确认了预期的限制）、负向验证覆盖了哪些常见配置错误及其报错文案。
- [ ] 回头检查 `docs/dev/给brickKit的反馈.md` 第七部分和已删除的几份一次性回信文档提到的判断，跟本文档的真实结果是否一致，不一致的地方要么更新反馈文档、要么单独写一份新的回信给 brickKit（尤其是"两个不同外壳各自收编同一组件的不同版本"那条他们自己都没验证过的场景，大概率值得专门写一份）。
- [ ] `git status` 干净，本文档标记为完成。

**结果**：_(待执行)_

**结论**：_(待执行——与本项目一贯的判据一致：真的走一遍上面每一步，不是读代码/读文档猜结论)_
