# 阶段三调研记录（SOP-R R-3 的完整版）

> 用途与 [`02-阶段二.md`](./02-阶段二.md) 完全一致：设计计划 §8 是精炼版，这里是完整版，
> 给"要核对某条判断是否真的查证过"的人看。**一个阶段一份，不往上一份里加**（单份文档不许无限膨胀）。
>
> ⚠️ **本阶段的调研分两轮做，读之前先知道这一点。**
>
> **第一轮（写设计计划时）**：只对 `infra-print` 做了真调研，其余 7 份的 §8 是凭既有认知写的，
> 用 🧭/🔍 标记把"哪些没查过"标了出来。**这是偷工**——把缺口标清楚不等于把缺口补上，
> 而阶段二那 4 份是三个 agent 真读了 ERPNext/Odoo/Tryton/metasfresh 源码才写的，
> **同一个项目里两个阶段的严谨度不该不一致**。
>
> **第二轮（用户指出后补做）**：对全部组件补做了真实资料查证，**结果是四处改了设计**（见下方各节的 ⭐）。
> 这件事本身值得记进阶段三复盘：**SOP-R 不是文档格式要求，它真的会改设计**——
> 四处里有两处（钉钉的异步语义、通知的 critical 概念）如果不查，会在实现期或上线后才炸。
>
> 每条记录仍标三类：
>
> | 标记 | 含义 | 可信度 |
> |---|---|---|
> | 🧭 **架构推理** | 从设计书既有条款 + 本项目已建成组件的先例推导出来的，**不依赖外部源码** | 结论可直接用；错了是我们自己的推理错 |
> | 🔍 **待核实** | 依赖某个外部项目"实际怎么实现"的判断，**目前基于既有知识、尚未对着源码逐行核对** | **开工前必须核对**，对应设计计划 §9 的待决问题 |
| ✅ **已查证** | 第二轮补做时真的查了资料，来源附在条目里 | 可直接用；⭐ 标的是**因此改了设计**的 |
>
> **不许把 🔍 当成 🧭 用。** 阶段二的教训（复盘 §2.1）是"想当然地假设平台行为、实测才发现相反"——
> 外部项目的行为同理，没真的读过就老实标着。

---

## 目录

- [infra-iam-casdoor：适配层与 claims 镜像](#infra-iam-casdoor适配层与-claims-镜像)
- [infra-workflow / infra-notification / integration-im-dingtalk：通知链路三件套](#infra-workflow--infra-notification--integration-im-dingtalk通知链路三件套)
- [infra-print：打印渲染（本阶段唯一真查了外部资料的一节）](#infra-print打印渲染本阶段唯一真查了外部资料的一节)
- [infra-bff-mobile / frontend-standard / crm-opportunity](#infra-bff-mobile--frontend-standard--crm-opportunity)

---

## infra-iam-casdoor：适配层与 claims 镜像

**采纳进**：[`infra-iam-casdoor.md`](../infra-iam-casdoor.md) §1.1（登录方式范围）、§3.2（两个 token）、§3.3（签发方的责任）、§4、§8；
连带改了设计书 §6.1（两句被第 14 章推翻的话）与**附录 D 的登录时序图**。

### 1. 🧭 设计书 §6.1 与第 14 章互相矛盾，而矛盾底下藏着"谁签 token"这个从没被问过的问题

不是文字瑕疵，是两条硬约束顶在一起：

| 约束 | 出处 | 原文大意 |
|---|---|---|
| 认证流不经过适配层 | §6.1 | 浏览器直连 Casdoor 走标准 OIDC |
| JWT 必须带 `roles[]` / `dept_path` / `org_id` | §14.1.5 | 而这些数据在 `infra-authz` 的表里，Casdoor 不知道 |

**关键在于：这两条只有在"Casdoor 直接签业务组件要验的那个 token"这个隐含前提下才矛盾。**
而这个前提从来没有被明写过——它只是**决策 115/116 之前的世界观残留**，留在 §6.1 的一句话
（"JWT 携带权限 Claims"）和附录 D 的一张时序图（"IAM-->>FE: 返回 JWT (含权限 Claims)"）里。

**角色被挪进 `infra-authz` 之后，从来没有人回头问过"那现在谁签这个 token"。** 一旦问出来，
前提就松动了：认证归 Casdoor、签发归适配层，两条约束同时成立（详见 §3）。

⚠️ **我第一版没问出这个问题，而是接受了那个前提、去解它带来的难题**（结果就是下面 §2 那条走错的路）。
**这是本阶段最值得记的方法论教训：当两条约束"看起来必然矛盾"时，先找出那个没被明写的隐含前提。**

### 2. ⛔ 一条走错又被推翻的路：让 Casdoor 直接签带角色的 token

**这一节记的是错误答案，保留它是因为它比正确答案更有教育意义。**

初版设计（本文件与设计计划的第一版）是这样的：Casdoor 仍然直接签发业务组件要验的那个 JWT，
本组件负责把 `infra-authz` 的角色**镜像**进 Casdoor 的用户属性，让它签得出 `roles[]`。为了防止
"authz 改完角色立刻写 `stale_since` → 组件 401 → 前端刷新 → Casdoor 还没同步到、刷回来还是旧角色 → 再 401"
这个**转不出去且服务端零报错**的死循环，还专门设计了一条"等镜像落地确认再写 `stale_since`"的握手
（新增一个 `infra.iam.claims.synced.v1` 事件 + `infra-authz` 多消费一条边）。

**被一句提问推翻**：*"Casdoor 的镜像不是只用来做用户登录么？只要用户登录了，理论上不应该管 Casdoor 事情了吧？"*

回头核对，这条路同时踩了三个独立的否决点，任何一个都足够：

| 否决点 | 说明 |
|---|---|
| **踢人链路是断的** | §14.1.6 要求"踢人 → refresh token 已撤销 → 刷新失败 → 登出"，而 `infra-authz.md` §3 把撤 refresh token 列为 **authz 自己的接口**。token 若由 Casdoor 签，authz 撤不了，除非反过来调 Casdoor——直接违反 authz 设计计划 §5 的"不依赖 iam，方向是反的" |
| **死循环是自找的** | 它整个来自"镜像必然异步"这个前提。换成签发时现问 authz，前提消失，那套握手也就不需要存在了 |
| **把刚拆开的东西粘回去** | §14 把角色放进 authz 图的就是换 Keycloak 时角色数据一行不迁；往 Casdoor 镜像一份等于自毁这条 |

**更要命的是，`infra-authz.md` 早就写了正确答案，是我没对照**：它的 §3 里 `ResolveClaims` 的说明白纸黑字写着
"给 `iam` 适配层：`sub` → `roles[]` / `dept_path` / `org_id`。**登录与刷新时各调一次**"——
"登录时调一次"只有在**适配层站在签发路径上**时才成立。写新组件时没有回头读被依赖方的设计计划，
是这次的直接教训（阶段二复盘 §2.1 记的 `erp-finance` 字段名猜错，根因完全一样）。

### 3. 🧭 正确形态：两个 token，Casdoor 登录完就退出

```
① 浏览器 ↔ Casdoor       纯 OIDC，认证 100% 归它 → 身份 token（无角色）
② 前端 → 适配层 换一次    验 Casdoor 签名 → 调 authz ResolveClaims → 签出应用 token
③ 业务组件               验适配层的 JWKS（iamJwksUrl 指向适配层，不是 Casdoor）
④ 刷新走适配层            重新调 ResolveClaims，角色天然最新
```

**"登录完就不该再管 Casdoor 了"这条直觉是对的**，且它同时解掉了上面三个否决点：踢人由适配层作废自己的
refresh token（authz 通过事件通知）、死循环因为"现问不缓存"而不可能发生、Casdoor 里一个角色字段都不写。

**代价必须写明**（设计计划 §5 也记了）：适配层成为签发方，要管签名密钥、JWKS、密钥轮换、refresh token 存储；
且**登录与刷新这条路上多了 `infra-authz` 这一环**——authz 挂掉时已登录的人做业务不受影响（§14.1.9 fail-static），
但新登录和刷新会失败。这是"角色零延迟、且不可能签出旧角色"换来的账。

### 4. 🔍 Casdoor webhook 的投递保证与签名机制

`webhook_deliveries` 这张去重表的设计（去重键取什么、要不要轮询兜底）取决于 Casdoor 到底给不给：
① 稳定的投递 ID；② 签名头；③ 失败重投。**三个都没确认过。**

**最坏情况的对策已经想好**：如果 webhook 既没有投递 ID 也没有签名，就退化成"定期轮询 Casdoor 的用户列表
+ 按 `updated_time` 增量对账"，`webhook_deliveries` 换成 `user_sync_cursor`。这条退化路径不影响其他任何
组件——`infra.iam.user.*.v1` 事件照发，下游看不出区别。→ 设计计划 §9 第 2 条。

### 5. 🧭 登录方式的范围定了，而它连带决定了"钉钉通知发给谁"怎么解

**范围（已定）**：账号密码注册+登录、微信、QQ、Google。**没有钉钉。**

这四种全部是 Casdoor 侧的配置，适配层零代码——**不管用户用哪种方式认证，Casdoor 产出的都是同一个标准
OIDC 身份 token，适配层根本不知道用户是扫码还是输密码进来的**。以后加飞书/企业微信/LDAP/SAML 同理。

**但"没有钉钉登录"有一个连带后果**：Casdoor 拿不到用户的钉钉身份（unionid），而阶段三的审批通知恰恰要
发钉钉。所以链路只能改成：

```
Casdoor 持有手机号（注册时填 / 管理员录）
  → infra-iam-casdoor 把手机号放进 infra.iam.user.*.v1 事件
  → infra-notification 存进 user_contacts 快照，派发时把手机号给适配器
  → integration-im-dingtalk 用"手机号查 userid"接口换成钉钉 id（它自己缓存）
```

**分工的判据**：手机号是**跨平台通用的自然标识**，钉钉 userid 是**平台特有的**——所以前者可以在链路上传递，
后者必须由对应的适配器自己解析。这条是族级规矩，对后面四个 IM 适配器同样成立
（见 `integration-im-dingtalk.md` §1.1）。

⚠️ 另一半的风险记在 `infra-notification.md` §9 第 2 条：**社交登录注册的用户可能没填手机号**，
那样通知发不出去。对策是记 `SUPPRESSED` + 在待办界面显示"未能通知"，而不是静默失败。

🔍 **待核**：钉钉那个"手机号查 userid"接口的权限要求与频率限制（→ dingtalk 设计计划 §9 第 1 条）。

### 6. 🧭 明确没有参考、且不打算参考的

**Ory Hydra / Zitadel / Authentik 这类"自己实现 OIDC Provider"的项目**：不是没查，是**问题不成立**——
我们不实现 Provider，Casdoor 官方镜像才是 Provider。评估它们等于重新讨论"要不要自己写 IdP"，
那个问题决策 86（零代码的东西不包成组件）与决策 87（JWT 本地验签）早就答完了。

**Casbin**：`infra-authz` 设计计划 §8 已经判过（只看模型分层、不引入）。本组件与它更无关——
我这一层不做任何判定。

---

## infra-workflow / infra-notification / integration-im-dingtalk：通知链路三件套

**采纳进**：[`infra-workflow.md`](../infra-workflow.md)、[`infra-notification.md`](../infra-notification.md)、
[`integration-im-dingtalk.md`](../integration-im-dingtalk.md) 三份的 §1/§3/§4/§8。

> 三份放在一起调研、一起写，因为它们是一条链：workflow 建待办 → notification 决定发给谁走哪个通道 →
> dingtalk 真的调 API。**分开写必然互相猜**——阶段二 `erp-finance` 消费 `sales.order.created.v1` 时字段名
> 猜错（复盘 §2.1），根因就是两份设计计划各写各的。

### 1. 🧭 三条铁律推导出的边界，比"职责划分"更硬

设计书 §6.6 给 workflow 三条禁令（严禁业务规则判断 / 严禁直查业务库 / 严禁反向同步调用业务组件），
§6.9 给适配器一条（严禁业务逻辑）。把它们当公理往下推，很多看起来"可以商量"的设计其实只有一个答案：

| 推论 | 从哪条来 |
|---|---|
| 审批人只能由业务组件算好传进来 | 铁律一：算审批人 = 业务规则 |
| 待办列表要显示的字段只能是**创建时传入的快照** | 铁律二：不能回查业务库 |
| 审批结果只能靠**事件**回给业务组件 | 铁律三：不能反向同步调用 |
| `exception` 任务不能有"重试"按钮 | 铁律三：重试 = 反向调用业务组件 |
| 适配器不能自己重试 | §6.9 + §6.7"重试逻辑不要重复写" |

**这几条我原本以为是设计选择，推完发现全是被约束定死的**——所以 `infra-workflow.md` §1.2 专门列了
"三种看起来很合理、但会让本组件变质的需求"，因为它们每一条单独看都很小、都顺手能做。

### 2. 🧭 通知中心的入口：为什么不给同步 `Notify` rpc

三选一的取舍，最后选了"只走事件"：

| 方案 | 否决/采纳理由 |
|---|---|
| 同步 `Notify` rpc | ❌ `infra-notification` 是 `default` 但可被裁；任何同步调用方都要建依赖边，而**通知发不出去绝不该让业务动作失败**。且 workflow 铁律三本来就不能同步调它——主要来源都走不了同步，再为别人开一个口就是两套幂等两套重试 |
| 通用意图事件（`infra.notification.requested.v1`，谁要通知谁发这条） | ⏸ **暂不做**。它要求所有发布方统一改，而阶段三只有**一个**通知场景（审批待办）。SOP-P 的判据："逻辑本身简单却硬套模式是更糟的结果" |
| 消费来源方自己的领域事件 ✅ | 阶段三只消费 `infra.workflow.task.created.v1` 一条。**泛化的触发点写死在设计计划 §3.1**：来源事件超过 5 条时改成通用意图事件 |

### 3. 🧭 族级 subject 与包名——这是"加一个通道不用改通知中心"的全部机密

`channel:im` 有五个成员，`slot:iam` 有两个。**族内契约面必须完全一致**（§5.11），落到具体就是三条：

| 东西 | 必须是族级 | 反例（写成实现级会怎样） |
|---|---|---|
| proto 包名 | `integration.im.v1` / `infra.iam.v1` | 换通道/换 IdP 时调用方要改 import 路径 |
| 派发 subject | `infra.notification.dispatch.im.v1` | 通知中心要知道装了哪些适配器 = 把装配期知识搬进运行时 |
| 回调 subject | `integration.im.result.v1`（`adapter` 放 payload） | **每加一个适配器都要改通知中心的消费列表**，族就白分了 |

⚠️ **写 `infra-notification.md` 时我第一版把回调写成了 `integration.im.dingtalk.result.v1`（实现级），
写 dingtalk 那份时才发现不对、回头改的。** 记一笔是因为：这种错在单个组件的视角里看不出来，
**只有把族里两份文档摆在一起才暴露**——这正是 SOP-D 要求"跨组件对照着看"的意义。

### 4. 🧭 NATS 是广播，所以适配器必须自己过滤

`besdk.Consume` 用的是 `nc.Subscribe` 不是 `QueueSubscribe`（阶段二踩坑记录 E1 实测确认）。
五个 IM 适配器订同一个 subject → **每个都会收到每一条**。所以每个适配器收到后第一件事是
"看 `target_adapters` 里有没有我，没有就丢弃"。

**漏掉这一步的症状**：装了三个 IM 通道的客户，每条通知收到三遍。⚠️ 这条要写进族级 `AGENTS.md`，
因为它对后面四个成员同样成立，而且**单装一个通道时测不出来**。

### 5. 🧭 `retryable` 划出的分工线

重试策略归通知中心（§6.7），但"这个错误值不值得重试"只有适配器知道（钉钉的错误码是钉钉特有的）。
所以回调事件带一个 `retryable` 布尔：**适配器判"值不值得"，通知中心定"隔多久、几次"**。

两边都判 → 打架（外面 3 次 × 里面 3 次 = 实际 9 次）；都不判 → 给离职员工重试五次。
细则见 `integration-im-dingtalk.md` §4.1 那张错误码表。

### 6. ⭐ 判掉了设计书 §5.11.1 悬着的"审批路由"那一行

§5.11.1 的示例表里，「审批路由」的结论栏一直写着"槽位族信号，**但要先确认它不违反 §6.6**"。
现在 workflow 真的设计出来了，可以确认并判掉：

1. **审批路由根本不在 workflow 里**——"这张单该谁审"由业务组件算好后通过 `assignee` 传入（铁律一）；
2. 所以这条分歧长在 `erp-sales` / `erp-purchase` 这类业务组件内部，而它们**都被 `crm-*`/BFF 建了强依赖边**；
3. 按 §5.11 硬约束 → **不能做成 slot，落点是业务组件的 `customer_fork`**。

与阶段二那 9 条分歧的判定完全一致（复盘 §4 第 5 条）。**已回写设计书 §5.11.1。**

### 7. 🔍 待核实清单（开工前必须做掉）

| # | 要核的 | 影响谁 |
|---|---|---|
| 1 | 钉钉：手机号查 userid 的接口名与权限、工作通知要不要 `agent_id`、access_token 有效期与并发申请限制 | dingtalk §8/§9-1。⚠️ 阶段三计划要求**真调钉钉沙盒**不是 mock，所以要先有一个测试企业 |
| 2 | Novu 的 provider 错误分类是怎么做的 | dingtalk §4.1 那张 `retryable` 表 |
| 3 | Odoo `mail.activity` / ERPNext `ToDo` 的实际表结构 | workflow §2 的表设计 |

⚠️ 这三条都不影响上面 🧭 那些结论的成立——**架构已经可以定，待核的是实现细节**。


---

## infra-print：打印渲染（本阶段唯一真查了外部资料的一节）

**采纳进**：[`infra-print.md`](../infra-print.md) §3.2（两条渲染路径）、§3.3（引擎必须可换）、§8。

> ⚠️ **本节的 ✅ 是真查过的**（用了 web 搜索，来源见下），不是凭印象——因为"市场上到底用不用 HTML 模板"
> 直接决定 §6.11 的选型对不对，而这是用户明确要求先查清楚的一条。

### 1. ✅ 市场主流确实是 HTML 模板，§6.11 的选择成立

| 项目 | 模板形态 | 渲染引擎 |
|---|---|---|
| **ERPNext / Frappe** | **HTML**（Jinja，Print Format） | `wkhtmltopdf`（旧默认）→ **正在迁 Chromium** |
| **Odoo** | **HTML**（QWeb 报表） | `wkhtmltopdf` |
| **Tryton** | 核心是 ODT（relatorio），另有 Webkit Report 扩展走 HTML（Genshi） | LibreOffice / `wkhtmltopdf` |
| **OCA `report_py3o`**（Odoo 生态） | **ODT**（LibreOffice） | LibreOffice |

**结论**：两家最主流的开源 ERP（ERPNext、Odoo）都走 HTML 模板 → 设计书 §6.11 定 HTML 是对的。

### 2. ✅ 一个改变设计的发现：两家的默认引擎正在被换掉

`wkhtmltopdf` **已经多年没有实质更新**，用的是老旧 WebKit，**现代 CSS（Flexbox / Grid / CSS 变量）
基本不支持**，非拉丁字体也常出问题。ERPNext 已经在往 **Chromium** 迁（`print_designer` 从 v1.5.0 起
可选 Chrome 引擎）。

**"渲染引擎会换"因此不是假设，是被两家主流项目正在发生的迁移验证过的事。**
→ 直接推出 `infra-print.md` §3.3：**引擎必须藏在一个内部接口后面**，业务代码不许直接 import WeasyPrint。

**我们的选型**：默认 **WeasyPrint**（纯 Python、pip 装完即用、镜像小，契合"客户本地单机部署"），
**不选 `wkhtmltopdf`**（已停维护），Chromium 作为版式要求高时的 Fork 点。

### 3. ✅ ODT/LibreOffice 是真实存在的第二流派 → Fork 点，不是 slot

`report_py3o` 与 Tryton `relatorio` 的卖点很明确：**模板是 ODT，业务人员用 LibreOffice
所见即所得地改，不需要开发者**。这服务的是"没有 IT 团队的中小客户"——是标准的
"每一种都合理、只是客户画像不同"。

**但按 R-4 第 3 步判：不能做成 slot。** `erp-sales` 等业务组件对 `infra-print` 建了强依赖边
（它的 §5 入边表），§5.11 硬约束明确"有组件依赖它 → 不允许做成 slot"。**落点是 `customer_fork`**。

⚠️ **这正面回答了"将来能不能实现 ODT 那一套"**：能，路径是 Fork 一份换掉 §3.3 那个内部接口的实现，
`Render` 契约一个字不改、调用方无感。已记进 `_可替换性地图.md`。

**来源**：[Frappe 论坛：为什么还在用 wkhtmltopdf](https://discuss.frappe.io/t/why-is-frappe-still-using-wkhtmltopdf-for-pdf-generation/146403) ·
[frappe/print_designer Chrome PDF Generator PR](https://github.com/frappe/print_designer/pull/399) ·
[Odoo QWeb Reports 文档](https://www.odoo.com/documentation/19.0/developer/reference/backend/reports.html) ·
[OCA reporting-engine](https://github.com/OCA/reporting-engine) ·
[Py3o Report Engine](https://apps.odoo.com/apps/modules/14.0/report_py3o)

### 4. 🔍 还没查、但影响实现的

| # | 要核的 | 影响 |
|---|---|---|
| 1 | WeasyPrint 在容器里的中文字体怎么装 | 症状是"开发机好好的、客户机器全是方框"。→ 设计计划 §9 第 1 条 |
| 2 | 斑马 ZPL II 指令集的具体形态 | §3.2 第二条路径。ZPL 是文本指令，不经渲染引擎 |

---

## infra-bff-mobile / frontend-standard / crm-opportunity

**采纳进**：三份设计计划的 §1/§3/§5/§8。

> 这三份的调研含量低于前面几节，**原因不是偷懒，是设计书已经把选择空间锁死了**：
> §6.5 定了 BFF 是 GraphQL、§12.6 把前端技术栈逐格锁定（决策 111/112/113/114）、
> §1.4 定死了 CRM 与 ERP 零同步边。**这里只记真正需要判断的几处。**

### 1. 🧭 BFF 的 schema 手写还是从 proto 生成——这是"BFF 为什么存在"的问题

从 62 份 proto 生成 schema 看起来更"自动"，但生成出来的是**契约的机械并集**：前端要发 5 个查询
才能拼出一个页面，**正好丢掉 §6.5 要的"展示聚合"**。手写才能做到"一个查询喂满一个页面"。

**代价**（后端契约变了要人工同步）**用 CI 检查兜，不靠记性**——`make contract-check` 里加一条
字段比对。已记进设计计划 §3.1 与阶段三计划 Task 11。

### 2. 🧭 DataLoader 必须是 per-request 实例——一条会"悄悄读到别人数据"的路径

DataLoader 的标准用法是每个请求一个实例。做成全局单例会**跨请求缓存命中**，
症状是 **A 用户看到 B 用户的数据**，而**单请求测试永远测不出来**。

这是本项目第 N 条"不报错、无症状"的越权路径（前三条：`SET` 不带 `LOCAL`、模块里 `os.Getenv`、
用户路径用 `SystemClient`）。已写进 `infra-bff-mobile.md` §7。

### 3. 🧭 前端"只判 features 不判 permissions"这条坑，开发时必然测不出来

`可见路由 = features ∩ permissions`（§14.1.8）。**开发时用的管理员账号两个条件都满足**，
所以漏判 permissions 在开发期 100% 测不出来，上线后表现为**菜单看得见、点进去整页 403**。
→ `frontend-standard.md` §3.3。

### 4. 🧭 `crm.opportunity.won.v1` 必须带 `owner_id` / `dept_path`——本阶段最容易漏的字段

`erp-sales` 消费这条事件建单，而**事件 handler 里没有 JWT**（§14.2.6：handler 用系统身份）。
如果事件不带归属，建出来的订单 `owner_id`/`dept_path` 为空：

- `owner_id` 空 → "我的订单"列表里谁都看不到它；
- `dept_path` 空 → `org` 维前缀匹配不上，**除了超管谁都看不见**。

**而且全程不报错**：订单建成了、库存占了、应收也生成了，只是列表里查不到。

⚠️ **这条是照着阶段二的教训主动挖出来的**：复盘 §2.1 记的 `erp-finance` 字段名猜错，
根因是"写事件契约前没读消费方的设计计划"。这次先读了 `erp-sales.md` §4（它把这条 subject
占位了但没定 payload 形状），才发现归属字段这个洞。→ `crm-opportunity.md` §4.1。

### 5. 🧭 "CRM 与 ERP 零同步边"在 crm-opportunity 身上第一次真正被考验

附录 E 的链路**看起来**就是"赢单 → 建订单"，很容易顺手写成一次同步调用。三条后果：
赢单会因为库存不足而失败（销售明明已经赢了）、只买 CRM 的客户装不了、
后续七个 `crm-*` 组件会照抄这个先例。

**正确形态是事件握手**，且**订单建不建得成与商机赢没赢是两件独立的事**——
阶段三 Task 14 要真的制造一次库存不足，断言**商机仍然是 `WON`**。


---

## 第二轮补做：全组件资料查证（四处改了设计）

> 第一轮只查了 `infra-print`。这一轮把其余组件补齐，**结果四处改了设计、两处验证了原设计、一处让
> 一个待决问题当场有了答案**。下面按"改了设计"排前面。

### ⭐ 1. ✅ 钉钉的"发送"是异步的——`integration-im-dingtalk` 状态语义整个改了

**查证结果**：

| 事实 | 说明 |
|---|---|
| 接口叫 **`asyncsend_v2`**，返回 **`task_id`** | 只代表"钉钉受理了这个任务" |
| 另有 **`getsendprogress`** / **`getsendresult`** 两个接口 | 真实投递结果要单独回查 |
| **`agent_id`（微应用 id）与 `msg_body` 必填**；`userid_list`/`dept_id_list`/`to_all_user` 三选一 | 意味着必须是**企业内部应用** |
| 手机号查 userid 的接口是 `topapi/v2/user/getbymobile` | ✅ 确认存在，`infra-iam-casdoor` §9-3 定的"走手机号匹配"路线成立 |

**改了什么**：初版设计里适配器提交成功就发 `success: true`——**那是谎报送达**。症状极难查：
通知中心记着"已发送"、用户说"没收到"，两边都有"证据"。

改成**两阶段**（`phase=ACCEPTED` → `phase=CONFIRMED`），`delivery_attempts` 存 `task_id`，
并连带要求 `infra-notification` 的状态机加一个 **`ACCEPTED`（已受理待确认）中间态**。

⚠️ **不能省掉回查只发 ACCEPTED**——那样"用户不在企业里"这类最常见的失败永远发现不了，
`dingtalk_user_map` 的失效映射也永远清不掉。

**🔍 仍待核**：调用频率限制与每日上限（文档里没查到明确数字）。

### ⭐ 2. ✅ Novu 的偏好模型有两条我完全没想到的——`infra-notification` 补了两个概念

| 查到的 | 我原设计 | 改成 |
|---|---|---|
| 偏好分**全局通道偏好**与**工作流级偏好**，**全局覆盖工作流级** | 只有一层（`sub × 类别 → 通道`） | 两层，全局优先 |
| ⭐ **`critical` 工作流不出现在用户偏好界面里，用户关不掉** | 无此概念，用户可关任意通道 | **审批待办标 critical** |

**`critical` 这一条对我们是必需的，不是锦上添花**：审批通知若能被用户关掉，业务链路就断了——
"我没收到审批通知所以没审"会变成常见投诉，**而系统看起来一切正常**（记录是 `SUPPRESSED`，
"按用户意愿"）。

**来源**：[Novu · Notification Preferences Concepts](https://docs.novu.co/platform/concepts/preferences)

### ⭐ 3. ✅ Odoo `mail.activity` 有一个我漏掉的一等字段——`infra-workflow` 补 `due_at`

`mail.activity` 的字段构成：`res_model_id` + `res_id`（多态挂到任意单据）、`user_id`、
**`date_deadline`**、`summary`、`activity_type_id`。

**漏掉 `date_deadline` 的后果不是少个字段**，是"超期未审批"这个 ERP 里最常见的待办诉求
**整个无从表达**（列表排序、超期提醒、超期报表全落空），而补它要改表结构。

顺带加了一条 `infra.workflow.task.overdue.v1` 旁路事件——⚠️ **但"超期了要怎样"不归 workflow**
（铁律一：升级/催办/自动通过都是业务规则），它只报告事实。

**来源**：[odoo/addons/mail/models/mail_activity.py](https://github.com/odoo/odoo/blob/14.0/addons/mail/models/mail_activity.py)

### ⭐ 4. ✅ Casdoor 其实支持自定义 claims——但被推翻的方案依然被推翻

**查证结果三条**：

1. Webhook 在 `Settings > Webhooks` 配 URL + 选事件，**没有内置签名，但可以加自定义 header**
   → 校验方式定为"比对共享密钥 header"，解决了设计计划 §9 第 2 条的一半；
2. `User.Properties` 是 `map[string]string` 自定义属性；
3. ⭐ **token format 选 `JWT-Custom` 时，可以把 `Properties.<key>` 作为自定义 claim 放进 token。**

**第 3 条值得单独记一笔**：它说明初版那个"把角色镜像进 Casdoor 让它签"的方案**技术上完全可行**。
**我们否决它纯粹是架构理由**（踢人链路断、死循环、把角色焊回 Casdoor），不是因为做不到。

> **这个区分很重要**：一个方案"做不到"和"做得到但不该做"，在文档里必须分清楚——
> 否则半年后有人查到 Casdoor 支持自定义 claims，会以为我们当初是不知道才没用。

**来源**：[Casdoor · Webhooks Overview](https://casdoor.org/docs/webhooks/overview/) ·
[Casdoor · Application terminology](https://casdoor.ai/docs/application/terminology/)

### 5. ✅ 验证了原设计的两处（没改，但从"assumed"变成"verified"）

| 组件 | 查到的 | 结论 |
|---|---|---|
| `infra-bff-mobile` | GraphQL Yoga **内置 Persisted Operations 插件，明确支持"构建期注册表 + 拒绝未知哈希"的 safelisting 形态**（与运行时注册的 APQ 是两回事）；depth/complexity 有现成的 `graphql-armor` | §3.4 的设计**原本就对**，现在是验证过的。官方口径另加一条：同时有 Yoga 版与 Envelop 版插件时优先用 Yoga 版（走 HTTP 层 hook，可跳过执行步骤） |
| `frontend-standard` | vue-vben-admin 的权限流程确实是"后端返回角色码 → 递归过滤路由表 → `router.addRoute` 动态挂载"，路由用 `meta.roles` 声明 | **决策 112 的判断准确**。⭐ 但查出一处比"读它不装它"更可操作的：**`addRoute` 动态挂载机制本身可以照搬**，要换的只是过滤依据 |

**来源**：[Yoga · Persisted Operations](https://the-guild.dev/graphql/yoga-server/docs/features/persisted-operations) ·
[Vben Admin · 路由和菜单](https://doc.vben.pro/guide/essentials/route.html)

### 6. ✅ 让一个待决问题当场有了答案：`crm-opportunity` 的 `probability`

Odoo `crm.lead`：`probability`（0–100，**可由阶段自动带出、也可手工覆盖**）、`expected_revenue`
（赢单预期金额），**加权值即时算不落库**；失单必须记 loss reason。

→ 设计计划 §9 第 4 条从 📋 变成 ✅：**阶段带默认值 + 允许手工覆盖，加权金额算出来不存**
（派生值存了就会漂移）。

⚠️ **一处刻意不学**：Odoo 里 `probability=100` 即视为赢单并自动关闭。**我们不学**——
赢单是显式的 `MarkWon` 动作，它会触发建单、锁库存、生成应收一整条链路，
**不能被一个百分比数字顺手触发**。

**来源**：[odoo/addons/crm/models/crm_lead.py](https://github.com/odoo/odoo/blob/14.0/addons/crm/models/crm_lead.py) ·
[Cybrosys · Odoo 17 CRM 阶段与管道](https://www.cybrosys.com/blog/an-overview-of-managing-stages-and-pipelines-in-odoo-17-crm)

### ⭐ 7. ✅ `frontend-standard` 开工前第一件事：vxe-table 免费/企业版边界核实完毕，锁定

设计书 §12.6.6 点名的"三条地板"（可编辑单元格+Tab 连续录入、虚拟滚动、冻结列）**全部在 MIT 免费部分**，
不需要替换成 AG Grid Community / RevoGrid / TanStack Table。

**核实结果**（2026-09-09，`vxe-table@4.21.9`，来源 GitHub README + 官方文档站）：

| 能力 | 免费 / 企业版 | 备注 |
|---|---|---|
| 虚拟滚动（万行不卡） | ✅ 免费 | 核心包自带，非插件 |
| 冻结列 + 固定表头 | ✅ 免费 | |
| **可编辑单元格 + Tab 键连续录入** | ✅ 免费 | 三条地板里风险最高的一条，**确认不收费** |
| 树表格 | ✅ 免费 | |
| Excel 导入导出/打印 | ✅ 免费 | |
| 甘特图适配 | ✅ 免费 | v4.16 起 release note 写"适配 Gantt 甘特图"，是"能配合甘特图库用"，不是 vxe 自己实现甘特图——本阶段不用（§9 第 4 条已定不做甘特图场景），不影响判定 |
| 数据汇总 / 聚合函数（透视表类） | 💰 **企业版**（`store.vxeui.com`） | 落在"地板"之外的次要能力 |
| 单元格区域选取 / 复制粘贴 / 查找替换 | 💰 **企业版** | 同上 |
| 集成图表（表格内嵌 ECharts 类图表） | 💰 **企业版** | 本项目图表走独立 ECharts（§12.6 已锁定），不需要 vxe 这个集成能力 |

**判定**（按设计书 §12.6.6 第 2 步规则）：三条地板全部免费 → **锁定 vxe-table，不替换**。
企业版收费的三项（数据汇总/聚合、单元格区域选取全家桶、集成图表）本阶段业务页面
（§1.1 的 7 组列表/详情页）都用不到——列表页的合计/分组需求用后端聚合返回，
不需要前端表格自己做透视；真要用到时按 §12.6.6 的规则记一行逃生口
（`ui-kit-pc/README.md`），而不是绕开表格自己重实现。

⚠️ **两点局限，如实记录**：① 免费版功能列表以 GitHub README 的功能清单 + 官方博客
release note 为准，没有拿到 vxe 官方逐条列出的"企业版功能全集"页面原文（`store.vxeui.com`
是纯落地页，没有可抓取的功能矩阵文本）——即两处独立信源（README 功能清单 + 搜索结果里第三方
教程摘要）交叉印证一致，但都不是"官方逐条对照表"；② Tab 键连续录入具体到"编辑器内
Tab/Enter/方向键换格"这一交互细节，官方文档的 `table/edit/keyboard` 示例页面本身没有拿到
完整正文（`WebFetch` 只抓到标题），是从 README 的"键盘导航"归入免费功能清单这条推出的，
不是直接看到那个交互 demo 本身可用。**真做到这一步时如果发现 Tab 连续录入实际要企业版
才能用，按 §12.6.6 判定规则退回 AG Grid Community / RevoGrid 评估**，不要将错就错继续拿
vxe 硬凑。

**来源**：[GitHub · x-extends/vxe-table README](https://github.com/x-extends/vxe-table) ·
[VxeUI 企业版](https://store.vxeui.com) · `npm view vxe-table version` → `4.21.9`（2026-09-09 核实）
