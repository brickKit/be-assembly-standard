# 阶段三调研记录（SOP-R R-3 的完整版）

> 用途与 [`02-阶段二.md`](./02-阶段二.md) 完全一致：设计计划 §8 是精炼版，这里是完整版，
> 给"要核对某条判断是否真的查证过"的人看。**一个阶段一份，不往上一份里加**（单份文档不许无限膨胀）。
>
> ⚠️ **本阶段的调研方式与阶段二不同，读之前先知道这一点。** 阶段二那份是三个后台 agent 并行读
> ERPNext / Odoo / Tryton / metasfresh 源码得到的原始发现；本阶段的记录分成**两类**，每条都标了是哪一类：
>
> | 标记 | 含义 | 可信度 |
> |---|---|---|
> | 🧭 **架构推理** | 从设计书既有条款 + 本项目已建成组件的先例推导出来的，**不依赖外部源码** | 结论可直接用；错了是我们自己的推理错 |
> | 🔍 **待核实** | 依赖某个外部项目"实际怎么实现"的判断，**目前基于既有知识、尚未对着源码逐行核对** | **开工前必须核对**，对应设计计划 §9 的待决问题 |
>
> **不许把 🔍 当成 🧭 用。** 阶段二的教训（复盘 §2.1）是"想当然地假设平台行为、实测才发现相反"——
> 外部项目的行为同理，没真的读过就老实标着。

---

## 目录

- [infra-iam-casdoor：适配层与 claims 镜像](#infra-iam-casdoor适配层与-claims-镜像)

---

## infra-iam-casdoor：适配层与 claims 镜像

**采纳进**：[`infra-iam-casdoor.md`](../infra-iam-casdoor.md) §3.2（镜像同步 + 顺序保证）、§4（事件）、§8（参考实现）；
连带改了 [`infra-authz.md`](../infra-authz.md) §4（新增一条消费边）与设计书 §6.1（两句被第 14 章推翻的话）。

### 1. 🧭 设计书 §6.1 与第 14 章互相矛盾，且矛盾指向一个真实的实现难题

不是文字瑕疵，是两条硬约束顶在一起：

| 约束 | 出处 | 原文大意 |
|---|---|---|
| 登录流不经过适配层 | §6.1 | 浏览器直连 Casdoor 走标准 OIDC |
| JWT 必须带 `roles[]` / `dept_path` / `org_id` | §14.1.5 | 而这些数据在 `infra-authz` 的表里，Casdoor 不知道 |

**推导**：Casdoor 是签发者，签发时不问任何人（否则就"经过适配层"了）；那么它签的 token 里要有角色，
角色就只能**在签发之前**已经在 Casdoor 里。→ 唯一解是镜像同步。

**这条推理不依赖任何外部项目的实现细节**，所以标 🧭。它推翻的是 §6.1 那句"分配在 iam"——如果角色本来就
分配在 iam（Casdoor）里，这个问题根本不存在；正因为第 14 章把角色挪去了 `infra-authz`（为了换 Keycloak 时
不迁数据），才需要镜像这一步。**§6.1 那句话是决策 115 之前的世界观残留**，已回写。

### 2. 🧭 朴素的镜像会造出死循环——这是本阶段设计里最值钱的一条

完整推导见 [`infra-iam-casdoor.md`](../infra-iam-casdoor.md) §3.2。要点：

```
authz 改角色 → 立刻写 stale_since → 组件 401 token_stale → 前端刷新
  → Casdoor 还没同步到 → 刷回来还是旧角色 → 再 401 → 转圈转不出去
```

**症状特别值得记一笔**：用户侧看到的是"页面一直转圈"，服务端**每一条日志都正常**（401 是预期响应、
刷新是预期行为），没有任何一处会报错。这与导读里那批"不报错、没症状"的坑是同一类。

**解法**：`stale_since` 的写入时机往后挪一格，等 `infra.iam.claims.synced.v1` 确认镜像落地了再写。
代价是多一跳事件（毫秒级），而生效时延的大头是 bundle 的 15 秒轮询，**总时延不变**。

⚠️ 这条要求 `infra-authz` 消费一条它原本不消费的事件，已同步改它的设计计划 §4。**跨组件设计的连锁改动
必须当场做掉**——阶段二复盘 §2.1 记的 `erp-finance` 字段名猜错，根因就是"两份设计计划各写各的、没有对照"。

### 3. 🔍 Casdoor 的自定义 claims 到底写哪里——**开工前必须读源码确认**

镜像同步要往 Casdoor 写东西，但**写哪个字段目前是三选一，我没有对着源码确认过**：

| 候选 | 猜测的样子 | 如果是它，代价 |
|---|---|---|
| `User.Properties`（自由 KV） | 写一个 map，Casdoor 签 token 时带上 | 最理想：不碰 Casdoor 的 RBAC 概念 |
| Casdoor 原生的 Role 对象 | 在 Casdoor 里建同名角色再绑用户 | 次之：等于在 Casdoor 里维护一份角色副本，换 Keycloak 时这部分要重写（但角色**数据**仍在 authz，不算违反 §14 的切分） |
| Application 级 token 字段配置 | 在应用配置里声明"token 带哪些字段" | 要看它支不支持动态字段 |

**要读的地方**：`object/token_jwt.go`（token 里带哪些字段是怎么决定的）、`object/user.go` 的 `Properties`
字段用途、application 的 token 配置结构。→ 设计计划 §9 第 1 条。

⚠️ **这条不确定不影响 §3.2 那个顺序保证的成立**——无论写哪个字段，"先镜像、后写 stale_since"都对。
所以设计可以先定，实现细节待核。

### 4. 🔍 Casdoor webhook 的投递保证与签名机制

`webhook_deliveries` 这张去重表的设计（去重键取什么、要不要轮询兜底）取决于 Casdoor 到底给不给：
① 稳定的投递 ID；② 签名头；③ 失败重投。**三个都没确认过。**

**最坏情况的对策已经想好**：如果 webhook 既没有投递 ID 也没有签名，就退化成"定期轮询 Casdoor 的用户列表
+ 按 `updated_time` 增量对账"，`webhook_deliveries` 换成 `user_sync_cursor`。这条退化路径不影响其他任何
组件——`infra.iam.user.*.v1` 事件照发，下游看不出区别。→ 设计计划 §9 第 2 条。

### 5. 🔍 钉钉 unionid 的来源——**这条会牵连 `infra-notification`**

`infra-notification` 要把审批通知发给某个人，需要这个人的钉钉 id。链路上唯一可能持有它的是 Casdoor
（如果客户用钉钉扫码登录，Casdoor 作为 OIDC Provider 对接钉钉时会拿到 unionid）。

**没确认的是**：Casdoor 把第三方登录拿到的 unionid 存在哪、能不能通过 API 读出来。

**两条分支**（开工时二选一，`infra-notification` 的设计要能兼容两者）：

| 分支 | 如果 Casdoor 存了 unionid | 如果没存 |
|---|---|---|
| 数据来源 | `infra.iam.user.*.v1` 事件带上 `im_accounts` | 需要人工维护一张"系统用户 ↔ 钉钉用户"映射表 |
| 归谁 | 本组件（从 Casdoor 读出来发事件） | `infra-notification`（它自己的通道偏好表里多一列） |

→ 本组件设计计划 §9 第 3 条；`infra-notification` 的设计计划要在它的 §9 里记同一条的另一半。

### 6. 🧭 明确没有参考、且不打算参考的

**Ory Hydra / Zitadel / Authentik 这类"自己实现 OIDC Provider"的项目**：不是没查，是**问题不成立**——
我们不实现 Provider，Casdoor 官方镜像才是 Provider。评估它们等于重新讨论"要不要自己写 IdP"，
那个问题决策 86（零代码的东西不包成组件）与决策 87（JWT 本地验签）早就答完了。

**Casbin**：`infra-authz` 设计计划 §8 已经判过（只看模型分层、不引入）。本组件与它更无关——
我这一层不做任何判定。
