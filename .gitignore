# BrickKit CLI 生成的文件（不提交到 Git）
.brickkit/generated/

# 登录凭据（包含 Token）
.brickkit/credentials

# 环境变量文件（包含密码）
.env

# 组件源码目录：本项目用 git submodule 记住每个组件仓库的地址与精确 commit，
# 所以 components/ 本身要被跟踪（.gitmodules 就是设计书 §3.4.1 要的那张
# 「客户 → 组件仓库」映射表）。只忽略 brickkit sync 的归档区。
components/.archived/

# API 契约与 Manifest 缓存：默认提交、团队共享。不想提交就取消下面两行的注释
# .brickkit/artifacts/
# .brickkit/manifests/

# 基础资源的密钥目录（证书、keyfile 等），永不提交
infra/**/secrets/
build/
